---
column: review
agent: claude-opus
live: false
labels: [infra, frontend]
priority: high
updatedAt: 2026-08-22T16:05:00.000Z
---
# Paraglide i18n toolchain and guards

Stand up i18n per decisions/37-i18n-paraglide.md. Verified setup facts and
gotchas are in the research note — READ IT FIRST:
`/private/tmp/claude-501/-Users-jonathanturnock-Projects-ollama-webmcp-chrome/81c762bd-d84f-407b-b12d-2f806b3f03d9/scratchpad/i18n-research.md`
(Paraglide 2.x config for plain Vite + CRXJS, the missing-key gap that our
guard closes, localStorage-vs-chrome.storage strategy verification, the
reload-on-switch default, _locales mechanics, pt-BR casing pitfall).

No copy extraction in this card — prove the pipeline with a handful of
strings (e.g. the options page header) end to end.

## Checklist

- [x] Paraglide installed: project.inlang, messages/en.json, vite plugin with emitTsDeclarations, codegen in postinstall so check/tsc always see generated messages; m.key() proven typed (planted unknown key fails check)
- [x] `npm run guard:i18n` (scripts/guard-i18n.mjs): key-set diff of every locale vs en — missing OR orphan keys fail with file+key output; wired into npm run guard; proven with a planted violation
- [x] Locale strategy configured; verified whether sidepanel+options share localStorage (same origin) — if not, custom chrome.storage strategy; language picker added to options Settings (uses default reload-on-switch)
- [x] lang/dir bootstrap module run before mount on both surfaces (RTL set: ar)
- [x] _locales/en/messages.json scaffold + manifest default_locale + __MSG_extName__/__MSG_extDescription__ wired via manifest.config.ts; build verified to produce a loadable extension
- [x] npm test, npm run check, npm run guard, npm run build, npm run verify green

## Gates

- [x] tests — npm test green, 950 passed / 61 files, unchanged from the pre-card baseline (claude-opus, 2026-08-22T16:05:00Z)
- [x] check — svelte-check 1099 files 0 errors 0 warnings, tsc -p tsconfig.node.json clean (claude-opus, 2026-08-22T16:05:00Z)
- [x] guard — all SIX gates green including the new guard:i18n (claude-opus, 2026-08-22T16:05:00Z)
- [x] build — vite build green; dist/_locales/en/messages.json packaged, manifest carries default_locale + both __MSG_ placeholders (claude-opus, 2026-08-22T16:05:00Z)
- [x] verify — 9/9 required checks passed, screenshots best-effort PASS (claude-opus, 2026-08-22T16:05:00Z)

## Comments

- **claude-opus** (2026-08-22T15:40:00.000Z): Claimed the card. Baseline before touching anything: 950 tests / 61 files, `npm run check` 1088 files 0 errors, `npm run guard` green — so every failure below is mine, not inherited. Installed `@inlang/paraglide-js@2.24.1` and scaffolded the project by hand rather than through `npx paraglide-js init` (it is interactive and wants to rewrite tsconfig): `project.inlang/settings.json:1-11` (baseLocale `en`, `locales: ["en"]`, the message-format plugin, `pathPattern: ./messages/{locale}.json`) and `messages/en.json:1-25`. One gotcha the research note did not have: the docs' object form for a plural message does NOT compile — `@inlang/plugin-message-format` runs the file through `flatten()` first and reads a complex message as `value[0]`, so the `{ declarations, selectors, match }` block must be wrapped in an ARRAY (`messages/en.json:16-25`). The object form fails with a bare "Message must have at least one variant".
- **claude-opus** (2026-08-22T15:45:00.000Z): THE LAYERING DECISION, since it is the one thing this card had to settle before any copy could move. `src/paraglide` is NOT a fifth layer — it is GENERATED PRESENTATION, and it sits beside `src/ui` in the direction of the arrows: everything that renders may reach it, it reaches nothing. Encoded in `.dependency-cruiser.cjs:285-360` as three edits rather than an exclusion, because excluding the tree from the cruise would have dropped the edges INTO it and made the rule that matters unenforceable: (1) `shared-ui-is-ui-only` widened to `^src/(?!ui/|domain/|paraglide/)` so a shared component may call `m.*` (cards 101-105 need this); (2) a new `paraglide-is-not-for-the-domain` — `domain-is-pure` already forbids the same edge, and both firing is deliberate, the pattern `ui-does-not-import-infra` already uses next to `only-roots-construct-infra`, so a domain module reaching for `m` gets told about the error-CODE convention (decisions/34, decisions/37) and not just about purity; (3) a new `paraglide-is-a-leaf`, which is what makes excluding the tree from the OTHER guards safe — if the codegen ever grows an edge back into our source, the exclusion becomes a blind spot and this rule says so. Guard exclusions were mostly already in place from card 91's foresight (`scripts/lib/source-scan.mjs:30` `VENDORED`, `biome.jsonc:120`); this card added the type-program one, `tsconfig.app.json:83-99` — without it the `src/**/*.js` include glob drags every generated module in as a ROOT file to be checked under maximal strictness. `emitTsDeclarations` is what makes that exclusion free: TS resolves the `.d.ts` in preference to the `.js`, so call sites stay fully typed while the generated tree is an opaque dependency.
- **claude-opus** (2026-08-22T15:50:00.000Z): Build wiring. `paraglideVitePlugin` is FIRST in the plugin list (`vite.config.ts:31-47`) because it writes source. The compiler options are a single shared object, `paraglide.options.mjs:1-63`, imported by both the Vite plugin and `scripts/compile-i18n.mjs:1-24` (`postinstall`) — two copies of the `strategy` chain would drift in the one window nobody looks at, between `npm ci` and the first build. `.mjs` not `.ts` because postinstall runs on a bare install with no TS loader guaranteed; `tsconfig.node.json:14-24` gains `allowJs` (not `checkJs`) so `vite.config.ts` type-checks against its JSDoc annotation. PROOF the postinstall step earns its place: `rm -rf src/paraglide && npm run check` → 7 errors across 5 files ("Cannot find module '../paraglide/messages.js'"); `npm run compile:i18n` then `npm run check` → 1099 files, 0 errors.
- **claude-opus** (2026-08-22T15:52:00.000Z): PLANTED-VIOLATION PROOF, direction 1 (the compiler's job). `m.optionsPageTitle()` → `m.optionsPageTitleTypo()` in `src/options/App.svelte:25`: `npm run check` exits 1 with `Property 'optionsPageTitleTypo' does not exist on type 'typeof import(".../src/paraglide/messages/_index")'. Did you mean 'optionsPageTitle'?`. Reverted, green. Typed PARAMS too, not just keys: dropping the argument from `m.historyClearAllButton({ count })` in `src/options/components/HistorySection.svelte:168` gives `Expected 1-2 arguments, but got 0.`, exit 1. Reverted, green. Also planted a `src/domain/settings/index.ts` → `src/paraglide/messages.js` import and confirmed the cruise reports BOTH `paraglide-is-not-for-the-domain` and `domain-is-pure` as errors; and planted the same import in `src/ui/storageMessage.ts` to confirm the UI edge is genuinely ALLOWED (0 errors). Both reverted.
- **claude-opus** (2026-08-22T15:54:00.000Z): PLANTED-VIOLATION PROOF, direction 2 (nobody's job until now). `scripts/guard-i18n.mjs:1-160` diffs each declared locale's key set against the base locale's, reading `project.inlang/settings.json` rather than globbing so a DECLARED locale with no file is a failure rather than an absence nobody notices. It also records each key's KIND — a locale that translates a plural as a flat string silently loses the plural, which is the same class of bug as a missing key. Planted a `de` locale missing `optionsPageSubtitle`, carrying an orphan `someKeyOnlyGermanHas`, and demoting `historyClearConfirmTitle` to a flat string: 3 violations, exit 1, each printed as `messages/de.json — MISSING/ORPHAN/…  key "…"`. Then deleted `messages/de.json` while leaving `de` declared: 1 violation, exit 1. Reverted; guard green at `base locale "en", 1 declared locale(s) [en:7]`. The single-locale case is NOT special-cased — the loop still runs for `en` itself, where the diff is trivially empty but the file-exists and structure checks are real, so nothing here changes when card 105 adds the other nine.
- **claude-opus** (2026-08-22T15:58:00.000Z): THE ORIGIN-SHARING FINDING, verified in a real Chrome rather than reasoned about. Drove the built `dist-verify/` through the harness's own launcher (`verify/lib/browser.mjs`) and probed both surfaces: side panel and options page report the SAME `location.origin` (`chrome-extension://dkbjkgonkfcfbjghmglnlfmnpkipciob`), a key written from the side panel reads back in the options page and vice versa, and `PARAGLIDE_LOCALE` set from options is visible in the panel. So THEY SHARE ONE localStorage, and decision 37's plain `["localStorage", "preferredLanguage", "baseLocale"]` chain works as written — the custom `chrome.storage.local` strategy the research note kept in reserve is NOT needed. Same run confirmed the `_locales` wiring end to end: the extension loads, `chrome.runtime.getManifest().name` comes back as `"OpenChat (WebMCP)"` (Chrome resolved `__MSG_extName__` itself) and `chrome.i18n.getMessage("extDescription")` returns the full description. And the bootstrap: both documents come up `lang="en" dir="ltr"`, where `dir` proves `applyDocumentLocale()` ran — neither `index.html` carries a `dir` attribute, so the default would be the empty string.
- **claude-opus** (2026-08-22T16:00:00.000Z): The surface work. `src/infra/dom/document-locale.ts:1-48` is the lang/dir bootstrap, next to `dark-mode.ts` because it is the same shape — an attribute on the document element that must be right before the first paint, wired by a composition root. It takes the locale AND the direction as ARGUMENTS: the roots read them (`src/sidepanel/main.ts:143-152`, `src/options/main.ts:71-79`) so the adapter's only dependency stays the document, and the RTL question stays with Paraglide's own `getTextDirection()` — 2.24 resolves it from `Intl.Locale`'s text info with a hardcoded `ar`/`he`/… fallback set, which is strictly better than the hand-rolled set I first wrote and, more to the point, cannot disagree with itself. Card 104 should call it directly rather than re-deriving. The language picker is the first section of `src/options/components/SettingsSection.svelte:222-253`, deliberately NOT stored through the `SettingsStore` port — Paraglide already owns that value in localStorage and mirroring it into `chrome.storage` would give one setting two sources of truth, the authoritative one being the copy we do not control. It renders one option today (`English`, as its own endonym via `Intl.DisplayNames`) rather than hiding until there are two. Copy extracted this card, and no more: the options header (`src/options/App.svelte:24-29`), an INTERPOLATED string and a PLURAL VARIANT (`HistorySection.svelte:167-172`, replacing a `sessions.length === 1 ? "" : "s"` ternary that is right in English and wrong in Russian and Arabic), plus the three language-picker strings. Seven keys total.
- **claude-opus** (2026-08-22T16:05:00.000Z): Gates all green — `npm test` 950/950 (unchanged), `npm run check` 1099 files 0 errors, `npm run guard` all six, `npm run build`, `npm run verify` 9/9 with screenshots PASS (the options screenshot shows the Language card and the interpolated `Clear all history (6)`). Docs updated for the sixth gate: `README.md:126-140`, `docs/01-architecture.md:445-459`, `.claude/skills/pre-commit/SKILL.md:10-16`. NOT committed, per instruction. Two things cards 101-105 should know. (1) The message-format plugin is fetched from jsDelivr and cached in `project.inlang/cache/` (gitignored), so the FIRST `postinstall` on a fresh clone needs network; every later one is offline. If that ever becomes a problem the fix is a local module path in `settings.json`, not a workaround in the guard. (2) Component tests that assert English copy will start failing the moment card 101 extracts the strings they read — decision 37 anticipates this ("tests assert through the message functions"), and the 950 still pass here only because this card touched copy no test reads.
