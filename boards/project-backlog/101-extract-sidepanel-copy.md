---
column: review
agent: claude-sonnet
live: false
labels: [frontend]
priority: high
updatedAt: 2026-08-24T02:12:00.000Z
---
# Extract all side-panel copy to messages

Move every user-facing string in src/sidepanel/** (and src/ui/ shared
components it renders) into messages/en.json with m.key() usages, per
decisions/37-i18n-paraglide.md. Includes: header/menu items, composer
placeholder and states, transcript notices and retry affordances, empty
states, approval card copy, tools/call-log labels, history rows, tooltips,
aria-labels (localized too — they're user-facing), and the error-code →
message maps for provider/storage/tool failures surfaced in the panel
(codes come from the domain vocabularies per decision 34 — no copy remains
in src/domain). Interpolations become typed params; counts use variants.
Key naming: stable, component-scoped, reviewable (e.g. composer_placeholder,
tools_empty_title) — journal the convention.

## Checklist

- [x] Zero hardcoded user-facing strings left in src/sidepanel/** (grep sweep for literal text in markup; dev-only console/debug strings exempt and journalled)
- [x] aria-labels and accessible names localized; verify/checks/screenshots.mjs selectors updated to read the same messages (en default keeps captures stable)
- [x] Error-code→copy maps live UI-side; domain exports codes only
- [x] Component tests assert via message functions, not string literals
- [x] npm test, npm run check, npm run guard (incl. guard:i18n), npm run build, npm run verify green

## Gates

- [x] tests — npm test green, 950/950 (unchanged), test files migrated to assert via m.key() (claude-sonnet, 2026-08-24T02:10:00Z)
- [x] check — svelte-check 1293 files 0 errors, tsc -p tsconfig.node.json clean (claude-sonnet, 2026-08-24T02:10:00Z)
- [x] guard — all six gates green including guard:i18n (200 en keys, single locale) (claude-sonnet, 2026-08-24T02:10:00Z)
- [x] build — vite build green, paraglide compiles into the bundle (claude-sonnet, 2026-08-24T02:10:00Z)
- [x] verify — 9/9 required checks passed, screenshots best-effort PASS (accessible-name locators unaffected — English wording preserved verbatim) (claude-sonnet, 2026-08-24T02:10:00Z)

## Comments

- **claude-sonnet** (2026-08-24T00:30:00.000Z): Claimed the card. Surveyed every file in src/sidepanel/** (37 components/stores/services) plus the src/ui shared surface the panel renders (Markdown.svelte/markdown.ts, storageMessage.ts) before writing a single line — read every `.svelte`/`.ts` end to end rather than grepping blind, since the card asks for aria-labels/tooltips too, which grep alone misses. Also read decisions/37, card 100's journal (boards/project-backlog/100-*.md), and the generated `messages/historyclearallbutton3.js` to confirm the compiler's exact `{param}` interpolation syntax before writing any message text.
- **claude-sonnet** (2026-08-24T00:50:00.000Z): KEY CONVENTION, settled before extracting: `component_camelCaseDescription`, e.g. `composer_placeholder`, `toolsPanel_emptyPageTitle` — matches the card's own examples exactly (card 100's own keys, `optionsPageTitle`/`historyClearAllButton`, predate this convention and were left as-is rather than renamed, since renaming them is card 102/105's call, not mine to make on a file I don't own the other half of). A second tier of GENERIC, cross-component keys with no prefix — `loadingProvidersLabel`, `openOptionsAction`, `retryAction`, `copiedLabel`, `argumentsHeading`/`errorHeading`/`resultHeading`, `thisPageLabel`, `runningLabel`, `connectionStatus_*` — used wherever two or more components render the exact same word for the exact same concept (verified by re-reading both call sites before reusing, never by string-matching alone). `messages/en.json:24-38` lists them up front. This is a deliberate departure from strict "one key per literal occurrence": a `retryAction`/`copiedLabel` etc. shared verbatim keeps a copywriter (card 103) from having to remember to edit the word in four places, and keeps the file from drifting the way `capabilityBadge`/`connectionStatus` already had (see the DRY finding below).
- **claude-sonnet** (2026-08-24T01:10:00.000Z): PARAGLIDE FORMAT GOTCHA, worth flagging for 102-105: a literal `{` in message TEXT (not meant as an interpolation) is silently misparsed as a placeholder with the literal contents between the braces as the param NAME — `"{ } (empty object)"` compiled to a function expecting an input literally named `" "` (a single space), which nothing could ever supply. Confirmed by compiling both ways and reading the generated `.js` (`src/paraglide/messages/toolargvalue_emptyobject3.js`). Fix: never write a bare `{`/`}` in message text that isn't a real `{paramName}`. This cost `toolArgValue_emptyObject` its bracket glyph (now "(empty object)" instead of "{ } (empty object)", `messages/en.json:187`) — the one deliberate reword in this card, everywhere else is extraction-only. `[ ]` for the empty-list case survives untouched since square brackets aren't special to the format.
- **claude-sonnet** (2026-08-24T01:30:00.000Z): MARKUP-PRESERVING STRATEGY for copy with inline `<strong>`/`<code>`/`<em>` or an untrusted interpolated value: two techniques, chosen per case. (1) STATIC copy with embedded markup and no untrusted param (ToolsPanel.svelte's three long empty-state descriptions, `messages/en.json:220-225`) renders via `{@html m.key()}` — safe because the string is 100% developer-authored, never touches page/tool-supplied data, exactly like Markdown.svelte's own `{@html}` use one layer up (that one sanitises; this one has nothing to sanitise, since nothing in it varies). (2) copy with an UNTRUSTED interpolated value that also needs a `<strong>` around just that value (App.svelte's origin-mismatch notice, ApprovalCard's "Runs on `<strong>`", Composer's needs-confirmation label) is split into `_prefix`/`_suffix` message pairs sandwiching a plain Svelte `{expression}` text node — Svelte's default text interpolation auto-escapes, `{@html}` would not. Tool names, server names and page origins are all page/server-supplied per decisions/02/17 and must never reach `{@html}`; `src/sidepanel/components/ToolCallRow.svelte`'s `toolCallRow_serverCallsThis({title})` is the sharpest example — `toolMcpAnnotations.title` is explicitly documented as attacker-influenceable.
- **claude-sonnet** (2026-08-24T01:45:00.000Z): DOMAIN-STRING FINDINGS (constraint: "if you find a user-facing string still produced IN src/domain, the mapping moves UI-side"). Fixed the one that was small, self-contained, and blocked correct pluralisation of a component I was already editing: `src/domain/chat/transcript-groups.ts:88-133`'s `ActivitySummary.countLabel` (a pre-pluralised English string, `"3 tool calls"`/`"1 tool call"`) is now `stepCount: number` — the domain can't pluralise per-locale at all, so it had no business producing the sentence, only the fact. `ActivityGroup.svelte`'s `summaryText` now calls `m.activityGroup_stepCount({count})` (a real plural-variant message). Updated `src/domain/chat/transcript-groups.test.ts:134-144` (`.countLabel` → `.stepCount`, asserting the number not the sentence). NOT fixed, and journalled here instead because the blast radius reaches well outside `src/sidepanel/**` into `src/options/**` (card 102's territory) and would need touching in one atomic step, not partially: (1) `src/domain/providers/provider.ts:160-177`'s `describeProviderError` — six English sentences ("Request was cancelled.", "Authentication failed (…): …", etc.) called from both `src/sidepanel/stores/selection.svelte.ts` and `src/options/components/ProvidersSection.svelte`. (2) `src/domain/providers/capability.ts:90-99`'s `reasonForCapability` fallback strings ("Tool support not verified for this model.", "This model doesn't support tool calling.") — same cross-surface reach, called from `ProviderPicker.svelte` (this card) and the options page's model rows. (3) `src/domain/chat/title.ts:21`'s `UNTITLED_CHAT = "New chat"` fallback, read by `titleFromMessages`/`titleFromSummary`, called from `App.svelte`, `HistoryListItem.svelte`, `OverflowMenu.svelte` — three call sites in THIS card's own scope, but the fix (inject the fallback as a required parameter, like `originLabel` was injected into `createChatService`) touches `title.test.ts`'s ~10 assertions too, and I drew the line at doing it without also auditing whether `src/options` reads the same function. (4) `src/domain/chat/turn.ts` (six+ sentences: "This page does not currently expose any callable tools…", "Stopped by the user before this call ran.", tool-call fallback errors) and `src/domain/tools/sign-in.ts`/`types.ts` (OAuth form validation prose, "Request was cancelled.") are the same pattern at larger scale — this is pre-existing debt from before decisions/34/37 landed, not something this card introduced, and a full sweep is its own card/decision, not a drive-by inside 101. Recommend a dedicated follow-up card once 102-105 land, so the UI-side home for this wording is chosen once for both surfaces rather than picked twice.
- **claude-sonnet** (2026-08-24T02:00:00.000Z): DRY FIX taken along the way (not required by the checklist, but directly caused by extracting the same word twice): `OverflowMenu.svelte` and `ContextChip.svelte` each carried their own identical `Record<ConnectionStatus, string>`, each flagged with its own clean-code DRY TODO. Extracted `src/sidepanel/presentation/connectionStatus.ts`'s `connectionStatusLabel()`, the same shape `capabilityBadge.ts`/`toolOrigin.ts` already establish, and deleted both TODOs along with both literal tables. `capabilityBadge.ts` and `toolOrigin.ts` themselves also moved onto messages (`capabilityBadge_noTools`/`_unverified`/`_toolCapable`, `thisPageLabel`) since both are presentation functions that produce copy several components render.
- **claude-sonnet** (2026-08-24T02:05:00.000Z): EXEMPTIONS (dev-only, left as literal English on purpose): `src/sidepanel/app-services.ts:105-126`'s two `throw new Error("[webmcp] …")` wiring-bug messages (never shown to a user — they crash a misconfigured composition root, read by a programmer in devtools); `src/sidepanel/main.ts`'s `console.log`/`console.error` trace/error lines; `src/sidepanel/stores/panel.svelte.ts`'s `window.__webmcpPanelDebug` snapshot surface (explicitly named in the card as exempt); `src/sidepanel/services/mcpTools.ts:102`'s one `console.warn`. `src/ui/components/ui/command/command-dialog.svelte`'s default `title`/`description` props ("Command Palette"/"Search for a command to run…") were left alone too — confirmed via a repo-wide grep that `CommandDialog` is never imported anywhere outside its own kit folder, so it is unreachable vendored scaffolding, not live UI (same `VENDORED` treatment the rest of `src/ui/components/ui` already gets from `scripts/lib/source-scan.mjs`/`biome.jsonc`).
- **claude-sonnet** (2026-08-24T02:08:00.000Z): TEST MIGRATION. Every component test that asserted English copy (`ApprovalCard.test.ts`, `Composer.test.ts`, `HistoryListItem.test.ts`, `HistoryPanel.test.ts`, `OverflowMenu.test.ts`, `ProviderPicker.test.ts`, `Transcript.test.ts`) now imports `m` and asserts through `m.key(...)` calls instead of string literals, per the checklist and decisions/37's stated reason ("copy edits don't silently break tests"). Two patterns: exact `getByRole(..., { name: m.key() })`/`getByText(m.key())` for whole-string matches, and `getByText(new RegExp(m.key({...params})))` where the copy is a SUBSTRING of a larger text node (e.g. a plural count sitting inside a joined `·`-separated line) — `new RegExp` on the resolved message text reuses the exact wording as the pattern rather than hand-writing a second, driftable regex. `storageMessage.test.ts` was left untouched: it already asserts via regex against the ASSEMBLED sentence (`storageFailureMessage`'s own composition), not a literal copy of the reason clauses, so it already satisfies the same principle. 950/950 tests pass unchanged in count — every rendered string is byte-identical to before (extraction-only, see the one exception above), so nothing needed a NEW expectation, only a NEW way of stating the old one.
- **claude-sonnet** (2026-08-24T02:11:00.000Z): Final grep sweep (fork, independent pass over every `src/sidepanel/**/*.svelte`/`*.ts` non-test file) came back clean: zero hardcoded `label=`/`placeholder=`/`aria-label=`/`title=`/`dismissLabel=`/`alt=` literals, zero sentence-shaped markup text outside developer comments, and the only `.ts` string-literal hits were doc-comment prose and `testing/fake-services.ts`'s `"Test Server"` mock fixture name (test-only, correctly left alone). Moving to review.
- **claude-sonnet** (2026-08-24T02:10:00.000Z): SCOPE COUNT: 198 new keys added this card (200 total in `messages/en.json`, 2 carried from card 100) across ~35 files (every `src/sidepanel/**` component/store/service that had copy, plus `src/ui/storageMessage.ts`, `src/ui/markdown.ts`, `src/ui/components/Markdown.svelte`, and the one domain fix in `src/domain/chat/transcript-groups.ts`). Also wired `document.title` in `src/sidepanel/main.ts` to `m.app_documentTitle()` — `index.html`'s static `<title>` is the pre-JS fallback, brought into the same locale as the rest of the document post-mount, same reasoning as the existing lang/dir bootstrap immediately above it. Gates: `npm test` 950/950, `npm run check` 1293 files 0 errors, `npm run guard` all six (guard:i18n reports `en:200`), `npm run build` green, `npm run verify` 9/9 + screenshots best-effort PASS (screenshot locators are accessible-name-based and untouched, since wording is byte-identical). NOT committed, per instruction. For card 102 (options-surface extraction, next): reuse the two-tier key convention (component-scoped + a small shared generic tier — don't be afraid to add to the generic tier rather than duplicate `retryAction`/`openOptionsAction`/`copiedLabel` a second time under an `options_` prefix); watch for the same literal-`{`/`}` Paraglide gotcha; and please read this card's domain-string findings above before touching `ProvidersSection.svelte`/`McpServersSection.svelte`, since `describeProviderError`/`reasonForCapability` are called from options code too and a fix there should cover both surfaces in one pass, not be re-discovered.
