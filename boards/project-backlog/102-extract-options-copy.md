---
column: review
agent: claude-sonnet
live: false
labels: [frontend]
priority: high
updatedAt: 2026-08-24T03:10:00.000Z
---
# Extract all options-page copy to messages

Same treatment as card 101 for src/options/**: section headings and
descriptions, the credential warning, provider/MCP forms (labels, hints,
validation errors, reserved-header messages), test-connection outcomes,
OAuth sign-in states, preset picker, history section incl. the clear-all
dialog, attribution section (the Flaticon licence text stays legally
intact — translate the surrounding copy, keep required attribution wording
per its licence), settings policies. Manifest strings (_locales) get their
en source finalized here too.

## Checklist

- [x] Zero hardcoded user-facing strings left in src/options/** (grep sweep — one deliberate exception, see journal)
- [x] Validation/test-outcome copy keyed by error codes from the domain vocabularies
- [x] Attribution licence requirements respected and journalled
- [x] _locales/en finalized (name, description; no action title exists to finalize — see journal)
- [x] Component tests assert via message functions
- [x] npm test, npm run check, npm run guard, npm run build, npm run verify green

## Gates

- [x] tests — npm test green, 963/963 (up from 950, +13: providerMessage.test.ts, capabilityMessage.test.ts) (claude-sonnet, 2026-08-24T03:00:00Z)
- [x] check — svelte-check 1498 files 0 errors, tsc -p tsconfig.node.json clean (claude-sonnet, 2026-08-24T03:00:00Z)
- [x] guard — all six gates green including guard:i18n (en:401 keys, single locale) (claude-sonnet, 2026-08-24T03:00:00Z)
- [x] build — vite build green, paraglide compiles into the bundle (claude-sonnet, 2026-08-24T03:00:00Z)
- [x] verify — 9/9 required checks passed, screenshots best-effort PASS (options-light/options-dark section headings byte-identical to before) (claude-sonnet, 2026-08-24T03:00:00Z)
- [x] optionsSmoke — node verify/checks/optionsSmoke.mjs, 13/13 checks passed against the real built extension (all its text/placeholder locators matched the extracted messages) (claude-sonnet, 2026-08-24T03:00:00Z)

## Comments

- **claude-sonnet** (2026-08-24T02:30:00.000Z): Claimed the card. Read every file in src/options/** end to end (App.svelte, all 13 components, all 5 forms/ modules, main.ts) before writing a line, plus card 101's journal (boards/project-backlog/101-*.md) for the key-naming convention and the three deferred domain-string findings this card owns. Reused card 101's two-tier convention exactly: `component_camelCase` scoped keys, plus a shared no-prefix generic tier for byte-identical words used by ≥2 components (`cancelAction`, `editAction`, `removeAction`, `showAction`/`hideAction`, `testConnectionAction`, `testingLabel`, `savingLabel`, `saveChangesAction`, `displayNameLabel`, `enterDisplayNameError`, `bearerTokenLabel`, `enableAction`/`disableAction`, `loadingLabel`, `providerType_ollamaLabel`/`providerType_openAiLabel`, `providers_addProviderAction`, `permissionGrantedBadge`/`permissionGrantedTitle`/`permissionNeededBadge`/`permissionNeededTitle`/`permissionNeededForHostBadge`/`permissionDeniedRetryMessage`, `headerValuesMaskedTitle`, `customHeaderCountLabel`). Also reused three EXISTING sidepanel keys verbatim where the options page's own wording was byte-identical: `loadingProvidersLabel`, `historyListItem_messageCount`, `historyListItem_toolCallCount` — DRY across surfaces, same as card 101's own capabilityBadge/toolOrigin extraction.
- **claude-sonnet** (2026-08-24T02:45:00.000Z): 201 new keys added to messages/en.json (200→401 total), across AttributionSection.svelte, HeadersEditor.svelte, HistorySection.svelte, McpServerForm.svelte, McpServerRow.svelte, McpServersSection.svelte, McpTestResult.svelte, PresetPicker.svelte, ProviderForm.svelte, ProviderRow.svelte, ProvidersSection.svelte, SettingsSection.svelte, and forms/headerRows.ts, forms/hostPermission.svelte.ts, forms/testConnection.ts, forms/testResultDisplay.ts (the latter two hold the "test-outcome copy keyed by error codes" checklist item — every `TestOutcome`/`McpTestOutcome` kind's sentence is now `m.testResultDisplay_*`/`m.testConnection_*`, switched on the domain's own `kind` discriminant). `src/options/main.ts` also now sets `document.title = m.optionsPageTitle()` post-mount, mirroring card 101's sidepanel treatment of the same pre-JS-fallback `<title>` pattern.
- **claude-sonnet** (2026-08-24T02:50:00.000Z): ATTRIBUTION LICENCE REASONING (checklist item). Flaticon's free-licence attribution (AttributionSection.svelte) names three required identifiers verbatim — the author's username "sonnycandra", the pack name "ECommerce", and "Flaticon" itself, each as a link to the exact required URL (flaticon.com/authors/sonnycandra, flaticon.com/packs/ecommerce-1340, flaticon.com/). These three are proper nouns / the licence's required identifiers and are kept UNTRANSLATED in every locale — only the connecting English words ("Icon by", "from the", "pack on") are inside the translatable `attributionSection_credit` message. The whole sentence (words + the three `<a>` tags) is one static, fully developer-authored string with zero interpolation, rendered via `{@html m.attributionSection_credit()}` — safe per card 101's technique 1 (`{@html}` is fine when nothing in the string is untrusted/dynamic). This keeps the attribution legally intact (the link + the exact author/pack/brand names never change) while letting every other locale read the surrounding sentence in its own language.
- **claude-sonnet** (2026-08-24T02:55:00.000Z): RICH-COPY TECHNIQUE, same {@html} rule applied to five more STATIC, no-interpolation blocks: HistorySection's description (in-page `#providers-heading` anchor) and storage warning (`<code>chrome.storage.local</code>`), ProviderForm's and McpServerForm's `headersDescription` snippets (`<code>x-api-key</code>`/`<code>Authorization</code>`), and SettingsSection's tool-safety-annotations notice (`<code>readOnlyHint</code>`). One DYNAMIC-value case needed a different shape: McpServerForm's manual-registration notice wraps `oauthDiscovery.issuer` (an authorization-server-supplied URL) in a real `<code>` Svelte element as a plain text node (auto-escaped, not `{@html}`) immediately followed by a separate static message (`mcpServerForm_manualRegistrationNotice`) — no prefix/suffix split needed since the dynamic value sits at the very start with nothing before it.
- **claude-sonnet** (2026-08-24T03:05:00.000Z): THE THREE DEFERRED DOMAIN-STRING FIXES (card 101's mandate). All three now split the domain/UI boundary the way decisions/34 asks, but with one real complication card 101 didn't flag: two of the three functions are ALSO called from domain-internal code this card was told not to touch.
  - **`describeProviderError`** (src/domain/providers/provider.ts:159): `src/domain/chat/turn.ts:313` calls it directly to build the terminal-error transcript message — turn.ts is explicitly out of this card's scope ("do not touch turn.ts's larger-scope strings"). Fix: left the domain function's ENGLISH implementation completely unchanged (turn.ts and capability.ts's `resolveCapability` keep working, byte-for-byte, with zero edits to either) and added `src/ui/providerMessage.ts` — a second, LOCALIZED `describeProviderError`, same switch, using `m.provider_*` keys — that `src/sidepanel/stores/selection.svelte.ts` and `src/options/components/ProvidersSection.svelte` now import instead. Two implementations of the same shape is the accepted cost, documented in both functions' own doc comments, with a named follow-up: a future card giving `ModelCapabilities` a raw `error?: ProviderError` field instead of pre-rendered `detail` text (and turn.ts a UI-independent failure report) could fold the domain-side copy away entirely.
  - **`reasonForCapability`'s fallback strings** (src/domain/providers/capability.ts:90): the two hardcoded English fallback sentences ("Tool support not verified for this model." / "This model doesn't support tool calling.") are REMOVED from the domain function, which now returns only `capability?.detail?.join(" ")` (`undefined` when there's no detail, full stop — no domain-invented copy). `src/ui/capabilityMessage.ts`'s new `capabilityReason` wraps it and supplies the two fallbacks via `m.capability_unverifiedFallback()`/`m.capability_noToolsFallback()`. Both call sites (`ProvidersSection.svelte`'s stale-default check, `ProviderPicker.svelte`'s row reason) now import `capabilityReason` instead of the domain export. `resolveCapability`'s OWN fallback (populating `ModelCapabilities.detail` from a failed `getCapabilities` call) still calls the domain-side `describeProviderError` internally — same accepted-debt shape as above, one hop removed.
  - **`UNTITLED_CHAT`** (src/domain/chat/title.ts:21): deleted outright. `titleFromMessages`/`titleFromSummary` take the fallback as a required `untitled: string` parameter now (same shape `createChatService` already takes `originLabel` in). All three sidepanel call sites — `App.svelte`, `OverflowMenu.svelte`, `HistoryListItem.svelte` — pass `m.chatTitle_untitled()`. `title.test.ts`'s ~10 assertions updated to pass a plain `"New chat"` literal (the suite tests DERIVATION logic, not the fallback's wording). No options-page call site existed.
  New/updated tests: `src/ui/providerMessage.test.ts`, `src/ui/capabilityMessage.test.ts` (new, mirror `storageMessage.test.ts`'s style, assert via `m.key()`), `src/domain/providers/capability.test.ts` (the two "falls back to a default sentence" tests rewritten to assert `undefined`, matching the domain function's new no-fallback behaviour).
- **claude-sonnet** (2026-08-24T03:08:00.000Z): TEST MIGRATION. All four options component test files (McpServerForm.test.ts, McpServersSection.test.ts, ProviderForm.test.ts, SettingsSection.test.ts) now assert via `m.key()` instead of English literals — same two patterns card 101 established: exact `getByRole(..., {name: m.key()})`/`getByLabelText(m.key())` for whole-string matches, `new RegExp(m.key({...}))` for a message that's a substring of a larger text node (storage-failure banners, the `"{key}" is already set` reserved-header error). Two DELIBERATE exceptions, left as literal English regexes and commented as such: the `/is set automatically by the client and cannot be overridden/` and `/Authorization" is already set/` reserved-header messages in McpServerForm.test.ts and the `"Content-Type is set automatically..."` one in ProviderForm.test.ts — these come from `validateServerHeaders` (src/domain/tools/servers.ts) and `reservedHeaderReason` (src/domain/providers/provider.ts), domain functions that already return raw English strings directly to the UI and are OUT of this card's three-fix scope (not named in the mandate, same class of debt as `turn.ts`). Zero rewording anywhere — every extracted string is byte-identical to what was there before, confirmed by all 963 tests, the full `npm run verify` (9/9 + screenshots best-effort PASS, section headings unchanged), and `node verify/checks/optionsSmoke.mjs` (13/13 — every literal-text/placeholder locator it drives against the real built extension still matched).
- **claude-sonnet** (2026-08-24T03:10:00.000Z): _locales/en (src/manifest strings): reviewed `public/_locales/en/messages.json` (`extName`: "OpenChat (WebMCP)", `extDescription`: kept in step with package.json's description per its own comment) and `manifest.config.ts` — both are already the finalized en source from card 100, nothing to change. No `default_title` is set on the manifest's `action` block (the toolbar opens the side panel directly, decisions/01), so there is no action title to finalize — confirmed by reading manifest.config.ts in full rather than assuming. Gates: npm test 963/963, npm run check 1498 files 0 errors, npm run guard all six (guard:i18n `en:401`), npm run build green, npm run verify 9/9 + screenshots best-effort PASS, `node verify/checks/optionsSmoke.mjs` 13/13. NOT committed, per instruction. For card 103 (the copywriter, next): owns ALL en source now across both surfaces (200 sidepanel keys from card 101 + 201 options keys from this card = 401 total in `messages/en.json`); please read this card's domain-string journal entries before touching `ProviderPicker.svelte`/`ProvidersSection.svelte`/`selection.svelte.ts` copy, since `src/ui/providerMessage.ts`/`src/ui/capabilityMessage.ts` are now the ONE place each of those two vocabularies' UI copy lives (not the domain files of the same names, which stay English on purpose for turn.ts/capability.ts's own internal use).
