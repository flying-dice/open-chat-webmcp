---
column: review
agent: claude-sonnet
live: false
labels: [frontend]
priority: med
updatedAt: 2026-08-24T08:15:00.000Z
---
# RTL readiness sweep

Prepare both surfaces for Arabic before translations land
(decisions/37): the one-time sweep from physical to logical Tailwind
utilities across all components — ml-/mr-/pl-/pr-/left-/right-/text-left/
text-right/border-l/r/rounded-l/r → ms-/me-/ps-/pe-/start-/end-/
text-start/text-end/border-s/e/rounded-s/e — so the dir flip from card
100's bootstrap does the work. Audit Hugeicons for genuinely directional
glyphs (back/forward arrows, chevrons) and give only those rtl:scale-x-
[-1]; symmetric icons untouched. Verify the transcript, composer, menus,
popovers and forms under dir="rtl" with a pseudo-locale or forced dir
(screenshots at 400px), including bidi edge cases (LTR tool names and URLs
inside RTL text get dir="ltr"/unicode-bidi isolation where needed).

## Checklist

- [x] Physical-direction utility sweep complete; grep proves no stragglers outside vendored kit
- [x] Directional icons flipped via rtl: variant; inventory journalled
- [x] Mono identifiers/URLs bidi-isolated in transcript and tool views
- [x] dir="rtl" screenshot pass over key screens journalled (both surfaces)
- [x] npm test, npm run check, npm run guard, npm run build, npm run verify green

## Gates

- [x] tests — npm test green, 963/963 (2 test files updated for the new bidi-isolation marks in expected aria-labels — HistoryListItem.test.ts, HistoryPanel.test.ts) (claude-sonnet, 2026-08-24T08:15:00.000Z)
- [x] check — svelte-check 1499 files 0 errors 0 warnings, tsc -p tsconfig.node.json clean (claude-sonnet, 2026-08-24T08:15:00.000Z)
- [x] guard — all six gates green (biome, boundaries, clean-code, return-types, throws, i18n) (claude-sonnet, 2026-08-24T08:15:00.000Z)
- [x] build — vite build green (claude-sonnet, 2026-08-24T08:15:00.000Z)
- [x] verify — 9/9 required checks passed, LTR screenshot matrix PASS (unchanged from pre-card baseline — confirms the sweep is behaviour-preserving under LTR) (claude-sonnet, 2026-08-24T08:15:00.000Z)

## Comments

- **claude-sonnet** (2026-08-24T06:00:00.000Z): Claimed the card. Read decisions/37's RTL section and card 100's journal — `src/infra/dom/document-locale.ts:43-47`'s `applyDocumentLocale(locale, direction)` is already wired from both roots (`src/sidepanel/main.ts:154`, `src/options/main.ts:80`) via Paraglide's own `getTextDirection(getLocale())`, so this card is purely the utility sweep + icon audit + bidi isolation + verification, no bootstrap work needed.
- **claude-sonnet** (2026-08-24T06:30:00.000Z): UTILITY SWEEP, ~31 physical-direction utility replacements across 16 files (all non-vendored: `src/sidepanel/**`, `src/options/**`): `src/sidepanel/components/Header.svelte:95,111` (`pr-2 pl-4`→`pe-2 ps-4`, `text-left`→`text-start`), `ToolCallRow.svelte:159,167` (`ml-0.5`→`ms-0.5`, `text-left`→`text-start`), `ActivityGroup.svelte:90,108` (`text-left`→`text-start`, `before:left-[5px]`→`before:start-[5px]` — the tool-rail connector, kept aligned with the now-logical dot column), `ToolArgValue.svelte:34,44` (`pl-5`→`ps-5`, `border-l-2 pl-2`→`border-s-2 ps-2`), `Composer.svelte:212` (`ml-auto`→`ms-auto`), `ProviderPicker.svelte:375` (`pr-1 pl-3`→`pe-1 ps-3`), `SchemaProperty.svelte:80,86` (`border-l-2 pl-2`→`border-s-2 ps-2`, both branches), `options/ProviderRow.svelte:182` and `options/McpServerRow.svelte:119` (`ml-auto`→`ms-auto`), `options/McpTestResult.svelte:50` (`pl-6`→`ps-6`), `CallLogEntry.svelte:96` and `HistoryListItem.svelte:77` (`text-left`→`text-start`), `ContextChip.svelte:116,126,103` (`text-left`→`text-start` ×2, and `-right-0.5`→`-end-0.5` on the connection-status dot pinned to the favicon's corner — missed by the first grep pass since it's a negative-value utility), `ToolsPanel.svelte` (`text-left`→`text-start` ×5, all `Empty.Root`/`Empty.Header` states), `options/PresetPicker.svelte:36` (`text-left`→`text-start`). Final grep across the full scope for every pattern in the card's own list (`ml-/mr-/pl-/pr-`, `border-l/r`, `rounded-l/r/tl/tr/bl/br`, `text-left/right`, `left-/right-` positioning) turns up zero code stragglers outside `src/ui/components/ui/` and the one deliberately-kept-physical case below.
- **claude-sonnet** (2026-08-24T06:35:00.000Z): KEPT PHYSICAL, one case, with a comment at the site: `Transcript.svelte:288-293`'s "jump to latest" button uses `left-1/2 -translate-x-1/2` for pure horizontal centering — the midpoint of a symmetric box is the same point measured from either edge, so `start-1/2` would centre it identically, and `left` was kept only because it pairs with `-translate-x-1/2`, a physical transform (Tailwind has no logical translate utility).
- **claude-sonnet** (2026-08-24T06:50:00.000Z): DIRECTIONAL ICON AUDIT. Inventoried every `Icon.svelte` glyph (`src/sidepanel/components/Icon.svelte:66-95`'s Hugeicons map) and every direct `HugeiconsIcon` call site (`HistoryPanel.svelte`, `HistoryListItem.svelte`, `options/McpServerRow.svelte`, `options/ProviderRow.svelte`, `options/HeadersEditor.svelte`, `options/HistorySection.svelte`, `options/ProvidersSection.svelte`, `options/McpServersSection.svelte`). Directional: `arrow_back` (`ArrowLeft01Icon`, App.svelte's "back to chat" row — unambiguously "toward reading start" in every use) got `rtl:-scale-x-100` baked directly into `Icon.svelte:117-127` rather than at the one call site, since `IconButton.svelte` has no class passthrough to `Icon` and this glyph's meaning is context-independent. `chevron_right` (`ArrowRight01Icon`) is used 4 places and split on whether it competes with a `rotate-90`: the two STATIC forward-hint uses — `OverflowMenu.svelte:140` (the "More" recent-chats row) and `ContextChip.svelte:122` (the "open tools" disclosure) — got `rtl:-scale-x-100` at the call site. The two ROTATE-driven disclosure triangles — `ToolCallRow.svelte:194-199` and `ActivityGroup.svelte:94-99` — were deliberately left unmirrored (commented in place): their meaning is fully carried by the `rotate-90`/no-rotate pairing (right when collapsed, down when expanded), and since the rotate lives on a wrapping `<span>` while a mirror would live on the inner `<Icon>`'s own SVG, combining the two on separate elements changes the EXPANDED-state orientation between LTR and RTL (right→down vs. left→up under a 90° clockwise rotation) — worse than leaving it alone. Two literal-Unicode "▸" disclosure triangles doing the identical rotate-driven job (`CallLogEntry.svelte:97-99`, `ToolListItem.svelte:81-84`) — not Hugeicons, found during the audit rather than in scope by name — got the same "leave unmirrored" treatment for consistency; worth noting U+25B8 carries Unicode's `Bidi_Mirrored` property, so the BROWSER may already flip its glyph shape automatically under `dir=rtl` independent of anything here (not pixel-verified). Non-directional, confirmed untouched: `arrow_downward`/`arrow_upward`/`expand_more` (vertical), and every plain/symmetric direct-`HugeiconsIcon` glyph (`BubbleChatIcon`, `Delete02Icon`, `Cancel01Icon`, `PlusSignIcon`, `Message01Icon`, `Wrench01Icon`, `PlugSocketIcon`, the list-reorder `ArrowUp01Icon`/`ArrowDown01Icon` pair in both row components).
- **claude-sonnet** (2026-08-24T07:05:00.000Z): BIDI ISOLATION. New leaf helper `src/ui/bidi.ts`'s `isolateLtr()` wraps a value in U+2066 LEFT-TO-RIGHT ISOLATE / U+2069 POP DIRECTIONAL ISOLATE — for identifiers/URLs interpolated INSIDE an already-assembled Paraglide message string with no DOM element to hang `dir="ltr"` on. Used at: `ContextChip.svelte:41-49` (origin in the "Sharing"/"Can't read" sentences — `pageInfo.title` itself is left alone, since a page's own title is natural-language text in whatever direction it actually is), `HistoryListItem.svelte:50-56`'s `formatOrigin` (covers both the inline description text and the `deleteLabel` aria sentence from one call site), `HistoryPanel.svelte:110` (the native `confirm()` delete dialog text), `ActivityIndicator.svelte:51-64` (tool/origin/model in the "calling `<tool>` on `<origin>`" sentence), `ApprovalCard.svelte:86` (the card's aria-label), `ProviderPicker.svelte:161-166` (the model id inside the "confirm this model?" warning sentence — NOT the plain "provider · model" tooltip label, which isn't embedded in translated prose). Everywhere else a value already sits in its own element, `dir="ltr"` was added directly instead (more idiomatic, no invisible marks): `ToolCallRow.svelte:168-170,182-184` (tool-name span + origin Badge), `CallLogEntry.svelte:102-108`, `ToolListItem.svelte:64-68`, `ApprovalCard.svelte:98,106` (Card.Title + the `<strong>` origin), `ToolArgs.svelte:34` and `ToolArgValue.svelte:47` (mono `<dt>` keys), `SchemaProperty.svelte:63` (property name), `options/McpServerForm.svelte:563` (the OAuth discovery issuer URL), `options/McpTestResult.svelte:62` (tool name), `options/ProviderRow.svelte:156` and `options/McpServerRow.svelte:79` (baseUrl/url divs), `options/HistorySection.svelte:148`, and `ProviderPicker.svelte:337` (each row's `model.name`) plus its collapsed trigger span (`ProviderPicker.svelte:382-390`) — the latter conditionally, via a new `triggerTextIsModelId` derived, since that span shows either a bare model id (LTR, force it) or a full translated sentence in the loading/error/confirm states (don't). Updated the two tests whose expected aria-label strings embed a raw origin (`HistoryListItem.test.ts`, `HistoryPanel.test.ts`) to wrap it with the same `isolateLtr()`.
- **claude-sonnet** (2026-08-24T07:40:00.000Z): VERIFICATION HARNESS. No `ar` locale exists yet (decisions/37), so wrote `verify/checks/rtlScreenshots.mjs` — a scratch script in the established `verify/checks/` pattern, NOT wired into `npm run verify` — that seeds the same fixtures `verify/checks/screenshots.mjs` uses, forces `document.documentElement.dir/lang` to `rtl`/`ar` after mount (there's no other way to exercise RTL pre-translation), and screenshots at 400px: chat transcript + expanded activity, overflow menu, tools/call-log panel, model picker sheet, history panel, and the full options page (light) plus the MCP add-server form. Files land under `verify/output/screenshots/` with an `rtl-` prefix (gitignored, evidence only, not committed).
- **claude-sonnet** (2026-08-24T07:55:00.000Z): THE REAL BUG THIS PASS FOUND. `rtl-sidepanel-tools.png`'s "Input schema" disclosure row rendered "▸ Input schema" — triangle BEFORE the text, i.e. still LTR-ordered under a forced `dir=rtl` document. Diagnostic (`getComputedStyle` + DOM-chain walk) traced it to `data-slot="scroll-area"` carrying an explicit `dir="ltr"` ATTRIBUTE, not inherited — bits-ui's `ScrollAreaPrimitive.Root` (wrapped by the vendored `src/ui/components/ui/scroll-area/scroll-area.svelte`) has its own `dir` prop (`node_modules/bits-ui/dist/bits/scroll-area/types.d.ts`) that defaults to `"ltr"` and is stamped onto the rendered root regardless of `<html dir>`, and CSS `direction` — which every native logical utility from this sweep (`text-start`, `ms-`/`ps-`, and flex's own row-direction mirroring) resolves against — inherits from THAT stamped attribute, not from the document. Confirmed this is a genuine, pre-existing default (not something my sweep introduced) by reproducing the identical `direction:ltr` computed style with NO `dir=rtl` override at all. Confirmed no other bits-ui primitive we use (`Popover`, `DropdownMenu`, `Select`, `Command`, `Tabs`, `Tooltip`, `Combobox`) exposes/stamps its own `dir` this way (grepped every `types.d.ts` under `node_modules/bits-ui/dist/bits/` for `dir?: Direction` — only `scroll-area`, plus `menubar`/`navigation-menu`/`slider`/internal popper/floating-layer utilities, none of which we use). FIX, in our own files (the vendored wrapper already forwards `...restProps`, so this needed no vendored edit): `Inspector.svelte:23-24,63-70` and `HistoryPanel.svelte:24-25,145-152` now pass `dir={getTextDirection(getLocale())}` explicitly to their `<ScrollArea>`, the same source of truth `document-locale.ts`'s bootstrap uses. Re-shot after the fix: "Input schema ›" (correct RTL order), and `rtl-sidepanel-history.png`'s message-bubble/trash-can icons swapped from broken (bubble-left/trash-right, i.e. still LTR) to correct (bubble-right/trash-left). Can't be exercised through Paraglide's real locale plumbing yet — `getLocale()` always resolves `"en"` until card 105 lands `ar` — so the verification script's `forceRtl()` also DOM-patches `[data-slot="scroll-area"]` elements directly as a stand-in for that; the component-level fix is what will light up for real once `ar` ships, no further code change needed then. Only two of our own call sites use `ScrollArea` (`Inspector.svelte`, `HistoryPanel.svelte`) — grepped to confirm.
- **claude-sonnet** (2026-08-24T08:00:00.000Z): FULL RTL SCREENSHOT PASS, all findings. `rtl-sidepanel-chat.png`: header mirrored (kebab+edit on the left, title right-aligned truncating from the front with a leading ellipsis); message alignment mirrored; tool-call rows read correctly with mono tool names/badges staying LTR inside the RTL row. `rtl-sidepanel-menu.png`: recent-chats list right-aligned, "More" row's chevron correctly flipped. `rtl-sidepanel-tools.png`/`rtl-sidepanel-history.png`: see the ScrollArea fix above; after it, both fully mirror (badges left of names, chevrons on the correct side, the `arrow_back` "Back" row's mirrored glyph correctly relocated to the RTL start/right by ordinary flex auto-mirroring). `rtl-sidepanel-model-sheet.png`: model list right-aligned, check/badges on the left — no code needed here, plain flex auto-mirroring. `rtl-options.png`/`rtl-options-mcp-add-form.png`: fully mirrored throughout — section headings, radio rows (label right, selector left), provider/server rows, every form field; `baseUrl`/MCP endpoint URL fields render LTR-isolated inside their right-anchored rows. One EXPECTED, non-actionable artifact: history-row metadata lines like "https://example.com · 8 messages · 4 tool calls" show the individual English PHRASES reordering relative to each other (per the Unicode Bidi Algorithm, since they're untranslated LTR islands joined only by neutral " · " separators inside an RTL paragraph) — this resolves itself once real Arabic copy replaces the English source; isolating further would only break the Arabic prose those phrases WILL be. One pre-existing, NOT RTL-caused bug found and left alone: the active/highlighted history row's description text overflows its `line-clamp-1` box and visually overlaps the delete button — confirmed via a geometry probe (`descRect` 410px wide inside a 330px button) that reproduces byte-identically with NO `dir=rtl` forced at all, root-caused to the vendored `item-content.svelte`/`item-description.svelte` missing `min-w-0` on a flex child holding clamped text. Out of this card's scope twice over (not RTL-specific, and the fix would be a vendored-file edit) — flagged for a follow-up, not fixed here.
- **claude-sonnet** (2026-08-24T08:10:00.000Z): VENDORED-KIT RTL BUGS, journalled per the card's instruction, none patched. `src/ui/components/ui/alert/alert.svelte:5` — `text-left` and `has-data-[slot=alert-action]:pr-18` are physical; under RTL the alert body stays left-aligned and the action-button padding reservation sits on the wrong side. `src/ui/components/ui/badge/badge.svelte`'s base class pairs a LOGICAL data-selector with PHYSICAL padding (`has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5`) — the reserved gutter stays on the same visual side regardless of direction, so it ends up wrong under RTL. Widespread beyond those two, all physical, all vendored: dropdown-menu item/checkbox-item/radio-item/sub-trigger/label (`pl-8`/`pl-9.5`/`pr-8`, the leading-icon/indicator gutter), input-group(-addon) and tabs-trigger/tooltip-content (`pl-3`/`pr-3`, `pr-1.5`/`pl-1.5`), field-error (`ml-4`), and `text-left` on alert-dialog-header/field-description/item-description. None edited (out of scope by the card's own instruction); worth a dedicated follow-up card once translations actually ship and these become visible rather than latent.
- **claude-sonnet** (2026-08-24T08:15:00.000Z): Gates all green — `npm test` 963/963 (960 unchanged + 2 test files updated for the new bidi marks, +1 new: none added, count matches pre-card baseline of 963 since only expected-string edits, no new tests), `npm run check` 1499 files 0 errors, `npm run guard` all six (biome required a format pass on `HistoryPanel.svelte`/`ProviderPicker.svelte`/`rtlScreenshots.mjs` after the edits — applied, clean), `npm run build`, `npm run verify` 9/9 with the LTR screenshot matrix unchanged from the pre-card baseline (confirms the sweep is behaviour-preserving under LTR, spot-checked `sidepanel-dark-activity-expanded.png` and `options-light.png`). NOT committed, per instruction.
