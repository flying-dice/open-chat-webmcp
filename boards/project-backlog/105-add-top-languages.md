---
column: review
agent: claude-opus
live: false
labels: [frontend, docs]
priority: med
updatedAt: 2026-08-22T18:35:00.000Z
---
# Add the top languages and close the i18n phase

Translate the humanized English source (card 103) into the nine locales
from decisions/37 — zh-CN, ja, de, fr, es, pt-BR, ko, ru, ar — for both
messages/{locale}.json and _locales/{locale}/messages.json (Chrome
underscore naming: pt_BR, zh_CN). Translations follow the card-103
glossary; technical terms (WebMCP, MCP, OAuth, API key) stay untranslated
where convention dictates; variants/plurals localized properly per
language, not copied. Pin pt-BR tag casing (known Paraglide fallback bug
history) and test its fallback. guard:i18n proves completeness both ways
for every locale. Spot-check screenshots for ar (RTL, from card 104's
groundwork) and one CJK locale (zh-CN) for layout overflow at 320px.
Mark translations as machine-produced pending native review in the docs.

## Checklist

- [x] Nine locales complete in messages/ and _locales/; guard:i18n green
- [x] Glossary consistency spot-audited across locales; plural variants localized
- [x] ar and zh-CN screenshot spot-checks at 320/400px journalled; overflow fixed
- [x] Language picker lists all locales with native names; switching verified
- [x] docs updated (i18n section: how to add a string, how to add a locale, native-review status); version bumped to 0.4.0
- [x] Full release gate: npm test, npm run check, npm run guard, npm run build, npm run verify green

## Gates

- [x] tests — npm test green, 974/974 in 65 files, unchanged from the pre-card baseline (no source behaviour changed that any test asserts) (claude-opus, 2026-08-22T18:25:00.000Z)
- [x] check — svelte-check 1509 files 0 errors 0 warnings, tsc -p tsconfig.node.json clean (claude-opus, 2026-08-22T18:25:00.000Z)
- [x] guard — all six green; guard:i18n now reports `base locale "en", 10 declared locale(s) [en:406 zh-CN:406 ja:406 de:406 fr:406 es:406 pt-BR:406 ko:406 ru:406 ar:406]` (claude-opus, 2026-08-22T18:25:00.000Z)
- [x] build — vite build green; dist/_locales carries all ten (ar de en es fr ja ko pt_BR ru zh_CN), manifest version 0.4.0 with all three __MSG_ placeholders (claude-opus, 2026-08-22T18:27:00.000Z)
- [x] verify — 9/9 required checks passed, 11-PNG screenshot matrix PASS; `node verify/checks/optionsSmoke.mjs` 13/13 (claude-opus, 2026-08-22T18:32:00.000Z)

## Comments

- **claude-opus** (2026-08-22T17:55:00.000Z): Claimed the card. Read card 103's glossary, cards 100/101/102's mechanics journals and card 104's RTL findings before touching anything. SPIKE FIRST, because getting it wrong would have been wrong nine times over: added a throwaway `ru` locale with a four-branch plural, compiled, and read `src/paraglide/messages/contextchip_toolcount2.js` — Paraglide emits ONE BRANCH PER `match` CATEGORY, selects between them with `registry.plural("ru", …)` (i.e. the TARGET locale's CLDR rules), and **returns the KEY NAME as the string** when a number selects a category the locale has no branch for. So the required category set is per locale, not English's two: `Intl.PluralRules(tag).resolvedOptions().pluralCategories` gives `other` for zh-CN/ja/ko, `one other` for de, `one many other` for fr/es/pt-BR, `one few many other` for ru, and all six for ar. Reverted the spike.

- **claude-opus** (2026-08-22T17:58:00.000Z): TRANSLATION METHOD. Wrote a shared brief (glossary table, register/typography rules per language, the four file mechanics, the per-locale plural category list) plus a standalone structural checker, then ran NINE parallel sub-agents, one per locale, each required to get the checker to `ok` before reporting. The checker verifies key set, kind, placeholder set, stray literal braces, HTML-tag multiset, byte-identical `declarations`/`selectors`, the locale's exact plural categories, and that `few`/`many`/`other` branches keep the count placeholder. Every one came back green; I then reviewed all nine by hand (see the audit entry below) rather than trusting the checker, which only proves structure.

- **claude-opus** (2026-08-22T18:00:00.000Z): **THE GUARD GOT THE TEETH THIS CARD NEEDED.** `scripts/guard-i18n.mjs:31-64,98-266` — the guard checked key sets and kinds; card 105 widened it to the rest of what a translation can silently break, all of it invisible at build time exactly like a missing key: a renamed or dropped `{placeholder}` (`{count}` is a NAME, not a position — `{anzahl}` renders literal braces), a literal `{` the message format reads as a placeholder (card 101's bug), a lost `<code>`/`<a>` tag in the ten rich-copy keys, and plural `match` categories that are English's rather than the locale's own — checked against `Intl.PluralRules(locale)`, the same CLDR data the compiled `registry.plural(locale, …)` selects with. The base locale is no longer skipped by the loop either (`guard-i18n.mjs:284-289`): diffed against itself its key/placeholder sets are trivially equal, but the literal-brace and plural-category checks are real for `en` too. PLANTED-VIOLATION PROOF, all five new classes at once against a temporarily-declared `pt-BR`: renamed `{title}`→`{titulo}`, a literal `{ }`, a stripped `<code>` pair, English's two categories on a locale that needs three, and a `countPlural=other` branch with the number written out as a word — 6 violations, exit 1, each named with its file, key and branch. Reverted; guard green.

- **claude-opus** (2026-08-22T18:05:00.000Z): **PER-LOCALE NOTES** (each translator's own glossary is in its sub-agent report; these are the choices worth a reviewer's attention). **zh-CN** — 对话 for chat (56 uses, zero drift to 聊天/会话), 提供方 for provider; re-split `providerForm_getApiKeyPrefix/Suffix` because Chinese cannot end a clause before the link, so the suffix carries the verb. **ja** — プロバイダー with the long vowel, 既定 for default; `storageMessage_sentence` changed from `{what}: {reason}.{suffix}` to `{what}。{reason}。{suffix}` because a Japanese colon between two full clauses reads badly, and the five reason fragments are written to suit. **de** — Sie throughout, Tool kept as the English word (Werkzeug reads wrong in a dev UI); `approvalCard_runsOnPrefix` became "Ausführungsort: " rather than "Läuft auf " to dodge a broken dative on the spliced `thisPageLabel`. **fr** — **conversation**, not "chat" (which reads as "cat"), used for all 54 occurrences; U+202F narrow no-break space before `: ; ! ?` and inside « » (101 of them); `activityIndicator_waitingFor` restructured to "En attente : {model}…" because "de le modèle" is ungrammatical with the fallback value. **es** — impersonal register, no tú/usted anywhere; **encabezado** for an HTTP header kept deliberately distinct from **cabecera**, the panel's top bar. **pt-BR** — Brazilian vocabulary throughout (salvar/guia/excluir), gender agreement on the plural chips ("{count} negadas" agrees with *chamada*). **ko** — names in confirm strings use apposition rather than 을(를), which dodges the unknown-final-consonant problem AND keeps parentheses out of the `*FailedWhat` strings that the test suite feeds to `new RegExp()`. **ru** — провайдер over поставщик; `historyClearConfirmTitle`'s en `one` branch ("Delete your ONLY chat?") could not survive, since ru `one` also fires at 21/31, so it became «Удалить {count} чат?». **ar** — MSA, no directional control characters added by hand (the UI isolates identifiers itself, card 104); the badge plurals put the verb first ("رُفض {count} استدعاءً") because a bare participle after a numeral is not something a native speaker writes.

- **claude-opus** (2026-08-22T18:08:00.000Z): **PLURAL HANDLING, and the proof it is real.** Every one of the 14 variant keys carries its locale's full category set — 1 branch for zh-CN/ja/ko, 2 for de, 3 for fr/es/pt-BR, 4 for ru, 6 for ar — with `declarations`/`selectors` byte-identical to `en` because those name the inputs the call sites pass. Two rules the brief made explicit and the checker enforces: in ru and ar the `one`/`two`/`few` categories cover many different numbers (ru `one` = 1, 21, 31…), so those branches keep `{count}` instead of a literal digit; and in the CJK locales the single `other` branch has to read for every number, so each is phrased with a measure word (`{count} 个工具`, 「ツール呼び出し {count} 件」, "도구 {count}개"). Where en's `one` branch hardcoded "1" it was rewritten around the placeholder. VISIBLE PROOF, from a live probe of the options page history rows across five locales: `en` "8 messages · 4 tool calls", `ru` «8 сообщений · 4 вызова инструментов» (many, then few — two different forms in one line), `ar` "8 رسائل · 4 استدعاءات أدوات" and, on the 2-message row, "استدعاءا أداة" — the Arabic DUAL, a category English does not have and which would have rendered the raw key name had the locale copied en's two branches.

- **claude-opus** (2026-08-22T18:10:00.000Z): **pt-BR VERIFICATION** (decisions/37's flagged Paraglide tag-casing bug, opral/paraglide-js#549). Not removed a key to test the fallback — that would just fail the guard. Instead, four pieces of evidence. (1) CASING IS PINNED: `project.inlang/settings.json:4-15` declares `"pt-BR"` and the file is `messages/pt-BR.json`; the compiled `src/paraglide/runtime.js:23` exports `locales = ["en","zh-CN","ja","de","fr","es","pt-BR","ko","ru","ar"]` with the casing intact, and every compiled message carries `if (locale === "pt-BR") return pt_br2_…(inputs)` (read in `src/paraglide/messages/optionspagetitle2.js:32-64`, which returns "Opções do OpenChat (WebMCP)"). (2) `isLocale()` IS EXACT: `pt-br`, `PT-BR`, `pt_BR` and `pt` are all `false` — so the tag casing genuinely matters. (3) BUT RESOLUTION IS NOT: 2.24's `toLocale()` (`runtime.js:568-579`) lowercases both sides before matching and `resolveLocaleWithStrategies` runs EVERY strategy's result through it, so a stored `pt-br` or a `Pt-Br` still canonicalises to `pt-BR` — #549's failure mode is fixed in this version, and I confirmed it rather than assuming. (4) THE FALLBACK LADDER, probed against the real compiled runtime: `navigator.languages` of `["pt-BR"]`→`pt-BR`, `["pt-br","en-US"]`→`pt-BR`, `["ar-EG","fr-FR"]`→`ar`, `["ja-JP"]`→`ja`; and `["pt-PT","en-GB"]`→**`en`**, `["zh-TW"]`→`en`, `["pt"]`/`["zh"]`/`["sv-SE"]`→no match, which the strategy chain then resolves to `baseLocale`. So European Portuguese and Traditional Chinese fall back to English rather than to the Brazilian/Simplified copy, which is the right answer for copy written in one variety, and nothing throws. Live confirmation of the whole chain in a real Chrome is in the switching entry below.

- **claude-opus** (2026-08-22T18:14:00.000Z): **PICKER + SWITCHING, verified in a real Chrome.** `src/options/components/SettingsSection.svelte:139-176` — the picker is still populated from Paraglide's compiled `locales` tuple, but `localeLabel()` no longer derives its rows from `Intl.DisplayNames`. I measured that API against the ten tags and it gives the WRONG answer for a picker three times over: `zh-CN` comes back "中文（中国）" where the endonym a Simplified-Chinese reader looks for is "简体中文", and `fr`/`es`/`ru` come back lowercase ("français", "español", "русский") because CLDR stores them as written mid-sentence. So there is now an explicit `LOCALE_ENDONYMS` table, typed `Record<Locale, string>` against the generated union — an eleventh locale added to settings.json with no endonym here is a `npm run check` failure, which is the property that matters for the one control a lost user navigates by. SWITCHING PROOF, not asserted from the store but driven end to end: `verify/checks/localeScreenshots.mjs:176-190` writes Paraglide's own `PARAGLIDE_LOCALE` localStorage key with `addInitScript` — i.e. BEFORE the page's first script, exactly the state the picker leaves behind after its reload — then loads each surface and asserts what came out. For `ar`: `lang="ar" dir="rtl"`, and the Arabic strings for `composer_placeholder`, `transcript_disclaimer`, `optionsPageSubtitle` and `settingsLanguageDescription` all present in the rendered text (probes read from `messages/ar.json`, so they cannot drift from the file). For `zh-CN`, `ja` and `de`: `lang` matches, `dir="ltr"`, all four probes green. And in every one of those four locales the picker opens with exactly ten rows reading `English | 简体中文 | 日本語 | Deutsch | Français | Español | Português (Brasil) | 한국어 | Русский | العربية` — the same list whatever language you are stuck in, which is the whole point of an endonym.

- **claude-opus** (2026-08-22T18:18:00.000Z): **SCREENSHOTS: what I looked at, and the two real bugs it found.** `verify/checks/localeScreenshots.mjs` is the card-105 counterpart to card 104's `rtlScreenshots.mjs` — same scratch-script pattern, NOT wired into `npm run verify` — but where 104 had to FORCE `dir`/`lang` because no `ar` existed, this drives the real locale plumbing. Per locale it shoots the side panel at 320 AND 400, the tools view at both, the options page full-page and the language picker open, and reports every element whose `scrollWidth` exceeds its `clientWidth` (skipping the ones that are supposed to scroll, and ellipsised text where clipping is the design). Ran `ar`, `zh-CN`, `de` and `ja`, plus `en` as the control.
  - **Overflow: nothing to fix.** `zh-CN`/`de`/`ja` report exactly the same two elements as `en` — an `sr-only` paragraph (the 1px-clip screen-reader trick, invisible by construction) and a 2px icon wrapper — so neither is translation-caused. The options page reports the same six radio-button indicators in every locale including `en`. `ar` adds four transcript-bubble divs at +8px, which are the ENGLISH fixture message bodies and their `<code>` runs sitting inside an RTL paragraph, not our copy; read at 320px, nothing is clipped or cut off. I eyeballed `locale-ar-sidepanel-320/400`, `locale-zh-CN-sidepanel-320`, `locale-de-sidepanel-320`, `locale-ar-options`, `locale-de-options` and both language-picker shots. German holds at 320 — the longest badge, "Herkunft unbekannt", fits because the tool name truncates to "purge-…" as designed; CJK line-height is fine at both widths.
  - **BUG 1, found by reading the Arabic options page: one English word in the middle of an Arabic line.** `src/options/components/HistorySection.svelte:151` had `· updated` hardcoded in the markup, and it had survived cards 101, 102 and 103 as a bare literal because it is a single connector word nobody greps for. Fixed as a real message (`historySection_updatedAt`, "updated {when}", translated into all ten — 405 keys became 406) so the word order can move: ja renders 「2026/8/22 18:22:08 に更新」 with the date FIRST, ko 「… 업데이트됨」, ar "آخر تحديث …".
  - **BUG 2, same reading, one line lower: the timestamps were in the wrong language entirely.** Four call sites used a bare `new Date(ms).toLocaleString()`, which formats in the BROWSER's locale — so a user running Chrome in English and the panel in Japanese got "8/22/2026, 6:22:08 PM" inside a Japanese sentence. New leaf `src/ui/datetime.ts` formats through `getLocale()`, i.e. the same value the message functions resolve with, and the four sites now use it: `HistorySection.svelte:149`, `HistoryListItem.svelte:99`, `CallLogEntry.svelte:71`, `McpServerForm.svelte:205`. Verified across five locales in one probe run: `ja` 2026/8/22 18:22:08 · `ru` 22.08.2026, 18:22:08 · `de` 22.8.2026, 18:22:08 · `ar` ٢٢‏/٨‏/٢٠٢٦، ٦:٢٢:٠٨ م · `en` 8/22/2026, 6:22:08 PM.

- **claude-opus** (2026-08-22T18:22:00.000Z): **THE BIG ONE: every portalled surface was still left-to-right in Arabic.** The Arabic language-picker screenshot showed the ten rows LEFT-aligned with the check mark on the right — identical to the German shot, i.e. not mirrored at all, while the trigger below it mirrored correctly. A DOM-chain probe traced it to a `<div dir="ltr">` directly under `<body>` wrapping `select-content`: bits-ui's `floating-layer-content.svelte:23` declares `dir = "ltr"` as a DEFAULT and stamps it as an attribute, and CSS `direction` — which every logical utility from card 104's sweep resolves against — inherits from THAT, not from the document. This is the exact shape of the `ScrollArea` bug card 104 fixed; that card grepped for components exposing `dir` and recorded the floating layer as "not used", which was true of the utility by name and false of it in fact, since Select, DropdownMenu, Popover (and the Command list inside it) and Tooltip all build on it. It could not be SEEN until a real `ar` locale existed. Probing the side panel under `ar` confirmed the overflow menu and the model picker were `direction: ltr` too. FIX, at our own call sites rather than in the regenerable vendored kit: new `src/ui/direction.ts`'s `uiTextDirection()` (one place, with the whole explanation), passed at all eleven sites — `SettingsSection.svelte:317`, `ProviderRow.svelte:195`, `ProviderForm.svelte:360`, `McpServerForm.svelte:490,530` (Select), `ProviderPicker.svelte:403` (Popover), `IconButton.svelte:117` and `HistoryListItem.svelte:144` (Tooltip), `OverflowMenu.svelte:97` (DropdownMenu), and card 104's two `ScrollArea`s (`Inspector.svelte:73`, `HistoryPanel.svelte:152`) moved onto the same helper so there is one answer to "what direction is the UI". ONE ASYMMETRY worth knowing, found by re-probing after the first attempt: Select/Popover/Tooltip take `dir` on their **Content**, but the menu family declares it on the **Root** and passes it down itself — `dir` on `DropdownMenu.Content` type-checks and does nothing, which is why `OverflowMenu.svelte` sets it on `<DropdownMenu.Root>` with a comment saying so. Re-probed after the fix: select-content, dropdown-menu-content, popover-content and command-list all compute `rtl`, and the re-shot picker has its rows right-aligned with the check on the left.

- **claude-opus** (2026-08-22T18:26:00.000Z): **GLOSSARY AUDIT, all nine, mechanical then by hand.** Wrote a per-locale audit that counts every plausible rendering of each glossary concept and flags any row with two live candidates. Every flag was then read in context, and all of them were false positives or legitimately distinct terms: substring artifacts (zh 边栏 inside 侧边栏; es "dar" inside "guardar"), key NAMES rather than copy (de "Provider" and fr "permission" appear only in identifiers), the OAuth vocabulary that English also spells "register"/"sign in" (zh 注册/授权服务器, ar تسجيل الدخول, es "iniciar sesión", pt "registro dinâmico"), the verb form of a noun (pt "conversar" = to chat, next to the noun "chat"), Japanese's correct noun/verb split (権限 the noun, 許可する the verb), and Arabic's definite-article variants (أداة/الأداة). Net result: each locale uses ONE word per concept, and the four chat/provider/model/tool anchors are at 51-61, 31-48, 34-41 and 38-91 occurrences per file with zero drift to a synonym.

- **claude-opus** (2026-08-22T18:30:00.000Z): **_locales, manifest and version.** `public/_locales/{ar,de,es,fr,ja,ko,pt_BR,ru,zh_CN}/messages.json` — Chrome's underscore spelling for the two regional tags, three keys each, name kept as the untranslated proper noun and every `extDescription` measured under the Web Store's 132-character limit (longest: de and pt_BR at 130). Added a THIRD manifest string while I was there: `action.default_title` (`manifest.config.ts:91-97`) → `__MSG_extActionTitle__`, because Chrome's fallback for the toolbar tooltip is the extension NAME, which is a proper noun and therefore identical in all ten locales and tells a first-time user nothing. `package.json:4` and `package-lock.json:3,9` to 0.4.0; the built `dist/manifest.json` reports `version 0.4.0` with all three `__MSG_` placeholders and `dist/_locales/` carries all ten directories. DOCS: new `docs/06-i18n.md` (the ten locales with their endonyms and plural categories, how to add a string, how to add a locale, the plural rules and why they are per-locale, RTL including the portal trap, and the guard's failure table), a machine-translation-pending-native-review banner at the top of it, `docs/01-architecture.md:446-469` for the widened guard, and README's feature list + docs index + the `guard:i18n` row.

- **claude-opus** (2026-08-22T18:35:00.000Z): Gates all green — `npm test` 974/974 in 65 files (unchanged; no test asserts anything this card changed), `npm run check` 1509 files 0 errors 0 warnings, `npm run guard` all six with `guard:i18n` at ten locales × 406 keys, `npm run build`, `npm run verify` 9/9 with the 11-PNG matrix PASS, and `node verify/checks/optionsSmoke.mjs` 13/13 (it matches English literals and the default locale is still `en`, so the smoke is untouched by the translations). NOT committed, per instruction. **Left for a follow-up, deliberately:** (1) `{protocol}` in `testResultDisplay_mcpSuccess*` is built in code as the hardcoded English `protocol 2025-06-18` (`src/options/forms/testResultDisplay.ts:117`), so it renders untranslated inside nine otherwise-translated sentences — five of the nine translators independently flagged it, and fixing it is a new message key plus a code change, not a translation. (2) The two reserved-header errors card 103 found in `src/domain/providers/provider.ts` and `src/domain/tools/servers.ts` are still outside the translation source — card 107 owns that. (3) The vendored-kit physical-direction utilities card 104 inventoried are now visible for real under `ar` rather than latent; still not this card's to patch. (4) The nine locales have had no native review — the docs say so in a callout, the README says so in the feature bullet, and a wording report should be treated as a bug with a one-line fix.
