---
column: review
agent: claude
live: false
labels: [frontend, bug]
priority: med
updatedAt: 2026-08-22T21:10:00.000Z
---
# i18n follow-ups: the protocol string and vendored-kit RTL utilities

Two items journalled by card 105 and left for a clean card:

1. **`{protocol}` renders untranslated** — a param whose VALUE is built in
   code as hardcoded English prose, so nine locales show an English clause
   inside a translated sentence (five translators flagged it
   independently). Find the construction site, turn the value into a
   code/data the UI maps through messages (established pattern:
   src/ui/*Message.ts).
2. **Vendored-kit physical-direction utilities** — card 104 inventoried
   physical `ml-/pl-/left-` usages inside src/ui/components/ui/ (Alert,
   Badge, DropdownMenu, InputGroup, Tabs, Tooltip); under real Arabic they
   are now visible. The kit must stay regenerable: fix via call-site
   overrides where possible; where only a vendored edit works, carry a
   commented local patch per the spinner.svelte precedent and journal it
   for re-application on regeneration. Re-run card 104's
   rtlScreenshots.mjs plus card 105's localeScreenshots.mjs (ar) to prove.

## Checklist

- [x] {protocol} value localized through a message map; all ten locales render it natively
- [x] Kit RTL fixes applied (call-site first, journalled patches second); ar screenshots re-eyeballed
- [x] npm test (scoped), npm run check, npm run guard, npm run build green — `npm run verify` explicitly skipped per this batch's coordinator instruction

## Comments

- **claude** (2026-08-22T20:40:00.000Z): **{protocol} fix.** Traced `messages/en.json:412`'s `{protocol}` param to its construction site: `src/options/forms/testResultDisplay.ts:117` built `` `protocol ${outcome.connection.protocolVersion}` `` as hardcoded English prose, fed into `mcpTestResultMessage`'s two translated sentences (`testResultDisplay_mcpSuccessEmpty`/`testResultDisplay_mcpSuccessWithTools`). This file lives in `src/options/forms/`, not the excluded sidepanel set, so no coordinator hand-off was needed — fixed directly. Added `testResultDisplay_mcpProtocolLabel` (`"protocol {version}"`) to `messages/en.json:412` and all nine other locales, glossary-matched against each locale's existing `testResultDisplay_mcpProtocolMismatch` wording (de "Protokoll", es/pt-BR "protocolo", fr "protocole", ja "プロトコル", ko "프로토콜", ru "протокол", zh-CN "协议", ar "بروتوكول"). `testResultDisplay.ts:117-119` now calls `m.testResultDisplay_mcpProtocolLabel({ version: … })` instead of the template literal. `npm run guard:i18n` green at 407 keys × 10 locales.
- **claude** (2026-08-22T20:55:00.000Z): **Vendored-kit RTL — all six components patched, all vendored (none fixable at a call site).** Every bug is a shared BASE default (a `has-data-[...]` gated variant, a `data-inset:` selector, or an `align`-variant's own CSS) baked into the component's own class string — not a per-instance style choice a consumer's `class` prop could reasonably override, so each got a minimal local patch with a "Local edit (card 108)" comment (the spinner.svelte/card-91 convention, grepped as `command-dialog.svelte`/`dropdown-menu-radio-group.svelte` precedents since no `spinner.svelte` exists in this kit). **Alert** — `src/ui/components/ui/alert/alert.svelte:5` (`text-left`→`text-start`, `pr-18`→`pe-18`) and `alert-action.svelte:16` (`right-3`→`end-3`, paired with the reserved gutter). Only call site: `src/sidepanel/components/NoticeCard.svelte` (not excluded — comment at :17-22 updated to match). **Badge** — `badge.svelte:4-8`: the icon slot was already logically named (`data-icon=inline-end/start`) but paired with physical `pr-1.5`/`pl-1.5`; swapped to `pe-1.5`/`ps-1.5`. **DropdownMenu** — `dropdown-menu-item.svelte`, `-checkbox-item.svelte`, `-radio-item.svelte`, `-sub-trigger.svelte`, `-label.svelte`: `data-inset:pl-9.5`/`data-[inset]:pl-8`→`ps-9.5`/`ps-8` (all five), checkbox/radio items' `pr-8 pl-3`→`pe-8 ps-3` and their absolutely-positioned check mark `right-2`→`end-2`, sub-trigger's arrow `ml-auto`→`ms-auto` (the `ArrowRight01Icon` glyph itself still always points right regardless of direction — flagged in the patch comment as a separate, unpatched concern; no existing RTL icon-flip precedent anywhere in the kit to follow). **InputGroup** — `input-group-addon.svelte:7-8`: same already-logical-name/physical-value bug as Badge (`"inline-start": pl-3 …` → `ps-3`, `"inline-end": pr-3 …` → `pe-3`, plus their `-ml-1`/`-mr-1`/`ml-[…]`/`mr-[…]` companions); `input-group.svelte:18`'s matching `[&>input]:pr-1.5`/`pl-1.5` → `pe-1.5`/`ps-1.5`. **Tabs** — `tabs-trigger.svelte:16`: `pr-1.5`/`pl-1.5` icon gutter → `pe-1.5`/`ps-1.5`; the vertical-orientation `after:-right-1` active-bar offset (line 19) deliberately left alone — this app only renders Tabs horizontally (`Inspector.svelte`) and which edge a vertical strip's indicator hugs isn't obviously a text-direction question, noted as a future call for whoever adds a real vertical-tabs consumer. **Tooltip** — `tooltip-content.svelte:30`: `has-data-[slot=kbd]:pr-1.5` → `pe-1.5`; the `slide-in-from-{left,right}`/`data-[side=…]` pairs are NOT bugs (floating-ui's physical placement side, independent of document direction) and were left alone, noted in the patch comment so a future pass doesn't "fix" them by mistake.
- **claude** (2026-08-22T21:05:00.000Z): **Screenshot evidence.** Re-ran both mandated scripts standalone. `node verify/checks/rtlScreenshots.mjs` (forced `document.dir`) — 7 PNGs written; read all of them. `rtl-sidepanel-chat.png`/`rtl-sidepanel-tools.png` show Badges correctly RTL-flowed; `rtl-options-mcp-add-form.png` shows the options page fully RTL-mirrored. Caught a harness gap while reading `rtl-sidepanel-menu.png`: `OverflowMenu.svelte`'s `DropdownMenu.Root` sets `dir={uiTextDirection()}` explicitly (the established per-component pattern, same shape as card 104's `ScrollArea` finding) rather than inheriting the document's forced `dir`, and `uiTextDirection()` still resolves the real locale ("en") under `rtlScreenshots.mjs`'s DOM-only forcing — so that one screenshot showed pre-fix (LTR-ordered) menu icons despite the source fix being correct. `node verify/checks/localeScreenshots.mjs ar` — real locale, real fix path — confirmed clean: `document: lang="ar" dir="rtl"` OK, both message probes OK, ten-locale language-picker complete. Read `locale-ar-sidepanel-400.png`/`locale-ar-tools-400.png` (Badges correctly right-flowed) and `locale-ar-options.png` (whole options page mirrored correctly, including the language Select's chevron on the visual left/logical-end). To directly settle the `DropdownMenu`/`InputGroup` patches under the REAL locale (since neither localeScreenshots.mjs nor rtlScreenshots.mjs opens the overflow menu or an auth-mode field), wrote two throwaway ad hoc scripts reusing `verify/lib/browser.mjs` + the storage fixtures (not committed, deleted after use): one opened the side panel's kebab menu under real `ar` — confirmed every `DropdownMenu.Item` icon now renders on the visual RIGHT (the RTL start side), fixing exactly what the forced-dir screenshot had missed; one opened the MCP-server form, selected "bearer" auth, and screenshot the `InputGroup` — confirmed the show/hide addon (`align="inline-end"`) renders on the visual left (RTL end side) as intended. Alert/Tabs/Tooltip's icon-gutter fixes are the identical already-logical-selector/physical-value pattern verified correct on Badge/DropdownMenu/InputGroup, but the current fixtures don't render an icon inside a Badge/Tab/Tooltip or a dismissible `NoticeCard`, so those three weren't independently re-screenshotted — noted here rather than silently assumed.
- **claude** (2026-08-22T21:08:00.000Z): **Gates.** `npm run compile:i18n` (regenerates gitignored `src/paraglide/`) then `npm run guard:i18n` — green, base locale "en", 10 locales × 407 keys, no missing/orphan/placeholder/markup/plural violations. `npx biome check` on every touched file — clean, no fixes needed. `npm run check` — my touched files are clean; 23 pre-existing errors remain in `src/domain/chat/service.ts`/`service.test.ts`, `src/infra/testing/http-test-server.ts`, `src/sidepanel/services/chatTurn.ts` (a `sharingAllowed`/`PageContextSnapshot` feature and a missing `@types/node`), none of which I touched — concurrent work from other agents in this session (card 119 "page-context-ui" and the mcp-tests groups own `src/infra`). `npm run guard` (all six: biome, boundaries, clean-code, return-types, throws, i18n) — green; `guard:clean-code`'s "nothing above 0.5" includes every file I touched. `npm run build` — green. `npx vitest run --project component` — 20 files, 198/198 passed (my actual scope, per the coordinator's test-scoping instruction). Full `npm test` for completeness: 1038/1048 passed, 10 failures all in `src/domain/chat/service.test.ts`/`turn.test.ts` — same concurrent `sharingAllowed` work (I touched none of `domain/chat/{session,turn,service,message}.ts`), not introduced by this card. `npm run verify` intentionally not run, per this batch's instruction. Not committed, per instruction.
