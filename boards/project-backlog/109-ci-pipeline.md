---
column: review
agent: claude
live: false
labels: [infra]
priority: high
updatedAt: 2026-08-22T18:51:18.000Z
---
# CI pipeline running the full gate suite

Implement decisions/39-ci-pipeline.md: a GitHub Actions workflow with a
fast always-on gate job (npm ci → check → test → guard → build) and a
verify job running the Chrome-for-Testing harness under xvfb with the CfT
cache keyed for reuse and screenshots uploaded as artifacts. Pin Node via
.nvmrc; enable npm caching. CI can't be fully exercised locally — validate
the workflow YAML (actionlint if available, or careful schema review),
dry-run every command locally exactly as the workflow spells it (fresh
npm ci in a temp clone to prove postinstall/i18n codegen works from
scratch), and journal what could only be proven on a real runner.

## Checklist

- [x] .github/workflows/ci.yml: gate job + verify job per decision 39; concurrency-cancel on superseded pushes
- [x] .nvmrc added and referenced; caches for npm and .chrome-for-testing keyed sensibly
- [x] Fresh-clone dry run proves npm ci → gate sequence green from scratch (journal the run)
- [x] Screenshot artifact upload wired; verify job's flake posture documented per decision 39
- [x] docs updated (README badge optional; docs mention CI as the enforcement home)
- [x] npm test, npm run check, npm run guard, npm run build green locally (fresh-clone dry run; see journal for the one unrelated in-tree exception) — `npm run verify` intentionally NOT run here, reserved for the coordinator's post-batch run per instructions

## Comments

- **claude** (2026-08-22T18:51:18.000Z): Claimed the card. Read decisions/39-ci-pipeline.md, verify/lib/chromeForTesting.mjs and browser.mjs (headless:false + `--enable-features=WebMCP`, no pinned CfT build id anywhere — resolveChromeForTesting() always resolves current "stable" at call time), scripts/compile-i18n.mjs and card 100's journal (the i18n message-format plugin fetches from jsDelivr into gitignored `project.inlang/cache/` on the FIRST postinstall of a fresh clone only — needs network, flakes there have a documented fix already: the local-module plugin path noted in card 100's journal). Confirmed no `.github/` existed yet, node is v24.15.0, and `npm ci`'s postinstall genuinely fetches on a truly fresh clone (verified `project.inlang/cache/plugins` did not exist pre-clone and was created during the dry run below).
- **claude** (2026-08-22T18:51:18.000Z): Delivered `.github/workflows/ci.yml` — two independent jobs (no `needs:` between them; each builds its own output: `dist/` vs `dist-verify/`), triggered on `push: branches: [main]` and `pull_request:`, with `concurrency: group: ci-${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true`. **gate** job: checkout → `actions/setup-node@v4` with `node-version-file: .nvmrc` + `cache: npm` → `npm ci` → `check` → `test` → `guard` → `build`, `timeout-minutes: 10`. **verify** job: same checkout/setup-node/npm ci, then `apt-get install -y xvfb` (ubuntu-latest ships Chrome's shared-lib deps already, just not an X server), then `xvfb-run --auto-servernum -- npm run verify`, then `actions/upload-artifact@v4` of `verify/output/screenshots` with `if: always()` so a failing run is just as reviewable as a passing one, `timeout-minutes: 15`. Full design rationale is written into the workflow file's own header + inline comments (the card's instruction to put docs there rather than touch docs/07, which is card 110's).
- **claude** (2026-08-22T18:51:18.000Z): Cache key design — the interesting part, since decisions/39 assumes "keyed for reuse" but there's no pinned CfT build id anywhere to key on (verify/lib/chromeForTesting.mjs:47-60 always resolves whatever "stable" currently is). Wrote `.github/scripts/resolve-cft-build-id.mjs` — the exact same `detectBrowserPlatform` + `resolveBuildId(Browser.CHROME, platform, ChromeReleaseChannel.STABLE)` call `resolveChromeForTesting()` makes, but WITHOUT the install half — so the verify job can learn the current stable build id as a cheap, install-free network call *before* `actions/cache`'s restore step runs, and key on `cft-<platform>-<buildId>` precisely. A cache hit means `npm run verify`'s own `resolveChromeForTesting()` finds the executable already at the expected path (`existsSync` check) and skips the download entirely; a miss (new stable build shipped) downloads once and the next run's key hits. No `restore-keys` fallback — a stale build id's cached binary would never satisfy that `existsSync` check, so restoring one would only cost bandwidth for nothing. Verified the resolver script itself works: `node .github/scripts/resolve-cft-build-id.mjs` → `build_id=152.0.7977.54` / `platform=mac_arm` against this machine's live network. Put this script under `.github/scripts/`, not `scripts/` — that directory is card 110's per the parallel-agent split, and this is CI-only plumbing never invoked outside the workflow.
- **claude** (2026-08-22T18:51:18.000Z): Flake posture (decision 39's "if CfT-under-xvfb proves flaky, mark non-required"): left the `verify` job WITHOUT `continue-on-error`. No real GitHub Actions runner has ever exercised this yet, so there is no observed flake to document — weakening the job pre-emptively would be guessing, not documentation. Wrote this reasoning directly into the workflow's header comment along with the exact trigger ("if a real run shows xvfb-under-CI flakiness local CfT runs don't reproduce, that's the moment to add `continue-on-error: true` with a comment recording what was observed"). The gate job stays the hard wall regardless of how that later call goes.
- **claude** (2026-08-22T18:51:18.000Z): actionlint installed via `brew install actionlint` (pulled in `shellcheck` as a dependency) and run clean: `actionlint .github/workflows/ci.yml` exits 0, no findings.
- **claude** (2026-08-22T18:51:18.000Z): Fresh-clone dry run — `git clone --no-hardlinks file:///Users/jonathanturnock/Projects/ollama-webmcp-chrome <tmp>/repo` (0.67s), then the exact gate sequence the workflow spells, timed individually on this machine (node v24.15.0, warm-ish local machine, no CI-specific cold-start overhead):
  - `npm ci` (postinstall compiles i18n, fetches the jsDelivr plugin fresh since `project.inlang/cache/` didn't exist pre-clone): **6.5s**, "added 308 packages... found 0 vulnerabilities".
  - `npm run check`: **4.2s**, 1509 files (temp-clone's larger 1509 vs. this tree's ~1103-1104 is just `svelte-check`'s file count including generated `src/paraglide/*`, not a discrepancy), 0 errors.
  - `npm test`: **7.8s** wall (37s CPU across workers), 65 files / 974 tests passed (main tree currently reports 980 — six more tests exist in the working tree than in the last commit; expected, other agents' in-progress work).
  - `npm run guard`: **3.0s**, all six sub-guards green (biome, boundaries, clean-code "ok — nothing above 0.5", return-types 172 functions, throws 7/7 allowlisted, i18n 10 locales complete).
  - `npm run build`: **3.2s**, vite build clean, `dist-verify`-style asset output.
  - **Total: ~25s wall** for `npm ci` through `build` on this machine with no cache. Set `timeout-minutes: 10` on the gate job — generous headroom for a cold GitHub-hosted runner (checkout + setup-node + a genuinely cold npm/postinstall network fetch) without masking a real hang. Temp clone deleted after the run.
- **claude** (2026-08-22T18:51:18.000Z): Local working-tree gates (not the isolated fresh clone — this repo, mid-session, with cards 110/111/112/113 all actively editing): `npm run check` green (1104 files, 0 errors), `npm test` green (980/980), `npm run build` green. `npm run guard` currently fails `guard:biome` on exactly two files — `src/options/components/ProviderForm.svelte` and `src/options/components/ProvidersSection.svelte` — both mid-edit by card 113 (its ownership per the parallel-agent split: "src/options components + related tests"), an unformatted import-statement split, not anything I touched. Confirmed by grepping the guard output for the offending file paths. This is NOT a regression from my changes (`.github/**`, `.nvmrc`, `README.md` only) and isn't mine to fix — the isolated fresh-clone dry run above is the authoritative proof that the gate sequence itself is sound end to end; this working-tree run is just a snapshot of a repo two other agents are actively mid-write on. `npm run verify` deliberately NOT run here per instructions (reserved for the coordinator's post-batch pass, not to be run concurrently with other agents).
- **claude** (2026-08-22T18:51:18.000Z): What only a real GitHub Actions runner can prove (nothing here was exercisable locally):
  1. **`xvfb-run -- npm run verify` actually works on ubuntu-latest** — this machine has a real display, so CfT-under-xvfb was never run here at all. Whether `apt-get install -y xvfb` alone is sufficient (i.e. ubuntu-latest's preinstalled Chrome already drags in every shared lib CfT needs — `libnss3`, `libatk-bridge2.0-0`, `libgtk-3-0`, `libgbm1`, `libasound2`, etc.) or whether an explicit dependency install step is also needed is unverified; this is exactly the class of thing decision 39's flake clause anticipates.
  2. **Whether the CfT download is genuinely fast on GitHub's network** — the local dry run's jsDelivr/npm fetches were fast on this network; GitHub-hosted runners' egress to `storage.googleapis.com` (where CfT builds live) and to jsDelivr is a different path with different characteristics.
  3. **`actions/cache` hit/restore actually round-trips correctly** for both the npm cache and the `.chrome-for-testing` cache — cache upload/download, key matching, and the two-run "populate then reuse" story can only be observed across two real CI runs on the same branch.
  4. **The `timeout-minutes` values are real bounds, not guesses that happen to be too tight or absurdly loose** — 10 (gate) and 15 (verify) are reasoned from the local dry run (gate) and from having no timing data at all (verify, explicitly flagged as a judgment call in the workflow's own comment).
  5. **`concurrency: cancel-in-progress` actually cancels a superseded run** — needs two rapid pushes to the same ref to observe.
  6. **actions/upload-artifact's `if-no-files-found: ignore` + `if: always()`** produces a genuinely useful, downloadable screenshot artifact on both pass and fail — never uploaded one for real.
- **claude** (2026-08-22T18:51:18.000Z): README updated at README.md:148-153 — one CI mention appended right after the existing "release gate" sentence, pointing at `.github/workflows/ci.yml`'s header comments and decisions/39, per instructions (docs/07-development.md is card 110's, worked concurrently — not touched). Not committed, per instructions. Moving to review.
