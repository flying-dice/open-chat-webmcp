---
column: review
agent: claude
live: false
labels: [backend, frontend]
priority: high
updatedAt: 2026-08-24T13:30:00.000Z
---
# Deep refactor pass: the debt the cheap tail left behind

Cards 81/90 cleared everything cheap; 77 accepted markers remain, and the
biggest are structural work nobody scheduled. Take them on now, per
.claude/skills/refactor/SKILL.md's one-fix-at-a-time discipline scaled up:

- **McpServerForm.svelte** (0.55 SRP, the largest UI file): split the OAuth
  sign-in state machine into its own component/module; the form keeps
  fields + validation. ProviderForm/McpServerForm's remaining mirror
  duplication (row shells, needs-reconnect rule) extracted where the DRY
  markers say so.
- **ProvidersSection.svelte** (0.4-0.5 SRP + DRY): the default-model
  capability/staleness subsystem and the shared reorder/permission-gate
  plumbing extracted (the DRY marker names McpServersSection as the twin);
  the loadModelsForProvider duplication with the selection store gets its
  shared helper (both marked 0.45).
- **buildData NAMING markers** and every remaining ≤0.5 marker whose fix
  is now reachable: fix or explicitly re-justify in place (a marker that
  survives this card must say why in its text).
- Behaviour-preserving throughout; the forms are sensitive — component
  tests + optionsSmoke are the safety net, run per extraction.

## Checklist

- [x] McpServerForm split; OAuth machine isolated and unit-tested
- [x] Section plumbing + model-loading helpers shared; twin files converge
- [x] Marker count journalled before/after (79 -> 38, zero new); every survivor's text re-justified
- [x] npm test, npm run check, npm run build green; optionsSmoke 13/13 after each form-touching extraction. `npm run guard` — five of six guards green; `guard:biome` fails only on scripts/dev-chrome.mjs, another card's new untracked file (see journal). `npm run verify` deliberately not run (card instruction: parallel agents).

## Comments

- **claude** (2026-08-24T12:05:00.000Z): Picked this up at **79** markers (the card says 77; the errors-as-values sweep landed two more since). Priority-1 work is in. **McpServerForm's 0.5 SRP marker is gone**: the OAuth sign-in state machine is now src/options/forms/oauthSignIn.svelte.ts:96-215 (`createOAuthSignIn` — held credential, in-flight flag, last error, manual-registration hand-off, and the `$state.snapshot` boundary that stops a reactive Proxy reaching chrome.storage), rendered by the new src/options/components/McpOAuthPanel.svelte:1-155 (the two panels, the status line, the copy-redirect-URI flash and the reveal-secret toggle — the only genuinely view-only state). The domain service stays the home of the FLOW (src/domain/tools/sign-in.ts): this was a UI-layer split, as the card asked. McpServerForm.svelte went 678 -> 422 lines and now does fields + validation + the connection test only. 18 unit tests for the machine in src/options/forms/oauthSignIn.svelte.test.ts:1-283 — no DOM, driven over the existing fake OptionsServices.
- **claude** (2026-08-24T12:20:00.000Z): The mirror duplication the DRY markers named. The "needs reconnect" rule (`expiresAt <= Date.now() && !refreshToken`) is one function in the domain now — src/domain/tools/servers.ts:66-90 `oauthNeedsReconnect`, with an injected `now` and 6 tests at src/domain/tools/servers.test.ts:99-146 — called by both McpServerRow.svelte:87 and the OAuth machine, so badge and status line cannot disagree (2 markers). The row shell is src/options/components/RegistryRow.svelte:1-125: wrapper, reorder pair, masked-header-count line and permission badges, with `badges`/`actions`/`children` snippets keeping every registry-specific decision at the caller (decisions/20's caution) — ProviderRow.svelte:130-198 and McpServerRow.svelte:52-105 both render through it (2 markers). Also renamed the two `buildData`s to `buildProviderConfig`/`buildServerConfig` and `TestOutcome`/`testConnection.ts` to `ProviderTestOutcome`/src/options/forms/providerTestConnection.ts (3 markers).
- **claude** (2026-08-24T12:40:00.000Z): Priority-2 work is in. The section plumbing the two registries typed out twice is src/options/forms/registrySection.svelte.ts:1-170 — `createSectionFailure` (card 95's write-failure line), `createRegistryTestGate` (the per-id grant map + the "cached grant -> `permissions.request` as the FIRST await -> run the test" flow, taking the attempt as a THUNK so a caller cannot start it early) and `permissionDeniedOutcome` — plus the pure swap at src/options/forms/reorder.ts:26-40. Nothing in either knows which registry it serves. ProvidersSection.svelte:60-86 and McpServersSection.svelte:36-52 both sit on them (8 markers). The default-model subsystem left ProvidersSection entirely: src/options/forms/defaultModel.svelte.ts:86-147 (`createDefaultModelOptions` — per-provider options, per-provider tokens, blocked reason) and :156-199 (`createStaleDefaultCheck` — card 41's stored-default check), taking that file 556 -> 322 lines and its 0.4 SRP marker with them.
- **claude** (2026-08-24T12:45:00.000Z): The loadModelsForProvider duplication (0.45 on both sides) got its helper in the DOMAIN, since the shared part is pure: src/domain/providers/capability.ts:87-140 `loadProviderModels` — list, branch on the error kind, resolve every capability, and abandon a superseded load at BOTH await boundaries via an injected `stillCurrent` (so the token discipline is preserved exactly, including not issuing capability lookups after supersession). It returns the RAW `ProviderError`, so each surface localizes with its own `describeProviderError` and keeps its own state shape: src/sidepanel/stores/selection.svelte.ts:379-414 and src/options/forms/defaultModel.svelte.ts:92-113.
- **claude** (2026-08-24T13:05:00.000Z): Worked the reachable tail, one marker at a time. DEAD: dropped the unreachable `"delete"` Icon entry (src/ui/icons.ts, src/sidepanel/components/Icon.svelte), the unimported `Msg` alias (src/infra/chrome-runtime/protocol.ts + its barrel line), and gave src/sidepanel/services/mcpTools.ts:69's subject-less doc comment a subject. DRY in the chat context: `truncateWithEllipsis` + `collapseWhitespace` are src/domain/chat/text.ts:19-35 (9 tests at src/domain/chat/text.test.ts) and `newId` is src/domain/chat/id.ts:24 — that is the three-way truncate split, the two-way id generator and title.ts's second whitespace-collapse, 6 markers, and session.ts/title.ts/turn.ts/service.ts all read shorter for it. DRY in infra: `authErrorFor` (src/infra/mcp/json-rpc.ts:77-88) collapses the four hand-written 401/403 blocks in streamable-http.ts and legacy-sse.ts; `budgetError` (src/infra/mcp/legacy-sse.ts:186-199) collapses that file's three timeout-vs-other ternaries; `readDecoded` (src/infra/chrome-storage/area.ts:167-187) collapses the read-then-decode composite at all five sites card 96's audit counted; `generateRecordId` (src/infra/chrome-storage/keyed-record-store.ts) is now the one id generator both registries call.
- **claude** (2026-08-24T13:15:00.000Z): NAMING and KISS. `ProviderPicker.svelte` -> **ModelPicker.svelte** (+ its test) — card 51/decisions/22 flattened it into a MODEL picker years of comments ago; every reference in src/ updated (0.5). `getMergedToolsForTab` -> `mergeToolsForTab` (src/sidepanel/services/mcpTools.ts:191) — "get" promised a pure read while scheduling a network refresh. `webmcpPanelDebugSnapshot` -> `webmcpPanelDebugHandle` (src/sidepanel/stores/panel.svelte.ts:373) — a "snapshot" that carried two mutators. `syncToTab` takes an explicit `{ force: true }` (src/sidepanel/stores/selection.svelte.ts:292-300) instead of `refresh()` assigning `tabId = undefined` to invalidate an identity check. `HistorySection.svelte`'s `sessions` -> `summaries` (decisions/13's retired vocabulary). And src/domain/tools/merge.ts:226-247 dropped its `<TServer extends ToolServerIdentity>` parameter for the concrete `McpServerConfig`: the inward edge it guarded against disappeared with the DDD restructure, and it had exactly one instantiation.
- **claude** (2026-08-24T13:25:00.000Z): **79 -> 38 markers, zero new, nothing above 0.5.** The 38 survivors all now carry a `STAYS:` clause saying why, in their own marker text, on one line so `npm run guard:clean-code` prints the reason with the finding — that is the substantive half of this card's third checklist item, and the reasons are specific, not boilerplate: the two 0.5 reserved-header DRY markers stay because the two rules disagree on CONTENT (a provider reserves `authorization` whenever it holds a key; a server only when the transport resolves auth) and unifying them needs a third bounded context, i.e. a decision record; chat-store.ts's SRP stays because its debounce/index-lock/eviction are one transactional story; the two app-services DRY markers stay because there is no layer both surfaces may share for a service locator and the duplicated lines are the ones guard:throws allows by name; the two main.ts DRY markers stay because decisions/29 wants composition roots spelled out. GATES: `npm test` 1048 passed (69 files; was 974), `npm run check` clean, `npm run build` clean, `node verify/checks/optionsSmoke.mjs` **13/13** — run after each of the three form-touching extractions, not just at the end. `npm run guard` fails ONLY on `biome ci` finding scripts/dev-chrome.mjs unformatted — a new untracked file in card 110's territory that I have not touched (`npx biome format --write scripts/dev-chrome.mjs` fixes it); `biome ci src/ verify/` is clean and the other five guards pass. `npm run verify` not run, per this card's instruction about the parallel agents.
- **claude** (2026-08-24T13:30:00.000Z): Two notes for whoever picks this up. (1) COLLISION: my ProviderPicker -> ModelPicker rename was silently reverted in src/sidepanel/App.svelte:34 by a concurrent write (the page-context/ContextChip work) and I re-applied it — worth a check that it survived. (2) A literal `<style>` inside a `//` comment in a Svelte `<script>` block makes the component fail to compile with a misleading "has no default export"; hit it while writing one of the re-justifications, worth knowing.
