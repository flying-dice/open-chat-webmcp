---
column: review
agent: claude
live: false
labels: [frontend]
priority: med
updatedAt: 2026-08-22T22:15:00.000Z
---
# Accessibility pass with a permanent axe check

Labels stayed stable through every migration, but no one has audited the
surfaces as a keyboard/screen-reader user. Two halves:

- **Audit + fix**: keyboard-only walk of both surfaces (view switches
  return focus sensibly; the approval card's focus trap; the picker's
  roving focus vs the filter field; Escape paths; the menu → subview →
  Back loop), live-region behaviour for streaming replies and the
  activity indicator (announcements without spam), name/role/value on the
  custom rows (history, tool list, call log). Fix findings; journal each
  with before/after.
- **Enforcement**: axe-core wired into the verify harness as a best-effort
  check over the seeded screens (both surfaces, light theme is enough),
  failing loudly on serious/critical rules, with a documented allowlist
  for any rule deliberately waived (each waiver justified).

## Checklist

- [x] Keyboard/focus audit journalled with findings fixed (focus return on view switch, trap correctness, Escape paths)
- [x] Streaming/live-region behaviour judged and tuned (aria-live polite, no per-token spam)
- [x] axe check landed in the harness; serious/critical clean; waivers justified inline
- [x] npm test, npm run check, npm run guard, npm run build green; `node verify/checks/axe.mjs` green (full `npm run verify` not run — parallel agents were editing verify/checks this batch)

## Comments

- **claude** (2026-08-22T21:05:00.000Z): Picked this up. Audited by DRIVING THE REAL UI, not by reading markup: a standalone Playwright script on the harness's own machinery (verify/lib/browser.mjs + verify/lib/seed.mjs, so the audit ran against exactly the seeded world verify/checks/screenshots.mjs captures), pressing real keys through Playwright's keyboard API and reading `document.activeElement` after every gesture. Deleted afterwards — the durable artefact is verify/checks/axe.mjs.

- **claude** (2026-08-22T21:20:00.000Z): FOCUS FINDING 1, the worst one — every view switch dropped focus to `<body>`. Measured on all three paths: menu → "Tools & call log" → Back; menu → "More" → Back; and the context strip's own chevron → Back. In each, the control that triggered the switch is part of what gets unmounted, so Chrome had nothing to move focus to and a keyboard user had to Tab in from the top of the document while a screen reader said nothing at all. BEFORE: three inline `view = "chat"` assignments. AFTER: one `switchView` in src/sidepanel/App.svelte:74-110 that lands focus deliberately — into a subview it focuses the "Back to chat" row (that view's first control, whose label states both where you are and how to leave), and back to chat it focuses the composer, falling back to the shell (`tabindex="-1"`, src/sidepanel/App.svelte:426-435) when the composer is in its blocked state and has no textarea. `Composer.focusInput` now reports whether it landed (src/sidepanel/components/Composer.svelte:137-149) so that fallback can never be silent, and IconButton gained a `bind:ref` for the Back button (src/sidepanel/components/IconButton.svelte:56-64). Re-measured: entering → "Back to chat", leaving → the "Message" textarea, on all three paths.

- **claude** (2026-08-22T21:28:00.000Z): FOCUS FINDING 2 — the sharing gate's ✕ threw focus away. Pressing it unmounts the ✕ and mounts "Share this page" in its place (and the reverse on re-enable), so the pressed button ceased to exist: measured focus on `<body>` both ways. That is also a state change with no announcement at all. FIX in src/sidepanel/components/ContextChip.svelte:143-165, :249, :264 — `toggleSharing` hands focus to the button that replaced the one just pressed, so the new button's own label ("Share this page" / "Stop sharing this page") is the announcement. No live region added on purpose: it would say the same sentence a second time. Re-measured: dismiss → "Share this page", re-enable → "Stop sharing this page".

- **claude** (2026-08-22T21:34:00.000Z): FOCUS FINDING 3 — copying a reply with the keyboard lost focus for 1.5s. MessageActions.svelte swapped the copy button out for a `role="status"` badge while the "Copied" confirmation showed, which unmounted the focused element: Tab restarted from the top of the panel and Enter did nothing until the badge swapped back. BEFORE: `{#if copied}<span role="status">…{:else}<IconButton …>`. AFTER (src/sidepanel/components/MessageActions.svelte:68-75): one button throughout, changing its own glyph and accessible name — which is also the better announcement, since a name change on the FOCUSED element is spoken, where a status badge appearing elsewhere in the transcript competed with whatever was being read. Re-measured: after Enter the focused button is named "Copied"; 1.9s later, the same focused button is named "Copy response".

- **claude** (2026-08-22T21:40:00.000Z): VERIFIED-AS-CORRECT, no change needed (recording these so the next pass doesn't re-derive them). (a) Menu → subview leaves focus on the overflow trigger before `switchView` moves it on — bits-ui returns it there correctly. (b) Escape from the overflow menu returns focus to the trigger; Escape from the model picker returns it to the composer's model chip. (c) ModelPicker's `onOpenAutoFocus` lands on the filter box when there is one (>8 models) and on the Command root otherwise, so arrow-key roving works with no click first — measured `div[role=application]` focused on open with the seeded fixture, which has no filter. (d) ApprovalCard focuses Deny on mount and is not re-focused as the transcript re-renders: its `$effect` depends only on the button ref and the card is keyed by `request.id`, so a streaming turn cannot steal focus back to it repeatedly. Pinned already by src/sidepanel/components/ApprovalCard.test.ts:84.

- **claude** (2026-08-22T21:52:00.000Z): LIVE REGIONS — the panel had ONE, and it was the wrong shape. `ActivityIndicator` carried `aria-live="polite"` on its sentence span, which (i) is INSERTED already holding its text, so the first and most important sentence is the one most screen readers skip, and (ii) unmounts with the turn, so nothing announced the turn ENDING — a screen-reader user got silence for the whole reply and no cue to go and read it. Streaming itself had no live region at all, and correctly so: a per-token region is exactly the spam this card was written to remove. AFTER: the sentence composition moved to src/sidepanel/presentation/turnStatus.ts (new), read by both the visible indicator and ONE persistent polite region in src/sidepanel/App.svelte:456 (`role="status"`, `aria-live="polite"`, `aria-atomic`, sr-only, mounted for the panel's whole life). It speaks the waiting/calling sentence — which changes at most once per tool call, so the throttling is structural rather than a timer — plus one line when the turn ends (`app_turnFinishedAnnouncement`). It stays SILENT for `streaming` (the arriving text is the feedback) and for `awaiting-approval` (the approval card moves focus into itself, which announces strictly more). ActivityIndicator.svelte:92-94 lost its own `aria-live`; Transcript.svelte:110 now reads the same `isSpokenPhase` rule instead of re-deriving it. Pinned by src/sidepanel/presentation/turnStatus.test.ts (7 cases).

- **claude** (2026-08-22T21:58:00.000Z): LIVE REGIONS, second finding — every NoticeCard was an ASSERTIVE region. shadcn's `Alert.Root` sets `role="alert"` for every variant, so the restricted-page notice (standing state, rendered at mount) barged in over the panel's own opening announcement every time the user opened onto a chrome:// tab, and the cross-origin notice did the same. That inverted this component's own documented rule about which notices are announcements and which are live state. FIX in src/sidepanel/components/NoticeCard.svelte:62-74 — `role="status"` for the calm variant, `role="alert"` kept for `"failure"` (something the user just did did not happen; interrupting is the point).

- **claude** (2026-08-22T22:04:00.000Z): NAME/ROLE/VALUE on the custom rows. (a) Tool list: every card rendered a disclosure whose accessible name was the identical words "Input schema", so a screen reader's button list read back N indistinguishable entries — fixed with a tool-naming aria-label (src/sidepanel/components/ToolListItem.svelte:80-88), bidi-isolated per card 104's convention. Re-measured: "Input schema for read-page-state", "Input schema for submit-form". (b) Call log: the row trigger named only the tool and its origin, while the approved/denied badge and the duration sit in a SIBLING span outside it — `aria-describedby` now pulls that existing wording in (src/sidepanel/components/CallLogEntry.svelte:112-121, :139). (c) Transcript timeline rows: status was carried ONLY by an `aria-hidden` coloured dot, so a screen reader could not tell a successful call from a failed one — `aria-describedby` now points at the meta badges and the error line (src/sidepanel/components/ToolCallRow.svelte:158-179, :207, :251, :274). Both (b) and (c) borrow words already on screen rather than composing a second sentence out of new keys that could drift from them. (d) History rows and ContextMarkers audited and left alone — the row button's name already carries title/origin/time/counts with `aria-current`, and the markers list is a real `ul` with an `aria-label`.

- **claude** (2026-08-22T22:08:00.000Z): AXE, half two — verify/checks/axe.mjs (new), runnable standalone (`node verify/checks/axe.mjs`) and registered in verify/run.mjs:35, :66, :503-518 as a best-effort named check `axe` sharing the suite's browser. Four screens against the SAME seed the screenshot matrix uses: sidepanel chat / tools & call log / history, and the options page, light theme. serious+critical FAIL with the rule, screen and selectors; moderate/minor are printed but do not fail (a gate that fails on best-practice advice is a gate people switch off). INJECTION: the card suggested `page.addScriptTag` from node_modules — that does not work and the file says so with the measured error: both surfaces run under MV3's `script-src 'self'`, and addScriptTag is inline script by construction, so Chrome refuses it. `page.addInitScript({ path })` is the CSP-safe route (DevTools-protocol injection, not subject to the document's CSP) and — unlike Playwright's `bypassCSP` option — changes nothing about the policy the page under audit actually runs under. WAIVERS: none. `AXE_WAIVERS` ships empty with a comment explaining that it exists for a future justified exception, because every serious/critical finding this pass turned up had a real fix.

- **claude** (2026-08-22T22:11:00.000Z): AXE FINDINGS FIXED (this is where the worst defect of the whole card turned up). CRITICAL `button-name` ×6 — the approval-policy and MCP-approval-policy radios on the options page had NO accessible name at all. bits-ui renders a RadioGroup item as `<button role="radio">`, and the `<label for>` around it only names LABELABLE elements, never a button: the most consequential setting in this extension was six unnamed buttons to a screen reader. Fixed with `aria-labelledby`/`aria-describedby` onto the wording already rendered (src/options/components/SettingsSection.svelte:361-388, :425-452). SERIOUS `color-contrast` — three light-mode failures, all real: `--muted-foreground` #71717b measured 4.39:1 on the context strip's own `--secondary` fill and 4.35:1 on options-page card descriptions (it passes on white, which is why it survived every earlier pass); the tonal destructive "Risk"/"Permission needed" badge measured 3.98:1; and a disabled MCP server row's `opacity-60` dimmed its endpoint URL below the bar. Fixed as: two light-mode token nudges in src/app.css:39-58 (`--muted-foreground` 0.552→0.535, `--destructive` 0.577→0.52, each with its measured before/after in the comment); the destructive Badge tint made OPAQUE via `color-mix` against `--background` (src/ui/components/ui/badge/badge.svelte:8-19, :26) so its contrast is a property of the variant rather than of whatever surface it lands on — the same badge measured 4.75:1 on a card and 4.28:1 on a muted row; and `opacity-60` → `bg-muted` on the inactive registry row (src/options/components/RegistryRow.svelte:49-62, :89), which was saying "disabled" twice anyway, since McpServerRow already renders an explicit badge. MODERATE `landmark-one-main` + `region` ×16 — the side panel had no landmarks at all, so there was no way to skip the header; fixed with a real `<main>` around the view area (src/sidepanel/App.svelte:465). Final state: 0 blocking, 2 moderate `region` advisories left on bits-ui-generated `#bits-*` nodes in the two subviews, printed by the check.

- **claude** (2026-08-22T22:14:00.000Z): Keys added — exactly two, appended as a block to all ten locales with register-faithful translations: `app_turnFinishedAnnouncement` (the live region's end-of-turn line) and `toolListItem_inputSchemaAriaLabel` (`{tool}`). Everything else reuses wording already on screen, on purpose: an accessible name composed from a second set of keys is a name that drifts from the one a sighted user reads. `node scripts/guard-i18n.mjs` green at 444 keys × 10 locales. axe-core pinned as an exact devDependency (`axe-core: "4.13.0"`) — I own package.json this batch.

- **claude** (2026-08-22T22:16:00.000Z): GATES. `npm test` 1238 passed / 80 files (was 1210/77 — +7 turnStatus cases, and the rest are other agents' additions landing alongside). `npm run check` 0 errors over 1694 files. `npm run build` green. Guards: boundaries, clean-code (nothing above 0.5), return-types, throws, i18n all green; `biome lint` clean over src/ and my verify files, `biome format` applied to everything I touched. `node verify/checks/axe.mjs` PASS, 0 blocking. Full `npm run verify` deliberately NOT run — two other agents were editing verify/checks/ and package-adjacent files through this batch, and a shared-browser suite run mid-edit proves nothing about either of our changes. Coordinator note: my run.mjs registration touches three places (the `checkAxe` import at :35, `"axe"` in `CHECK_NAMES` at :66, and the `report.runBestEffort` block at :503-518) — if the scenario-checks agent conflicts there, keep both; the blocks are independent.
