---
column: review
agent: claude-opus
live: false
labels: [infra, frontend]
priority: high
updatedAt: 2026-08-23T02:20:00.000Z
---
# Storybook bootstrap

Stand up Storybook per decisions/42-storybook.md. VERIFY the current
Storybook + Svelte-5 setup against live docs first (version, framework
package, the official Svelte CSF addon and its runes/snippet story syntax)
— trial in a scratch project like card 65 did, then port; the CRXJS plugin
must never load in Storybook's builder (mirror vitest.config.ts's
reasoning). Wire: svelte plugin, $lib alias, app.css + dark class,
Paraglide messages (generated output import), the lang/dir bootstrap as a
decorator so the locale toolbar drives real RTL, and the three global
toolbar axes (theme, all-ten locale, 320/400 width presets for sidepanel
stories). Stories render through the existing per-surface fake-services
modules and typed fixtures — extend those helpers for story use rather
than inventing story-only mocks (journal any gap). Prove the pipeline with
stories for three representative components (one sidepanel with services,
one options form, shared Markdown). Add npm run storybook /
build-storybook; build-storybook joins CI's gate job; stories excluded
from guards like tests; a guard:stories script diffs non-vendored
components against colocated *.stories.svelte files (failing on gaps —
seeded with an allowlist of the not-yet-covered components that cards
124/125 will empty).

## Checklist

- [x] Storybook runs and builds with verified-current versions; CRXJS never loads; scratch-trial findings journalled
- [x] Theme/locale/width toolbar globals working incl. real RTL via the bootstrap decorator
- [x] Three proof stories rendering through the shared fakes
- [x] guard:stories wired into npm run guard with the temporary allowlist; build-storybook in CI (actionlint clean)
- [x] npm test, npm run check, npm run guard, npm run build, build-storybook green

## Comments

- **claude-opus** (2026-08-23T01:50:00.000Z): Picked this up. Verified versions against live docs and npm rather than training data, today: Storybook **10.5.10**, framework package **`@storybook/svelte-vite`** (10.5.10), official CSF addon **`@storybook/addon-svelte-csf` 5.1.2** — v5 of the addon is the Svelte-5 rewrite (`defineMeta()` returning `{ Story }`, `{#snippet template(args)}` instead of slot templates, `tags: ["autodocs"]` instead of the `autodocs` story property). All three pinned EXACTLY in package.json:41-43, matching decision 42's "pinned" and the posture `@biomejs/biome`/`axe-core` already have. Peer ranges check out against this repo: the addon accepts `@sveltejs/vite-plugin-svelte` ^7 and `vite` ^8; `npm i -D` reported 0 vulnerabilities and no unmet peers.
- **claude-opus** (2026-08-23T01:55:00.000Z): SCRATCH TRIAL first, per the card — `npx storybook@latest init --yes` in a throwaway svelte+vite@8 project at /tmp/sb-scratch, committed to git beforehand so the diff was readable. Findings, and every one of them is why we ported by hand: (1) it **rewrote `vite.config.js` in place**, adding a whole `test.projects` block with `storybookTest()`, `@vitest/browser-playwright` and a headless-chromium instance — pointed at OUR vite.config.ts that would have collided with vitest.config.ts and the MV3 build; (2) it added five addons nobody asked for (`@chromatic-com/storybook`, `addon-vitest`, `addon-a11y`, `addon-docs`) plus `vitest`/`playwright`/`@vitest/coverage-v8` as devDeps; (3) it downloaded Playwright browser binaries; (4) it scaffolded `src/stories/` with a parallel (NOT colocated) example tree. We took exactly two things from it: the confirmed `main.ts` shape and the real Svelte-5 story syntax from its generated `*.stories.svelte` files.
- **claude-opus** (2026-08-23T02:00:00.000Z): CRXJS. The first `storybook build` against the ported config died exactly where the card predicted — `[plugin crx:manifest-post] SyntaxError: Unexpected token '.', "./rolldown"... is not valid JSON`, CRXJS's `generateBundle` hook trying to `JSON.parse` a Storybook chunk as the extension manifest. Root cause: `@storybook/builder-vite` LOADS the project's root vite.config.ts and merges it, and there is no supported way to un-merge a plugin from a `viteFinal` hook (it runs after the merge). The fix is `.storybook/main.ts:26` — `framework.options.builder.viteConfigPath: ".storybook/vite.config.ts"` — pointing the builder at a dedicated config, .storybook/vite.config.ts:1-73, which carries the $lib alias, the shared `paraglideOptions`, `tailwindcss()`, `svelte()` and vite.config.ts's `define` constants, and no `crx()`. Second finding while wiring it: `@storybook/svelte-vite` does **not** add `svelte()` itself (its `viteFinal` appends only a docgen plugin — read it in node_modules to confirm, after the build failed on `@storybook/svelte/static/PreviewRender.svelte` with "JSX syntax is disabled"), so ours is the only one, which is also what keeps a single Svelte internals instance in play.
- **claude-opus** (2026-08-23T02:05:00.000Z): DECORATORS (.storybook/preview.ts:89-126). Read the Svelte renderer's `decorateStory` to get the contract right rather than guessing: return the value `storyFn(context)` gave you and it counts as a side effect; return anything else and the renderer wraps the story in it via its own `DecoratorHandler.svelte`. So `withServices`, `withTheme` and `withLocale` are side effects and only `withPanelWidth` wraps (.storybook/PanelWidth.svelte:1-38). `withLocale` calls `setLocale(locale, { reload: false })` + `applyDocumentLocale(locale, getTextDirection(locale))` — the same pair src/sidepanel/main.ts:196 applies at boot; `reload: false` because Storybook re-renders on a globals change anyway and Paraglide's default reload would throw away the preview iframe. Toolbar items for the locale axis come from Paraglide's compiled `locales` tuple, so a locale added to settings.json appears with no edit here.
- **claude-opus** (2026-08-23T02:06:00.000Z): SERVICES SEAM (.storybook/story-services.ts:1-90). The card asked me to judge how; here is the judgment. `initSidePanelServices` throws on a second call and Storybook is ONE module graph for the whole session, so the per-test-FILE isolation Vitest gives the fakes for free is not available — a single init would let one story's canned data leak into the next. So the singleton is initialised once, lazily, and `resetSidePanelStoryServices()` refills it IN PLACE (`Object.assign` over the same object, so the identity `sidePanelServices()` hands out never changes) before every story renders; `initStoryServices()` runs at preview module scope so the slot is never empty when a story module is imported. Nothing forks the fakes — `createFake*Services`/`initFake*Services` are imported verbatim and a story seeds by reassigning a port method, exactly as the testing modules' own USAGE blocks document.
- **claude-opus** (2026-08-23T02:07:00.000Z): ONE REAL GAP in the shared fakes, fixed rather than worked around: src/sidepanel/testing/fake-services.ts imported `vi` from vitest for a single `vi.fn()` default on `createFakeExtensionShell`. Vitest is not resolvable in a browser bundle, so that import took the whole Storybook preview down before a story could render. Replaced with a plain no-op (src/sidepanel/testing/fake-services.ts:378-397) and dropped the import; every test that ASSERTS on `openOptionsPage` already installs its own spy first (OverflowMenu.test.ts:97, ModelPicker.test.ts:70, Transcript.test.ts:31), so nothing regressed — 1278 tests still green. Both surfaces' fake bundles are now importable from any bundler.
- **claude-opus** (2026-08-23T02:08:00.000Z): The endonym table WAS NOT exported — it was a `const` inside src/options/components/SettingsSection.svelte's instance script. Rather than journal a duplicate into `.storybook/`, extracted it to src/ui/localeNames.ts:1-53 (`localeEndonym`), which SettingsSection.svelte:47 and preview.ts:49 now both read. Two hand-written copies of ten labels with no compiler between them was the alternative; this way the `Record<Locale, string>` still fails `npm run check` if a locale is added without a name. Note for cards 124/125: SettingsSection's own story gets the table for free.
- **claude-opus** (2026-08-23T02:12:00.000Z): PROOF STORIES, all colocated. src/sidepanel/components/HistoryPanel.stories.svelte:1-107 (4 stories — seeded summaries, empty, storage failure, 320px — through `services.sidepanel` seeds typed against `SidePanelServices`); src/options/components/HeadersEditor.stories.svelte:1-76 (3 stories, incl. the reserved-`Authorization` error built from the REAL `reservedHeaderReason` + `providerReservedHeaderMessage`, and a `description` snippet child); src/ui/components/Markdown.stories.svelte:1-72 (rendered sample with bash/JSON code blocks, table, blockquote; a mid-stream incomplete-markdown story; a 320px one). Drove the dev server with Playwright and screenshotted four combinations — HistoryPanel dark/**ar** came back with `lang="ar" dir="rtl"`, the panel mirrored to the right, Arabic search placeholder and Arabic plural forms ("12 رسالةً · 3 استدعاءات أدوات"); HeadersEditor in `de` showed the translated reserved-header sentence; Markdown in `ja` showed the localized "コピー" button. The whole toolbar stack is genuinely live, not cosmetic.
- **claude-opus** (2026-08-23T02:15:00.000Z): GUARD (scripts/guard-stories.mjs:1-172). Same shape as guard:i18n — a set diff, printed per file, no heuristics. Components = the three non-recursive `*/components/*.svelte` directories, so the vendored `src/ui/components/ui/**` kit is excluded by construction. Coverage = a colocated `<Name>.stories.svelte`. SEEDED: **41** of 44 components are listed in scripts/story-allowlist.json (28 side panel, 13 options; the 3 with stories are not) — generated by the script's own `--seed` flag, so cards 124/125 regenerate rather than hand-edit if they need to. The allowlist is EXACT like scripts/throw-allowlist.json: proved all three failure modes — a planted `src/ui/components/PlantedGap.svelte` fails as an uncovered component, an entry for a component that now has a story fails as STALE, an entry for a deleted file fails as DANGLING. Excluded from the other guards the way tests are: `isTestCode()` in scripts/lib/source-scan.mjs:48-68 now matches `.stories.svelte` (that is what `guard:throws` and `guard:return-types` skip on), and .dependency-cruiser.cjs:445 excludes them from the cruise — a story imports its surface's `testing/fake-services`, which `only-roots-construct-infra` reserves for the four composition roots, and widening `ROOTS` to ~44 files would have emptied that rule of meaning. `guard:biome` still formats and lints them, and `npm run check` type-checks them like any other `.svelte`.
- **claude-opus** (2026-08-23T02:18:00.000Z): CI + type programs. `build-storybook` runs IN the gate job after `build` (.github/workflows/ci.yml) rather than as a parallel job: it reuses that job's `npm ci` and warm node_modules and takes ~3s against the gate's ~25s, where a parallel job would pay a fresh checkout + setup-node + npm ci to save three seconds and add a second required check for no extra signal. Output is gitignored and not uploaded — decision 42 declines to publish. actionlint clean. The config directory is split across BOTH type programs deliberately, one file each so nothing is checked twice: `.storybook/main.ts` + `.storybook/vite.config.ts` in tsconfig.node.json (build config, node), `.storybook/preview.ts` + `story-services.ts` + `*.svelte` in tsconfig.app.json (browser, svelte-check). Proved both halves are really checked by planting a type error in each and watching the right tool fail. `.storybook/**` added to biome.jsonc's `files.includes`. GATES ALL GREEN: `npm test` 1278/1278 (80 files), `npm run check` 0 errors, `npm run guard` all seven green incl. guard:stories, `npm run build`, `npm run build-storybook`.
- **claude-opus** (2026-08-23T02:20:00.000Z): GUIDANCE FOR CARDS 124/125. (1) Work the allowlist top-down and delete each line as its story lands — the guard fails on a stale entry, so the list cannot drift. Regenerate with `node scripts/guard-stories.mjs --seed` if a component is added or renamed mid-flight. (2) Four side-panel components do NOT read app-services directly — Composer, ApprovalCard, Transcript, ModelPicker read the panel's own stores (see src/sidepanel/testing/fake-services.ts's "WHAT THIS DOES NOT COVER"). Their tests `vi.mock` the store module, which a story cannot do; either seed the store through the services the store itself reads, or extend the store modules with a testing seam — do NOT invent a story-only mock. Journal whichever way you go. (3) `parameters.services` seeds must be NAMED, TYPED consts (`(services: SidePanelServices): void`), not inline arrows — Storybook types `parameters` as an open `any` bag, so an inline callback is an implicit-any failure under this repo's strictness. (4) Add `parameters.panelWidth` to side-panel stories and leave it off options/shared-UI ones. (5) When the last line goes, delete scripts/story-allowlist.json entirely — the guard treats a missing file as "full coverage required", and its final message changes to say so.
