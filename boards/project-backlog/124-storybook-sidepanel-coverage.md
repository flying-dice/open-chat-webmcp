---
column: review
agent: claude
live: false
labels: [frontend]
priority: med
updatedAt: 2026-08-23T14:50:00.000Z
---
# Storybook: side-panel coverage

Colocated stories for every non-vendored src/sidepanel component (and the
shared src/ui/components/Markdown.svelte if card 123's proof slice didn't
cover it), per decisions/42: at least one story each, and state-per-story
for the behaviour-rich ones — Transcript (streaming, notes/kinds, legacy
prose, activity groups), ApprovalCard (pending/annotations), ContextChip
(sharing/not-sharing/restricted), SelectionChip, ModelPicker (grouped,
degraded provider, manual entry), ToolsPanel (all four empty states),
history rows (active, clamped-long, deleting), notices, ContextMarkers.
Reuse the fake-services/fixture data; no story-only mocks. Remove every
covered component from guard:stories' allowlist. Spot-check ar + narrow
width via the toolbar for the RTL-sensitive stories and journal.

## Checklist

- [x] Every sidepanel component has stories; rich states covered; allowlist entries removed
- [x] RTL + 320px spot-checks journalled for the sensitive stories
- [x] npm test, npm run check, npm run guard (guard:stories shrinking), build-storybook green

## Gates

- [x] tests-passing — npm test: 1278/1278 (80 files) green, unchanged (claude, 2026-08-23T02:38:20.000Z)
- [x] typecheck — npm run check: svelte-check + tsc, 0 errors (claude, 2026-08-23T02:33:04.000Z)
- [x] guard — npm run guard: all seven sub-guards green, guard:stories now 44/44 with story-allowlist.json deleted (claude, 2026-08-23T02:41:10.000Z)
- [x] build — npm run build green (claude, 2026-08-23T02:41:30.000Z)
- [x] build-storybook — npm run build-storybook green, no console errors on the seeded stories (claude, 2026-08-23T02:41:48.000Z)

## Comments

- **claude** (2026-08-23T02:20:00.000Z): Picked this up. Read card 123's journal + "guidance for cards 124/125" in full, HistoryPanel.stories.svelte/HeadersEditor.stories.svelte/Markdown.stories.svelte as templates, .storybook/story-services.ts and .storybook/preview.ts for the seam contract, and every one of the 28 sidepanel components on scripts/story-allowlist.json plus their `*.test.ts` files (where present) to see exactly what each one reads. Confirmed which of the four "reads a store" components card 123 flagged actually need a seam: ApprovalCard.svelte does NOT (it imports `approve`/`deny` from stores/approvals.svelte, but takes `request: PendingApproval` entirely as a prop — src/sidepanel/components/ApprovalCard.svelte:54-69); Transcript.svelte reads `approvals.pending` (empty by default, matching Transcript.test.ts's own baseline) and the real `openOptionsPage` (calls the fake shell's no-op) — neither needed touching. Composer.svelte and ModelPicker.svelte both read `../stores/selection.svelte`'s module state, which only the async `syncToTab` ever populates in production — that one genuinely needed a seam.
- **claude** (2026-08-23T02:22:00.000Z): SEAM: `setSelectionStateForTesting` + `EMPTY_SELECTION_STATE`, added to src/sidepanel/stores/selection.svelte.ts:561-616. Judgment call, written up in that function's own doc comment: `syncToTab` (the one production path that fills `selection`'s state) is async and multi-step, and Composer.test.ts/ModelPicker.test.ts already drive their components by `vi.mock`-ing this whole module wholesale, backed by a plain `state` object shaped exactly like the new `SelectionStateSnapshot` — Storybook has no per-story `vi.mock` equivalent (one module graph for the whole session), so this function sets the SAME state the real `selection` getters read, directly, through a real exported seam rather than a story-only double. Typed, explicit return type (`void`), journalled. Also reused two EXISTING real production seams with no new code needed: `presenter.show(session)` (src/sidepanel/stores/panel.svelte.ts:139) for OverflowMenu.stories.svelte's recent-chat highlighting/export-enabled state, and left ToolCallRow.svelte's `panel.toolCalls` lookup at its honest default (empty — nothing in Storybook ever calls `presenter.show` for that story, so `logEntry` is `undefined` and the duration label falls back exactly as it does for a real panel before any chat loads) — journalled in ToolCallRow.stories.svelte's own header rather than adding a seam for a duration label that already degrades sensibly.
- **claude** (2026-08-23T02:24:00.000Z): BUG FOUND AND FIXED while driving ModelPicker.stories.svelte/Composer.stories.svelte live: calling `setSelectionStateForTesting` synchronously from `parameters.services`'s seed hook threw Svelte's `state_unsafe_mutation` — `.storybook/preview.ts`'s `withServices` decorator runs inside a reactive derivation the Svelte-CSF addon builds internally, and Svelte refuses a direct `$state` write from there (a PLAIN object write, like the fakes' `services.chats.listChatSummaries = ...`, has no such restriction, which is why HistoryPanel's older seed never hit this). Fixed by deferring the write one `queueMicrotask` inside `setSelectionStateForTesting` itself (selection.svelte.ts:609-618) — confirmed live in the browser (see next entry) that this clears the error and the seeded state renders correctly on the story's very next paint. Documented as part of the function's own doc comment, including the consequence for a future test that calls it directly (`await Promise.resolve()` before asserting).
- **claude** (2026-08-23T02:30:00.000Z): ALL 28 SIDEPANEL STORIES landed, one file each, colocated: ActivityGroup (done/live-expanded/contains-a-failed-step), ActivityIndicator (waiting/calling-page/calling-server), AnnotationBadges (read-only/untrusted/server-destructive/unannotated), ApprovalCard (pending page tool/pending server tool destructive/origin-unknown), CallLogEntry (auto-success-collapsed/denied/server-error/still-running), CallLogPanel (with entries/empty), Composer (ready/busy/all four card-35 blocked states + at-320px), ContextChip (sharing/sharing+content/not-sharing/restricted/connecting/error/no-tab/320px), ContextMarkers (selection/selection+shortened/320px), Header (renamable/non-chat-view/new-chat-disabled/320px), HistoryListItem (ordinary/active/long-clamped/deleting/320px), Icon (custom glyph/standard/mark size), IconButton (plain/filled-primary/filled-danger/compact/disabled), Inspector (default/empty-restricted), MessageActions (copy-only/copy+regenerate), ModelPicker (grouped/degraded-provider-with-fix/manual-entry), NoticeCard (restricted-standing/dismissible/failure), OverflowMenu (with-recent-chats/disconnected), SchemaProperty (required-string/enum/nested-object/array-of-objects), SelectionChip (english/RTL-arabic/320px), ToolArgValue (string/empty-string/number/boolean/null/undefined/array/nested-object), ToolArgs (a-few/none/undefined), ToolCallRow (success/running/denied/error/untrusted-content/stalled/server-tool), ToolListItem (page/server/unannotated), ToolSchema (with-properties/no-parameters/no-declared-schema), ToolsPanel (all four empty states + no-server-tools + populated), Tooltip (top/bottom), Transcript (streaming/note-kinds-incl-provider-error-with-fix-block/legacy-prose-passthrough/activity-group-live-expanded/empty/320px). All fixture data is plain, typed, named consts — no story-only mocks; every store-reading component goes through the real, unmocked store module.
- **claude** (2026-08-23T02:36:00.000Z): TYPECHECK FIXES: ModelPicker.stories.svelte imported `ModelListEntry` from `../../domain/providers` (wrong — it's `selection.svelte.ts`'s own interface); fixed the import. NoticeCard.stories.svelte/Tooltip.stories.svelte both put slotted tag content where `{...args}` was already spread, and since both components declare a required `children: Snippet` prop, Svelte's compiler correctly refused to guess which "children" should win (`'children' is specified more than once`) — fixed by passing `children={theSnippet}` explicitly by name instead of as tag content, the same way HeadersEditor.stories.svelte passes its `description` snippet by name. `npm run check`: 0 errors after both fixes.
- **claude** (2026-08-23T02:41:00.000Z): ALLOWLIST: removed all 26 remaining sidepanel entries via targeted single-line Edits (Icon.svelte's line went first, separately, right after its story landed). The options-group agent finished emptying its own half of the same file concurrently — my last edit (Transcript.svelte's line) landed on a file that had already been reduced to exactly that one line by the other agent's edits, so per the card's instruction ("if you remove the final remaining entry overall, delete the file") I deleted scripts/story-allowlist.json outright rather than leaving an empty `"uncovered": []`. `guard:stories` now reports 44/44 and its own missing-file branch ("every non-vendored component has a colocated story", no allowlist reference at all).
- **claude** (2026-08-23T02:38:00.000Z): RTL + 320px SPOT-CHECKS, driven live against `npm run storybook` with Playwright (card 123's method) — discovered partway through that this session's Playwright browser is SHARED with the parallel options-group agent (a `browser_tabs list` call turned up its tab, mid-navigation, on a completely different story than the one I'd just asked for), so every check below was taken in a dedicated new tab (`browser_tabs {action:"new"}`) to avoid cross-talk. **ContextChip** at `ar` + 320px (`side-panel-contextchip--at-320-px`): the dismiss ✕ sits at the strip's leading (right, under `dir="rtl"`) edge, the tool-count/chevron/favicon+status-dot cluster mirrors to the trailing edge, confirming the physical `rtl:-scale-x-100` chevron and the logical `ps-`/`pe-` utilities both resolved correctly. **SelectionChip** "RTL excerpt" at `ar`: the dismiss button is on the far left (mirrored), the bold "النص المحدد" label and the Arabic excerpt itself read right-to-left via `dir="auto"`, and the interpolated `Origin` token inside the excerpt stayed a correctly-isolated LTR run instead of having its surrounding punctuation flip. **ContextMarkers** "Selection + shortened page content" at `ar`: both pills render right-aligned (`justify-end` under `dir="rtl"`), with the first marker (page-selection) at the visual right (RTL reading start) and the second (page-content, shortened) to its left — the list's `justify-end` + logical-order pairing confirmed live, not just read off the source.
- **claude** (2026-08-23T02:39:00.000Z): Also live-verified the two components riding the new `setSelectionStateForTesting` seam actually render real data, not just type-check: ModelPicker's "Grouped" story shows Local Ollama's heading with llama3.1 checked-active, an "Unverified" section (mystery-model, "Capability check timed out."), and a "No tool support" section (tinyllama + the `ollama pull llama3.1` hint) — exactly decisions/22's three-bucket layout. Composer's "Blocked: no providers registered" story shows the empty-state copy and "Open options to add a provider" button. Both confirmed AFTER the `queueMicrotask` fix (the previous attempt threw `state_unsafe_mutation` in the console, caught by watching for the tool result's own error log rather than only trusting the screenshot).
- **claude** (2026-08-23T02:42:00.000Z): GATES, run after the `queueMicrotask` fix and the allowlist-file deletion (so they reflect the final state): `npm test` 1278/1278 (80 files) unchanged; `npm run check` 0 errors; `npm run guard` all seven sub-guards green, `guard:stories` 44/44 (0 awaiting cards 124/125); `npm run build` green; `npm run build-storybook` green (production build, independent of the shared dev-server port contention noted above). Did not touch `src/options/**` or run `npm run verify`, per the card's scope. Ready for review.
