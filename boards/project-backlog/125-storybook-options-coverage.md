---
column: review
agent: claude
live: false
labels: [frontend]
priority: med
updatedAt: 2026-08-23T02:39:00.000Z
---
# Storybook: options coverage

Colocated stories for every non-vendored src/options component per
decisions/42: sections (providers with rows/badges/test outcomes, MCP with
OAuth states, settings policies, history incl. the clear-all dialog,
attribution), forms (ProviderForm masked-key/headers/validation-error
states, McpServerForm auth modes + the extracted McpOAuthPanel states),
PresetPicker, RegistryRow, HeadersEditor (reserved-name error),
McpTestResult (ok/error/tool-list disclosure). Reuse the options
fake-services/fixtures; empty guard:stories' allowlist to zero and flip
the guard to no-allowlist mode so future components require stories.

## Checklist

- [x] Every options component has stories; form error/edge states covered
- [x] guard:stories allowlist emptied and strict mode on
- [x] npm test, npm run check, npm run guard, npm run build, build-storybook green

## Gates

- [x] tests-passing — npm test green, 1278/1278 (80 files) (claude, 2026-08-23T02:38:15.000Z)
- [x] types-clean — npm run check 0 errors (claude, 2026-08-23T02:38:00.000Z)
- [x] build-green — npm run build and npm run build-storybook both green (claude, 2026-08-23T02:39:00.000Z)

## Comments

- **claude** (2026-08-23T01:20:00.000Z): Picked this up. Read card 123's journal/guidance (CSF syntax, named typed `parameters.services` consts, the `.storybook/story-services.ts` seam — options side uses `resetOptionsStoryServices`/`initFakeOptionsServices`) and the HeadersEditor proof story as the template. Read every one of the 13 options components on the allowlist plus their existing `*.test.ts` siblings (ProviderForm.test.ts, McpServerForm.test.ts, McpServersSection.test.ts) to lift real fixtures and the bits-ui Select "portal, `findByText` not `findByRole`" recipe from, rather than inventing a second one.
- **claude** (2026-08-23T01:55:00.000Z): Wrote the 5 presentational, services-free stories first: src/options/components/AttributionSection.stories.svelte:1-16 (1 story, static content only), PresetPicker.stories.svelte:1-19 (1 story), McpTestResult.stories.svelte:1-46 (5 stories incl. a `play`-driven disclosure-expand and the "not an MCP endpoint" kind card 39 calls out by name), RegistryRow.stories.svelte:1-70 (5 stories over the shared row shell with stand-in badges/actions snippets), McpServerRow.stories.svelte:1-70 (6 stories — disabled/bearer/needs-reconnect badges via the real `oauthNeedsReconnect`), ProviderRow.stories.svelte:1-85 (6 stories — real preset backend badge via `getPreset`, stale-default badge, blocked-default reason, a test outcome with its copyable fix command).
- **claude** (2026-08-23T02:10:00.000Z): McpOAuthPanel.stories.svelte:1-146 — the interesting one. `createOAuthSignIn` (../forms/oauthSignIn.svelte.ts, card 113's extracted machine) takes no services itself but its `signIn`/`continueManual` read `optionsServices()` at call time, so 3 states (not connected / connected / needs reconnect) come straight from `initialAuth`, and 3 more (signing in, manual-registration panel, sign-in failed) are REAL machine transitions: each story seeds `mcpSignIn.begin` through `parameters.services.options` and a `play` function clicks the actual "Sign in" button — the same states oauthSignIn.svelte.test.ts drives, reached through the UI instead of the machine's API directly. Confirmed `<Story play={...}>` is genuinely wired (not just a no-op prop) by reading node_modules/@storybook/addon-svelte-csf/dist/runtime/create-runtime-stories.js and Story.svelte before relying on it, and confirmed `storybook/test` (not a separate `@storybook/test` package — that split happened in Storybook 8+) exports `within`/`userEvent`/`waitFor`/`expect`.
- **claude** (2026-08-23T02:25:00.000Z): The three services-backed sections + two remaining forms: HistorySection.stories.svelte:1-90 (seeded/empty/storage-failure + a `play`-driven "Clear-all confirmation open" — the card's dialog portal check, see below), McpServersSection.stories.svelte:1-89 (a "Seeded — mixed auth states" story with a bearer-token row, a reconnect-needed OAuth row and a disabled row side by side — the card's explicit "OAuth-state rows" ask), ProvidersSection.stories.svelte:1-111 (rows/badges, a stale-default banner for a `getDefaultSelection` pointing at a deleted provider id — decisions/97 — and a `play`-driven "Test connection succeeded" outcome, since the outcome lives in an internal `gate.outcomes` this section has no prop for), SettingsSection.stories.svelte:1-51, ProviderForm.stories.svelte:1-104 (hosted-preset prefilled, a LOCAL-but-`type:"openai"` preset's "Add one anyway" escape hatch via a real click — the Ollama preset can't demonstrate this, its `type:"ollama"` never shows the key field at all, so I used the LM Studio preset instead — masked API key, the blank-name validation error using ProviderForm.test.ts's own exact recipe (`required` blocks a truly empty submit; whitespace-only reaches the JS check), and the reserved-`Authorization`-header error rendered inline with no interaction needed since `headerRowError` is derived live), McpServerForm.stories.svelte:1-181 (bearer/oauth-connected/needs-reconnect auth modes via `initial`, plus choosing OAuth from a fresh add via a real Select interaction, plus the same 3 machine-transition states as McpOAuthPanel).
- **claude** (2026-08-23T02:33:00.000Z): Removed all 13 options lines from scripts/story-allowlist.json via targeted single-line Edits (never a whole-file rewrite) while the sidepanel agents were removing their own lines from the same file concurrently — hit "modified on disk" conflicts on 8 of the 13 edits, re-read each time per the skill's guidance, and every edit still applied cleanly against the current content. `node scripts/guard-stories.mjs` confirmed 0 options entries left. The sidepanel side finished shortly after and removed the file's last remaining line themselves, which (per card 123's guidance #5) deleted scripts/story-allowlist.json entirely — I didn't do that deletion myself, but confirmed after the fact that `guard:stories` now reports "44/44 component(s)... every non-vendored component has a colocated story" with the file gone, i.e. strict no-allowlist mode.
- **claude** (2026-08-23T02:38:00.000Z): PORTAL FINDINGS — verified live, not just inferred. Drove `npm run storybook` and used Playwright against `http://localhost:6006/iframe.html?id=<story>&viewMode=story` directly (bypassing the manager) to check the two portal-bearing patterns actually render: (1) HistorySection's `AlertDialog` — bits-ui portals `AlertDialog.Content` straight into the iframe's own `document.body`, no workaround needed, confirmed by asserting through `canvasElement.ownerDocument.body` and seeing "Delete all 2 chats?" render and become interactive; (2) McpServerForm's/ProviderForm's `Select` (a popover, not a dialog) — same portal-to-iframe-body behaviour, no workaround for FINDING the option (`findByText`, matching the test file's own documented reason: `getByRole`/`findByRole` never sees anything inside a bits-ui floating-layer in this jsdom-free, real-browser context either, since floating-ui's `visibility:hidden`-until-positioned trick still applies). BUT found and fixed one real bug the same run: a click immediately after closing the Select intermittently hit `pointer-events: none` on `document.body` — bits-ui's scroll-lock release is a real `setTimeout`, same race McpServerForm.test.ts's `afterEach` documents for jsdom. Fixed by polling `getComputedStyle(body).pointerEvents` via `waitFor` in `chooseOAuthMode()` (McpServerForm.stories.svelte:105-116) rather than a fixed sleep, which was still flaky at 50ms. Also caught and fixed a second real race: McpServersSection's "Add form opened" `play` fired before the section's own `onMount` load finished, hitting "Loading MCP servers…" with no button yet — switched `getByRole` to `findByRole` (McpServersSection.stories.svelte:82-89). Re-verified every fixed story and ~10 others (McpOAuthPanel's 3 machine-transition stories, ProvidersSection's seeded/stale-default/test-outcome stories, ProviderForm's Add-key-anyway and validation-error stories, SettingsSection's read-failure story) with zero console errors after the fixes.
- **claude** (2026-08-23T02:39:00.000Z): GATES. `npm test` 1278/1278 (80 files, unaffected — stories excluded from the suite). `npm run check` 0 errors (the only errors seen mid-work were all in `src/sidepanel/**`, the parallel cards' territory, never mine). `npm run guard:biome`/`boundaries`/`clean-code`/`return-types`/`throws`/`i18n` all green against my files (the 5 remaining biome format failures at the end of the session are all `src/sidepanel/*.stories.svelte`). `node scripts/guard-stories.mjs` — 44/44, allowlist file gone, strict mode confirmed. `npm run build` and `npm run build-storybook` both green. Never touched `src/sidepanel/**`, never ran `npm run verify`, no commits made. Moving to review.
