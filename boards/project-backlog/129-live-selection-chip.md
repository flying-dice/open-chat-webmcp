---
column: review
agent: claude-opus
live: false
labels: [frontend, backend]
priority: high
updatedAt: 2026-08-24T02:15:00.000Z
---
# Live selection chip (Gemini-style)

Per decisions/40 as revised today: the chip updates as the user selects
different text, without interacting with the panel. Relay: debounced
document selectionchange listener emitting a content-free
runtime:selection-changed ping (single-sourced message list; notify only
when the settled state actually differs — no per-character spam; covers
form-control selections, which fire document selectionchange in Chrome).
Worker: route like tools-updated with the sender tab id. Panel: the infra
chrome-runtime layer exposes a subscription port (stores may not touch
chrome.*); pageSharing reacts to a ping for the CURRENT tab by running the
existing gated refreshSelection — every guard (no pageInfo, restricted,
dismissed, superseded pulls, dismissed-text) applies unchanged. Update
docs/03 + PRIVACY.md wording per the decision; extend the sharing-gate
scenario with a no-panel-interaction step (select → chip appears; change
selection → chip text changes; dismiss gate → pings ignored). Keep the
diagnostic breadcrumbs coherent with the new trigger.

Also carries two spec additions Jonathan asked for on 2026-08-24 while
comparing against Gemini's panel live: a 3-character minimum selection
length enforced inside `extractSelection` itself (not panel-side, so a
sub-minimum selection can never be silently attached at send), and
keyboard/shift+arrow extension reflected in the chip essentially
immediately (tight settle debounce; length is part of the change identity).

## Checklist

- [x] Ping is content-free and debounced; relay tests incl. no-spam and form-control coverage
- [x] Worker routing tested at the fake-chrome seam; protocol single source updated
- [x] Panel subscription port + gated reaction; dismissed gate proven to ignore pings
- [x] Minimum selection length (3 chars) enforced in extractSelection, with unit tests
- [x] docs/03 + PRIVACY.md updated; decision 40 revision referenced
- [x] Sharing-gate scenario extended with the live-update steps, 3x green
- [x] npm test, npm run check, npm run guard, npm run build, npm run verify green

## Gates

- [x] tests-passing — npm test green, 1324 tests / 81 files (claude-opus, 2026-08-24T02:05:00Z)
- [x] typecheck — npm run check green, 2077 files 0 errors (claude-opus, 2026-08-24T02:05:00Z)
- [x] guards — npm run guard green, all seven (claude-opus, 2026-08-24T02:06:00Z)
- [x] verify — npm run verify green, 10/10 required + 2 best-effort (claude-opus, 2026-08-24T02:12:00Z)

## Comments

- **claude-opus** (2026-08-24T02:15:00.000Z): SHIPPED the live chip. THE MESSAGE: `runtime:selection-changed` in src/infra/chrome-runtime/protocol.ts:201-226, registered in `RUNTIME_MESSAGE_TYPES` (the `Record<RuntimeMessage["type"], true>` single source) and added to `RuntimeNotification`, which is what let src/background/sw.ts:337-345 widen `broadcastToolsUpdated` into `broadcastToPanel(msg: RuntimeNotification)` rather than grow a second copy of the same fire-and-forget + `lastError` read. It carries `{type, tabId}` and nothing else, deliberately — sw.test.ts:172-183 asserts the broadcast's key set is exactly `["tabId","type"]`, so a future "just the length, it's only a number" is a failing test rather than a code review. THE DEBOUNCE: src/content/relay.ts:82-97 (`SELECTION_DEBOUNCE_MS = 150`, at the short end on purpose — the chip is being watched while the user selects) and the listener at src/content/relay.ts:620-670. Two filters, not one: the 150ms settle collapses a drag or a held-down shift+arrow, and then `selectionIdentity` (src/infra/dom/page-extraction.ts:640-676) drops the ping entirely unless the settled selection actually differs from the last one REPORTED — a re-applied identical range from an SPA re-render, or a click that only collapses a caret, wakes nobody. The identity is derived from `extractSelection` itself rather than from `getSelection().toString()`, so the ping and the chip cannot disagree about what counts as a selection (min-length, form controls, the password exclusion, the cap are all already applied), and it is a byte count plus a 32-bit FNV-1a in base 36 — the only selection-derived value the relay keeps between events, and not text.
- **claude-opus** (2026-08-24T02:16:00.000Z): PANEL WIRING SHAPE — judged as the brief asked. The subscription surfaces through the EXISTING `chrome.runtime.onMessage` listener in src/infra/chrome-runtime/tab-sync.ts:370-392 (one listener, two broadcasts, one active-tab test; a sibling port would have meant a second registration to remember to tear down and a second copy of "which tab is on screen"). `TabSyncView` gained a fourth method, `selectionMaybeChanged(tabId)` (src/infra/chrome-runtime/tab-sync.ts:74-92). It is NOT implemented by ./stores/panel.svelte's `tabSyncView` — the gate store reads the page store, so that import would close a cycle — so the composition root assembles the seam from both stores at src/sidepanel/main.ts:213-224. That is the root doing its job rather than a store reaching sideways. The panel end is `notifySelectionMaybeChanged` (src/sidepanel/stores/pageSharing.svelte.ts:395-410), which checks the ping is for the page the panel has actually resolved and then goes through the SAME coalescer as a gesture: `initPageSharingSync`'s window is now exposed to the module through a `requestPull` slot (pageSharing.svelte.ts:379-393, 447-459), cleared on teardown and only by the sync that installed it. That was the point of the brief's "same coalescer" instruction and it earns its keep — releasing the mouse in the page and clicking into the composer is ONE intention arriving as a ping plus a gesture, and two pull paths would have raced each other's `pullToken` and put the page's text on the wire twice.
- **claude-opus** (2026-08-24T02:17:00.000Z): GATE-DROP PROOF, three independent ones. (1) Unit: pageSharing.svelte.test.ts's "makes NO PULL AT ALL while sharing is dismissed" dismisses the gate, fires a ping, then waits 700ms — past the coalescer's trailing window — and asserts the port was never called. It passes because the ping goes through `refreshSelection`'s existing gate check, not a second gate written for this card; there is one gate, and the ping dies at it. (2) The same file also proves a ping for a tab the panel is not showing pulls nothing, and that a ping after teardown pulls nothing. (3) End to end: the scenario's new "LIVE + GATE" step changes the page's selection TWICE with sharing dismissed, waits 3s (many times the relay debounce and the panel window over), and asserts zero "Selected text" chips while `data-sharing` is still false. Also worth recording: the adapter drops a ping for a background tab before the panel ever hears it (tab-sync.test.ts's "drops a ping for a BACKGROUND tab"), so answering a ping is never a read of a tab the user is not looking at.
- **claude-opus** (2026-08-24T02:18:00.000Z): JONATHAN'S TWO SPEC ADDITIONS (2026-08-24, from the live Gemini comparison), both landed. MINIMUM LENGTH: `MIN_SELECTION_CHARS = 3` at src/infra/dom/page-extraction.ts:76-97, applied in `extractSelection` after normalisation and trimming — so it covers the document path and the form-control path identically, and "  ab  " measures as the two characters it is. Enforced there and not panel-side for the reason the constant's doc comment now states: the send-time re-read is what actually attaches text to a turn, so a floor applied only where the chip is drawn would let a two-character selection ride along with a message having never been shown to anyone, which is precisely the visible-artifact rule in decisions/40. Five unit tests at page-extraction.test.ts (2 chars → empty, 3 chars → offered, padded "  ab  " → empty, form-control below the floor → empty, three CJK characters → offered, since the floor counts CHARACTERS and the cap counts bytes). KEYBOARD EXTENSION: covered by the 150ms settle plus length-in-identity — pinned three ways, at the identity level (page-extraction.test.ts's "differs when a selection is EXTENDED", four successive extensions of one anchor → four distinct identities), at the relay level (relay.test.ts's shift+arrow test → four pings), and in the browser (the scenario's "extending the selection to the whole panel section updates the chip again", 202 chars).
- **claude-opus** (2026-08-24T02:19:00.000Z): TESTS + THE JSDOM FINDING. New file src/content/relay.test.ts (10 tests) — the relay had none, so vitest.config.ts:70-76 now includes `src/content/**/*.test.ts` in the "domain" project, with the file opting itself into jsdom via a `@vitest-environment` docblock (the same per-file exception page-extraction.test.ts takes, rather than widening the project). MEASURED, since the card asked: jsdom 30 DOES fire `selectionchange`, for document ranges on every change AND for a focused `<input>` — but only the FIRST change on that input; three successive `setSelectionRange` calls produce exactly one event. So nine of the ten tests drive a real `Selection` and let the platform fire, and only the form-control EXTENSION half dispatches the event by hand, with the limitation written into the file's header. One more seam note recorded there: the relay cannot use sw.test.ts's `vi.resetModules()`-per-test pattern, because re-importing re-registers its `selectionchange` listener on the same jsdom document and nothing detaches the old one — ten imports would be ten live relays. Loaded once, with each test normalising the state instead. Also added: 5 sw.test.ts routing tests (sender-tab-id stamping, the empty-payload assertion, the drop with no sender tab, and "holds no state"), 5 tab-sync.test.ts tests (active tab, background tab, follows a switch, teardown, junk messages — and its fake's `runtime.onMessage.removeListener` now genuinely removes, since a no-op would have passed the teardown test for the wrong reason), and 7 pageSharing tests. 1324 green.
- **claude-opus** (2026-08-24T02:20:00.000Z): SCENARIO EVIDENCE, and an honest flake note. verify/checks/sharingGateScenario.mjs gained four consecutive steps that touch the PANEL not at all — select a different paragraph (`main .panel:nth-of-type(3) .hint`) and the chip follows it; extend to the whole section and it grows to 202 chars; clear the selection and the chip detaches on its own; re-select and it returns — plus the LIVE + GATE step described above. Those five are the only steps in the file with no panel interaction whatsoever, which is what makes them a test of the ping rather than of the focus pull. Result: 10/10 on runs 1, 3, 4, 5, 6, 7, 9, 10, 12 — well past three consecutive. Runs 2, 8 and 11 failed WHOLESALE at step 1 ("WebMCP Demo" never visible, 30s), and I chased it rather than shrugging: a throwaway probe (since deleted) showed the panel alive and logging, but tracking a RESTRICTED page — the demo tab's `onActivated` never reached it, so the panel sat on its own `chrome-extension://` tab for the whole run. It is the harness's pre-existing panel-as-a-tab race, NOT this card: the same probe reproduces it on stashed-out sources (1 failure in 8 baseline runs vs 4 in 20 on this branch — the same phenomenon, sampled twice). Worth a card of its own if it starts costing time; I have not opened one.
- **claude-opus** (2026-08-24T02:21:00.000Z): DOCS DELTA, in the existing register. PRIVACY.md's "Page access" section gains one sentence about the chip keeping up with what you select (chip only, sent only with your message, stops with the gate), and its "nothing is read in the background" bullet now names selecting text alongside focusing the panel and sending. docs/03-privacy-and-trust.md needed a correction as well as an addition: its "there is deliberately no page-context-changed notification message" sentence was made false by this card, so it now says the true and stronger thing — no message in the protocol can carry page text anywhere except in answer to a request the panel made — and a new short paragraph, "Selecting text updates the chip as you go", states the observation plainly, names `runtime:selection-changed` as carrying no text (not even a length), says the gate drops it, and mentions the 3-character floor so an accidental double-click is visibly accounted for. Gates: npm test 1324/1324, npm run check 0 errors, npm run guard all seven ok, npm run build (dist rebuilt — reload the unpacked extension and it is there), npm run verify 10/10 + both best-effort. Not committed, as instructed.
