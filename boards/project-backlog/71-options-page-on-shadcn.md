---
column: review
labels: [frontend]
priority: high
agent: claude-opus
live: false
updatedAt: 2026-08-22T16:10:00.000Z
---
# Options page on shadcn-svelte (wholesale)

Migrate the whole options page in one card per
decisions/28-shadcn-svelte-maia-zinc.md: options.css (386 lines of global
classes) is load-bearing for 7 of 11 components that have no scoped styles,
so the page cannot migrate incrementally. Keep the five-section single-page
structure and all flows: ProvidersSection (CRUD, reorder, set-default,
test-connection) with PresetPicker → ProviderForm; SettingsSection (the two
approval policies → RadioGroup or Select + Field); McpServersSection with
McpServerForm (including the OAuth sign-in flow — behaviour untouched);
HistorySection (clear-all with its confirm step → AlertDialog);
AttributionSection (Flaticon licence MUST stay visible).

Map .section→Card, .badge→Badge, .btn-*→Button variants, forms→Field +
Input/Label, .note→Alert, .test-result→Alert variants, .empty-state→Empty,
.preset-grid/.preset-tile→Card grid. Delete options.css entirely at the end.

## Checklist

- [x] All 11 options components migrated; options.css deleted; no scoped CSS remains except where Decision 28 allows
- [x] Provider add/edit/delete/reorder/set-default/test flows work end to end
- [x] MCP server add/edit/delete/test and OAuth sign-in flow work end to end
- [x] Approval-policy settings persist and reflect current values
- [x] Clear-all-history confirm uses AlertDialog and still wipes chat:*, tabchat:*, chat:index
- [x] Attribution section visible; masked API-key field behaviour preserved
- [x] Light and dark render correctly across all sections
- [ ] npm run check, npm run build and npm run verify green

## Comments

- **claude-opus** (2026-08-22T16:10:00.000Z): Wholesale migration done — src/options/options.css deleted and all 11 components moved onto the vendored shadcn kit. Class mapping actually used: `.section`→`Card.Root/Header/Content` wrapped in a plain `<section aria-labelledby>` with a real `<h2>` inside `Card.Header` (Card.Title renders a div, and the headings were load-bearing for the section labelling); `.note`→`Alert.Root` + `Alert.Description` (`.note--warning`→`variant="destructive"`); `.badge`→`Badge variant="outline"`, `.badge--primary`→default, `.badge--danger`→`variant="destructive"`; `.btn-primary`→`Button` default, bare `<button>`→`variant="outline" size="sm"`, `.btn-plain`→`variant="ghost"`, `.btn-danger`→`variant="destructive"`, `.icon-btn` ▲▼→`Button variant="ghost" size="icon-xs"` with Hugeicons `ArrowUp01Icon`/`ArrowDown01Icon`; `.form`/`.field`/`.field-row`→`Field.Field`/`Field.Label`/`Field.Description`/`Field.Error` + `Input`; `.api-key-field`→`InputGroup.Root` + `InputGroup.Input` + an `inline-end` `InputGroup.Addon` holding the Show/Hide `InputGroup.Button`; native `<select>`→`Select.Root/Trigger/Content/Item`; `.empty-state`→`Empty.Root/Header/Media/Title/Description` (`PlugSocketIcon`, `Wrench01Icon`, `Message01Icon` replacing the 🔌/🛠️/💬 emoji glyphs); `.preset-grid`/`.preset-tile`→a Tailwind `auto-fill` grid of card-styled `<button>`s in src/options/components/PresetPicker.svelte:34-72 (a button, not a Card wrapping one, so the whole tile keeps its keyboard/AT semantics); `.policy-option`→`RadioGroup` + `RadioGroupItem` inside `Field.Label`/`Field.Field orientation="horizontal"`/`Field.Content`, whose `has-data-checked:` styling gives the selected card its own treatment for free (src/options/components/SettingsSection.svelte:184-198). Added `alert-dialog` and `radio-group` to the kit via `npx shadcn-svelte@1.5.0 add … -y -o --skip-preflight` (the run also rewrote button/, byte-identical).
- **claude-opus** (2026-08-22T16:12:00.000Z): The one thing that went beyond a pure re-skin, and the coordinator should know about it: src/options/main.ts:1-16 no longer imports `src/lib/theme.css`. That sheet is UNLAYERED CSS, and Tailwind's utilities live in `@layer utilities`, so its `button`/`input`/`select`/`h1-h3`/`body` reset outranks every shadcn utility regardless of import order — with it loaded, every migrated Button and Input repainted back to the legacy Chrome-native pill. Dropping it is scoped to the options bundle (verified: the built options entry links only the shared app.css chunk; theme.css's `:root` tokens and reset ship only in the sidepanel-only CSS chunk), so the side panel is untouched and card 72 still owns deleting the file. Two knock-ons, both journalled rather than fixed here: (1) src/lib/components/Markdown.svelte (rendered by ProviderForm/ProviderRow for a copyable `unreachable` fix command) styles itself from theme.css tokens, so on this page it now renders plain-but-legible until card 67 migrates it — its scoped block is the only `<style>` left reachable from options; (2) `color-scheme: light dark` came from theme.css, so src/options/index.html:8 now carries `class="scheme-light-dark"` (confirmed generated in the built CSS).
- **claude-opus** (2026-08-22T16:14:00.000Z): Behaviour preserved deliberately, with notes. Clear-all history is now an `AlertDialog` (src/options/components/HistorySection.svelte:122-157) instead of `confirm()`; `handleClearAll` at :57-68 no longer asks a question, and the dialog's `Action` calls `event.preventDefault()` so the dialog stays open showing "Clearing…" while `clearAllChats()` runs. Everything the old prompt spelled out (chat count, message count, "cannot be undone") is in the dialog description. The two `confirm()` calls for removing a provider/server were deliberately left as native prompts — the card only asked for the history one. Both `RadioGroup`s are CONTROLLED (`value` + `onValueChange`, src/options/components/SettingsSection.svelte:178-181) rather than `bind:value`, because `handlePolicyChange` reverts the selection when the storage write throws and a two-way binding would leave the UI showing an unsaved policy; the same controlled shape is used for `type` in ProviderForm and `transport`/`authMode` in McpServerForm, where the value is a union type rather than a plain string. `testResultClass` in src/options/lib/testResultDisplay.ts:18-27 and src/options/lib/mcpTestResultDisplay.ts:29-38 now returns Tailwind token classes instead of the deleted `.test-result--*` names — same three-way ok/error/neutral split, same shared-by-both-call-sites role; McpServerForm's OAuth status line reuses the identical treatment via `oauthStatusClass` (src/options/components/McpServerForm.svelte:144-151). chrome.identity / permission-request ordering in the OAuth and test-connection handlers was not touched.
- **claude-opus** (2026-08-22T16:16:00.000Z): Two pre-existing a11y bugs found in McpServerForm's OAuth panel and fixed in passing, since the markup was being rewritten anyway: "Manual app registration" was a `<label for="mf-oauth-client-id">` duplicating the real Client ID label below it (two labels, one control), and "OAuth sign-in" was a `<label for="mf-oauth-signin">` pointing at a `<p>` (a label can only name a form control). Both are `Field.Title` group headings now — src/options/components/McpServerForm.svelte:629 and :683. No flow changed. Also noted for card 73: neither PresetPicker nor ProviderRow ever rendered `iconForProvider` (src/lib/providers/presets.ts:209), so there is no presets→icons coupling to unpick on this page — `ProviderRow` imports only `getPreset` for its backend label.
- **claude-opus** (2026-08-22T16:18:00.000Z): Gates. `npm run check` — 810 files, 0 errors, 0 warnings, whole project clean (no foreign sidepanel errors to report). `npm run build` — green. `npm run verify` NOT run, per the coordinator's instruction that it collides with the parallel agents; that checklist item is left unticked for the batch run. For what it's worth, verify never exercises the options page — `verify/checks/screenshots.mjs` only looks up "More options" and a side-panel menu item — so nothing on this card should move the harness either way. Verified instead by building to a private `dist-card71` and driving the real extension in Chrome for Testing with its own temp profile: full-page light and dark screenshots of all five sections; the add flow through PresetPicker → ProviderForm; the MCP add form and its OAuth panel; seeded rows exercising every badge (Default, backend label, custom-header count, Permission needed/granted, Bearer token, Disabled); and an end-to-end CRUD pass — add ×2 → reorder (`Move Beta up` → order flips) → edit+save → local-preset hides the API-key field → policy radio persists across a reload → remove → clear-all through the AlertDialog leaving only `chat:migrated-from-tab-sessions:v1` behind and the Empty state back. Zero page/console errors beyond the expected `ERR_CONNECTION_REFUSED` from `listModels()` against a localhost Ollama that isn't running. One layout fix came out of that: the permission badges inside a `Field` were being stretched by its `[&>*]:w-full`, so they carry `class="w-fit!"` (src/options/components/ProviderForm.svelte:381-383, src/options/components/McpServerForm.svelte:574-576).
