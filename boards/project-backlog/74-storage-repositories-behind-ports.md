---
column: review
agent: claude-opus
live: false
labels: [backend, infra]
priority: high
updatedAt: 2026-08-22T21:40:00.000Z
---
# Storage repositories behind ports

Pull every `chrome.storage` call site out of `src/lib` into adapters under
`src/infra/chrome-storage` implementing ports the domain owns, per
decisions/29-ddd-hexagonal-typescript-layout.md and the driven-port rules in
`.claude/skills/ddd-hexagonal/SKILL.md`. The offenders are `src/lib/session.ts`
(814 lines, ~24 `chrome.storage.local` sites, keys `chat:<id>` / `chat:index` /
`tabchat:<tabId>`), `src/lib/providers/registry.ts` (398, ~15 sites),
`src/lib/mcp/registry.ts` (395, ~14 sites and a near-identical twin of the
provider registry), `src/lib/settings.ts` (132, ~13 sites) and `src/lib/ollama.ts`
(746), which keeps its own out-of-registry config store under `ollama:baseUrl` and
`ollama:cap:<model>`. The sync/local credential split is a hard rule and must
survive untouched: secrets never enter sync, and header values count as secrets.
Pre-release means storage shapes are free to change — the legacy `session:*`
migration path is deleted rather than ported.

The design choices this card had to make are recorded in
decisions/32-storage-ports-and-error-vocabulary.md.

## Checklist

- [x] `ChatStore`, `ProviderRegistry`, `McpServerRegistry` and `SettingsStore` port interfaces declared in their owning domain contexts, each with a domain storage-error vocabulary (`Unavailable | NotFound | Conflict | Corrupt | Unexpected` carrying `cause`); adapters map quota errors, `chrome.runtime.lastError` and malformed JSON into it so no platform error escapes infra
- [x] `session.ts`'s ~24 storage sites collapse into one chat repository owning `chat:<id>`, `chat:index`, `tabchat:<tabId>`, the 400ms/2000ms debounce, the `MAX_RETAINED_CHATS` 400 eviction backstop and `withIndexLock`; the aggregate rules (message append, tool-call log, title) stay in `domain/chat`
- [x] `providers/registry.ts` and `mcp/registry.ts` CRUD extracted into two adapters over ONE shared keyed-record mechanic (list key in sync, per-id credential keys in local) — the near-duplicate is written once and both registries configure it
- [x] sync/local split preserved exactly: `providers:list`, `providers:default`, `mcp:servers:list`, `settings:approvalPolicy`, `settings:mcpApprovalPolicy` in sync; `providers:apiKey:<id>`, `providers:headers:<id>`, `mcp:auth:<id>`, `mcp:headers:<id>` in local — verified by reading back the sync namespace after a save and asserting no secret is present
- [x] `ollama:baseUrl` and `ollama:cap:<model>` leave `src/lib/ollama.ts` for a proper store — folded into the provider config space or its own repository behind the same port style; no module keeps a private storage side-door
- [x] legacy `session:*` / `session:index` migration code and the `chat:migrated-from-tab-sessions:v1` flag deleted, not ported (pre-release: no migrations); `session.ts` also stops re-exporting `SelectionResolution` from `providers/registry` — that type lives in `domain/providers`
- [x] `grep -rn "chrome.storage" src/` returns hits only under `src/infra/chrome-storage/` (plus any composition-root wiring), and `npm run guard:boundaries` proves no domain module reaches for it — enforced by a new storage-containment scan in `scripts/guard-boundaries.mjs`, with ONE named exception it also prints: `panel.svelte.ts`'s `debug:tab-sync-tracing` flag, which card 77 owns
- [x] npm run check, npm run build, npm run guard and npm run verify green

## Comments

- **claude-opus** (2026-08-22T21:40:00.000Z): Landed the whole card. **Ports** — `ChatStore` (src/domain/chat/store.ts:51-83) beside the aggregate it persists (src/domain/chat/session.ts:16-176, which now owns `ChatSession`/`ChatSummary`/`ToolCallLogEntry`, `MAX_RETAINED_CHATS`, `createChat`/`logToolCall`/`completeToolCall` and the new `chatPreview`/`summarizeChat`); `ProviderRegistry` plus `resolveProvider`/`resolveSelection`/`resolveDefaultSelection` as domain rules OVER it (src/domain/providers/registry.ts:66-141); `McpServerRegistry` with the config shapes and `validateServerHeaders` (src/domain/tools/servers.ts:102-169) — put in `tools` rather than its own context because `merge.ts`'s `ToolServerIdentity` is already the narrow view of exactly that type; `SettingsStore` (src/domain/settings/approval-policy.ts:61-71). **Error vocabulary** — one shared kernel, `src/domain/storage/error.ts:54-63`, rather than four copies of the same five-member union; it THROWS rather than returning a union because every one of these ports already rejected on failure and no caller handles it, so a result type would have changed control flow inside a "behaviour-preserving" refactor. Journalled as decisions/32-storage-ports-and-error-vocabulary.md, along with the other two judgement calls below.
- **claude-opus** (2026-08-22T21:41:00.000Z): **Adapters** — `src/infra/chrome-storage/area.ts:78-125` is now the only place `chrome.storage` is called and the only place a quota `DOMException` / `chrome.runtime.lastError` becomes a `StorageError` (classification at area.ts:33-48). `chat-store.ts:190-423` carries the 400/2000ms debounce, the `withIndexLock` queue (chat-store.ts:210-231, card 55's fix, unchanged), the eviction backstop and `toPlain`'s `$state`-proxy snapshot — all behaviour-identical. `settings-store.ts:29-64` keeps decision 20's two policies with zero shared code, deliberately. **The unification**: `keyed-record-store.ts:101-234` is the "ordered core list in sync + per-id credential parts in local" mechanic, and `provider-registry.ts:83-144` / `mcp-server-registry.ts:114-169` configure it. That removes ~470 duplicated lines AND makes the credential split structural — `partArea` and `listArea` are separate gateways, so a secret cannot reach sync by omission; what stays per-registry is only what genuinely differs (providers clearing `providers:default` on removal, provider-registry.ts:117-131; MCP defaulting `enabled`/`transport`, mcp-server-registry.ts:153-167).
- **claude-opus** (2026-08-22T21:42:00.000Z): **Ollama's side-door** split by KIND rather than by provider, per the card's "your judgement call". The base URL is configuration and went with the provider settings as `ProviderDefaultsStore`, keyed by `ProviderType` (src/domain/providers/config-store.ts:29-35); the capability answer is a derived cache and got its own small port, `ModelCapabilityCache` (config-store.ts:46-49). Keys are derived as `<type>:baseUrl` and `<type>:cap:<fingerprint>` (src/infra/chrome-storage/provider-config-store.ts:25-31), which is byte-identical to `ollama:baseUrl` / `ollama:cap:<digest>` for the only type that uses them, and gives the next provider type a namespace instead of a second side-door. `src/lib/ollama.ts` now takes both as options (ollama.ts:64-72 `resolveBaseUrl`, ollama.ts:370-411 `getCapabilities`) and touches no storage at all — deliberately, so card 75 can move it to `src/infra/ollama` without breaking `adapters-do-not-import-adapters`. They are injected at ONE site: `src/lib/providers/clients.ts:49-61`.
- **claude-opus** (2026-08-22T21:43:00.000Z): **Deleted** — `src/lib/session.ts` (814), `src/lib/settings.ts` (132), `src/lib/mcp/registry.ts` (395), and with them `migrateLegacySessionsOnce`/`runMigration`, the `session:*`/`session:index` keyspace and the `chat:migrated-from-tab-sessions:v1` flag (pre-release, no migrations). `src/lib/providers/registry.ts` was three things at once; the repository and the resolver left, and what remained — the provider-type service locator — is renamed `src/lib/providers/clients.ts:23-47` so its name matches its one remaining job (still card 79's to delete). `resolveSessionSelection` went too: it only wrapped `resolveSelection`, and `session.ts` re-exporting `SelectionResolution` from the registry — decisions/29's named inversion — is gone with it; selection.svelte.ts:57-73 now takes both from `src/domain/providers`. Net −2,079/+610 lines across 47 tracked files.
- **claude-opus** (2026-08-22T21:44:00.000Z): **Wiring — the shortcut, flagged for cards 77/78.** `createChromeStoragePorts()` (src/infra/chrome-storage/ports.ts:46-58) builds the six ports, and both composition roots call `initChromeStorage()` (src/sidepanel/main.ts:26-39, src/options/main.ts:25-39). But the ~20 components, 3 rune stores and 2 services that call these ports take no dependencies at all today, so they import the port instances from `src/infra/chrome-storage/wiring.ts` — the interim module-level bundle the card explicitly permits. Its header (wiring.ts:1-33) states why full injection was deferred (threading six ports through the UI IS cards 77/78, and `ui-does-not-import-infra` is parked for exactly them), why it is not a service locator (nothing is looked up by name or registered into it, so the import graph still shows every dependency), and precisely what deleting it takes. `src/lib/mcp/oauth.ts:603` reaches it too — I only moved its `updateServer` write onto the port and left the inversion itself for card 76, with a note at oauth.ts:56-62.
- **claude-opus** (2026-08-22T21:45:00.000Z): **Enforcement.** No deferred `.dependency-cruiser.cjs` rule became satisfiable: `no-src-lib` needs cards 75-79, and `ui-does-not-import-infra` / `only-roots-construct-infra` are precisely what the wiring shortcut violates — I updated all three comments to say what card 74 changed rather than softening them (.dependency-cruiser.cjs:181-215). One incidental win worth recording: the `providers/ollama.ts ⇄ providers/registry.ts` cycle card 73 attributed to card 79 is GONE, because `ProviderConfig` moved to the domain and the adapter no longer imports its constructor; `no-circular` still can't be promoted to `error` because `ToolArgValue.svelte`'s legitimate self-import remains (note rewritten at .dependency-cruiser.cjs:135-157). What I DID add is the rule this card actually earns: a storage-containment scan in `scripts/guard-boundaries.mjs:124-197` — `chrome.storage` may be called only from `src/infra/chrome-storage/`, with named per-card exceptions (one: panel.svelte.ts's debug flag, card 77's). Verified it fails on a planted violation before trusting it.
- **claude-opus** (2026-08-22T21:46:00.000Z): **Gates.** `npm run check` 838 files, 0 errors. `npm run build` clean. `npm run guard` clean (1 pre-existing recursive-component warning). `npm run verify` 9/9 required + screenshots PASS — that last one matters most here, since verify/checks/screenshots.mjs seeds `chat:<id>`/`chat:index`/`tabchat:1`/`providers:list`/`providers:default` directly and drives the real UI off them, so every key kept working unchanged. Beyond the gates I bundled the adapters with esbuild and ran them on plain Node against an in-memory `chrome.storage` fake (scratchpad, not committed): 33 assertions covering the checklist's "read back the sync namespace and assert no secret is present" for both registries (add AND update paths), credential clearing, default-selection cleanup on removal, the recycled-tab-id pointer guard, the debounce/flush boundary, every key name, and the quota→`Unavailable` mapping with `cause` retained. All passed. That run also caught a real defect: `createStorageAreaGateway` captured `chrome.storage.local` at construction time, which made merely importing `wiring.ts` depend on the extension APIs existing — now resolved per call (area.ts:79-86), which is also what makes card 83's infra tests possible.
