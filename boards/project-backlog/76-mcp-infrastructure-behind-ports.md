---
column: review
agent: claude-opus
live: false
labels: [backend, infra]
priority: high
updatedAt: 2026-08-22T22:55:00.000Z
---
# MCP infrastructure behind ports

Move the HTTP MCP stack into `src/infra/mcp` and dissolve its two layering
inversions, per decisions/29-ddd-hexagonal-typescript-layout.md and the
adapter/port rules in `.claude/skills/ddd-hexagonal/SKILL.md`.
`src/lib/mcp/client.ts` is the largest file in the repo (1294 lines): protocol
2025-06-18 over Streamable HTTP plus the legacy SSE transport, a never-throws
`McpResult` contract, timeouts CONNECT 10s / LIST 10s / DISCOVERY 12s / CALL 30s,
4 fetch sites — and it imports `package.json` for its version string.
`src/lib/mcp/oauth.ts` (602 lines, 3 fetch + 6 `chrome.identity` sites) implements
OAuth 2.1 + PKCE discovery (RFC 9728/8414), dynamic registration (RFC 7591) and
`getValidAuth` refresh, but imports `updateServer` from `./registry` — the
transport stack writing the config store. Both concerns become adapters behind
ports that `domain/tools` owns and `src/sidepanel/services/mcpTools.ts` (239)
consumes.

## Checklist

- [x] `src/lib/mcp/client.ts` → `src/infra/mcp`: both transports, the never-throws `McpResult` contract and the CONNECT/LIST/DISCOVERY/CALL timeout set move unchanged; `testServerConnection`, `listServerTools`, `callServerTool`, `discoverAllServerTools` keep their behaviour
- [x] `src/lib/mcp/oauth.ts` → `src/infra/mcp`: discovery, dynamic registration, `runAuthorizationFlow` and `getValidAuth` keep behaviour, and all 6 `chrome.identity` sites stay inside infra
- [x] `oauth.ts` no longer imports `updateServer` from the registry — refreshed tokens persist through an injected `McpAuthTokenStore` port declared in `domain/tools` and implemented by the chrome-storage adapter (over the registry's existing `auth` credential part, NOT a second `mcp:auth:<id>` key — see the journal)
- [x] `client.ts` stops importing `package.json` — the client version is injected at construction, sourced from a build-time `define` in vite.config.ts (or read from the generated manifest)
- [x] `domain/tools` declares the `McpToolGateway` / `McpDiscovery` port interfaces (list, call, discover-all, test-connection); `src/sidepanel/services/mcpTools.ts` and the options test-connection helpers depend on those interfaces, never on the client module
- [x] the `McpError` union and `describeMcpError` (already in `domain/tools`) are the only error shape crossing out of `src/infra/mcp` — no HTTP status or fetch rejection leaks
- [x] `npm run guard:boundaries` shows no `infra/mcp` → `infra/chrome-storage` edge: the token store arrives by injection from the composition root
- [x] npm run check, npm run build, npm run guard and npm run verify green

## Comments

- **claude-opus** (2026-08-22T22:46:00.000Z): **The move.** `src/lib/mcp/client.ts` (1,294) and `src/lib/mcp/oauth.ts` (602) are gone; `src/infra/mcp/` is 16 files, none over 300 lines, split by CONCERN rather than by which of the two originals a line came from: `protocol.ts:10-25` (version negotiation) · `timeouts.ts:33-45` (the whole budget ladder, all five constants) · `budget.ts:25-68` (the per-call `AbortController`) · `json-rpc.ts:77-110` (every HTTP-status/JSON-RPC-error → `McpError` mapping) · `sse.ts:41-148` (framing both transports share) · `session.ts:15-20,22,39-68` (`McpWireSession` + `initialize` validation) · `headers.ts:33-81` (decisions/15's safety net + both auth modes) · `streamable-http.ts:104-193` · `legacy-sse.ts:61-137,171-288` · `connect.ts:26-52` (transport selection) · `results.ts:99-123` (pagination + content normalization) · `gateway.ts:65-175` · `oauth-http.ts:31-94` · `oauth-metadata.ts:97-226` (RFC 9728/8414/7591) · `oauth.ts:63-72,146-226,233-299` (PKCE + the flow + refresh). `README.md:7-24` is the file-by-file map. Behaviour is unchanged throughout — same four operations, same never-throws contract, same 10/10/12/30s budgets, same defensive parsing.
- **claude-opus** (2026-08-22T22:47:00.000Z): **The inversion, dissolved.** `McpAuthTokenStore` (src/domain/tools/servers.ts:171-195) is one write-only method, `saveAuth(serverId, auth)`, and `createMcpOAuthClient({ tokenStore })` (src/infra/mcp/oauth.ts:233-299) takes it at construction. src/infra/mcp now imports NOTHING from src/infra/chrome-storage — `adapters-do-not-import-adapters` would have failed on the first depcruise run otherwise, which is exactly the point. **Deviation from the checklist, deliberately:** the card proposed a dedicated `mcp:auth:<id>` key, but card 74 had already made `auth` a credential PART of the server record (mcp-server-registry.ts, local area, decisions/32's keyed-record mechanic). A separate key would give one credential two homes — `McpServerConfig.auth` is what the form renders, what `McpServerRow` badges as "reconnect needed" and what the transport reads on the next connect, and all three would still come from the registry and silently disagree with the refreshed copy. So the port NARROWS the registry rather than shadowing it: `createChromeStorageMcpAuthTokenStore(registry)` (src/infra/chrome-storage/mcp-auth-token-store.ts:33-41), reasoned out in that file's header. What the port still buys is the real prize — the transport can no longer do anything to a stored server except replace its token, where `updateServer` could rename it, disable it or rewrite its URL.
- **claude-opus** (2026-08-22T22:48:00.000Z): **One latent bug fixed in passing.** The old `getValidAuth` did `await mcpServerRegistry.updateServer(...)` bare (oauth.ts:603). Since card 74 that rejects with `StorageError`, so a quota failure would have thrown OUT of a function whose module doc promises "returns an `McpResult`, never throws" — turning a successful token refresh into an unhandled rejection inside `connect()`. Persistence is now explicitly best-effort (`.catch(() => undefined)`, src/infra/mcp/oauth.ts:289), which is also already the documented behaviour for the `"draft"` server id the options form tests unsaved configs under.
- **claude-opus** (2026-08-22T22:49:00.000Z): **The version string** comes from a Vite `define` (vite.config.ts:11-21) filled from the same package.json manifest.config.ts already reads, typed as ambient consts in src/build-globals.d.ts:16-17 and consumed as `DEFAULT_CLIENT_INFO` (src/infra/mcp/protocol.ts:49-52). Chose the define over an injected-only option because the alternative duplicates `{name, version}` into both surface wiring files for a value that is genuinely a build fact, not a runtime choice — but `createMcpToolGateway` still accepts a `clientInfo` override (src/infra/mcp/gateway.ts:57-65), so a test can pin the handshake identity without a build. Verified in the output: `dist/assets/chunk-BZIwGo8m.js` carries the literal `{name:"openchat-webmcp",version:"0.1.0"}` and package.json is no longer in the bundle.
- **claude-opus** (2026-08-22T22:50:00.000Z): **Port surface.** src/domain/tools/gateway.ts:46-91 declares `McpToolGateway` (test/list/call/discover + `McpCallOptions`) and :142-165 `McpOAuthClient extends McpTokenResolver`. Kept the gateway as ONE interface rather than the card's `McpToolGateway`/`McpDiscovery` split: there is one implementation and every consumer would have ended up holding both halves, so splitting would have bought two names for one object. The split that DOES earn its keep is `McpTokenResolver` (gateway.ts:123-135) — the gateway depends on token refresh alone and never on the interactive flow, which is what lets the side panel wire a gateway without touching sign-in. Both ports are never-throws (`McpResult`), deliberately unlike decisions/32's throwing storage ports: an MCP failure is an ordinary outcome of talking to a third party, and every caller already branches on it. **Consumers** all now name only the port: mcpTools.ts:40-67 (gateway alias + `../../lib/permissions` directly, the shim deleted), mcpTestConnection.ts:22-28,55, McpServerForm.svelte:29-34,407-428 (its inline OAuth orchestration LOGIC untouched — card 78's). Instances come from two new interim wiring files, src/sidepanel/lib/mcpClients.ts:29-35 and src/options/lib/mcpClients.ts:15-21, two lines each on card 74/75's established pattern, headers naming what deletes them.
- **claude-opus** (2026-08-22T22:51:00.000Z): **Enforcement — two rules turned on, one still parked.** (1) NEW enforced depcruise rule `infra-does-not-import-src-lib` (.dependency-cruiser.cjs:125-151): the scoped half of `no-src-lib`, verified to fail on a planted `src/infra/mcp/headers.ts → src/lib/utils.ts` import. (2) NEW `chrome.identity` containment scan in scripts/guard-boundaries.mjs:220-243 — the direct analogue of card 74's storage containment, and a rule this card earns since `chrome.identity` was in src/lib before it. Home is `src/infra/mcp/`, one named exception (McpServerForm.svelte's `getRedirectURL()` for the copy-the-redirect-URI field, card 78's); verified it fails on a planted violation before trusting it. Factored the two scans onto one `containmentScan()` helper (guard-boundaries.mjs:136-193) rather than copy-pasting 40 lines. **`no-src-lib` stays parked, and the reasoning is now written into the config** (.dependency-cruiser.cjs:210-228): permissions.ts still calls `chrome.permissions` and is infrastructure (card 78's), and while everything else left in src/lib (markdown.ts, icons.ts, providerIcon.ts, dark-mode.ts, utils.ts, components/) is UI-layer or pure and no longer VIOLATES the layering, "src/lib" names no layer and both surfaces import it — enabling the rule with a carve-out for those files would encode "src/lib is fine actually", the opposite of decisions/29's conclusion. `ui-does-not-import-infra` also stays parked; its comment now lists all five interim wiring files as the complete set it will report on the day it is uncommented.
- **claude-opus** (2026-08-22T22:52:00.000Z): **Card 75's stale comments, swept.** src/lib/protocol.ts had NO importers left (only prose referenced it), so it is deleted outright rather than re-pointed — and with it the four comments that asserted it still re-exports something (src/domain/tools/tool.ts:4-11, src/infra/chrome-runtime/protocol.ts:14-18, src/domain/providers/provider.ts:21-27). src/domain/tools/types.ts:1-16,31,35,171 now point at src/infra/mcp and src/infra/openai instead of `client.ts`/`src/lib/providers/**`; the four stale refs inside client.ts went with the move (the `src/lib/protocol.ts`/`src/lib/providers/**` "owned by concurrent work" paragraph is simply gone — it is no longer true of anything). src/lib/mcp/permissions.ts deleted, its one importer pointed at src/lib/permissions.ts. src/infra/chrome-runtime/README.md rewritten: its rows are now status-tagged, and it records WHY `chrome.identity` did NOT land there as it originally planned — the `state` parameter is generated, sent and re-validated across the one `launchWebAuthFlow` call, so splitting it out would put half an anti-CSRF check in each folder, and `adapters-do-not-import-adapters` would have forced a domain port whose entire content is "open this URL".
- **claude-opus** (2026-08-22T22:53:00.000Z): **Gates.** `npm run check` 856 files, 0 errors. `npm run build` clean. `npm run guard` clean (the one pre-existing `no-circular` warning for ToolArgValue.svelte's legitimate self-import). `npm run verify` 9/9 required + screenshots PASS, run twice — before and after the enforcement changes. Net −2,115/+428 across the 25 tracked files plus 19 new ones. **For cards 77/78:** the five interim wiring files are the whole remaining `ui-does-not-import-infra` debt, and each is a two-to-three-line factory call — deleting them is a matter of moving those lines into `main.ts` and threading the bundle down, not of rewriting anything. Card 78 additionally owns McpServerForm.svelte's `redirectUri()` (the last `chrome.identity` exception), src/lib/permissions.ts's move to src/infra/chrome-runtime, and the src/options/lib/permissions.ts re-export shim that goes with it. Card 79 (or whichever empties src/lib) widens `infra-does-not-import-src-lib` to `no-src-lib`.
