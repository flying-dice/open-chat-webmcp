---
column: review
agent: claude-sonnet
live: false
labels: [infra, backend]
priority: high
updatedAt: 2026-08-23T03:02:00.000Z
---
# Vitest toolchain and domain tests

Add the unit layer from decisions/30-vitest-test-pyramid.md — the repo has no test
framework at all today, only the Chrome-for-Testing `verify/` harness, which
deliberately bypasses the Svelte UI. Wiring is non-trivial because vite.config.ts
is a multi-entry MV3 build (`svelte()` + `crx({manifest})`) and svelte.config.js
is bare (`export default {}`, no vitePreprocess), so the test config must reuse
the Svelte plugin without letting the CRXJS manifest plugin run. Tests live beside
their modules as `src/domain/**/*.test.ts` and use no platform mocks whatsoever —
that is the payoff of the domain extraction. The richest target is the merge
algebra in `domain/tools` (301 lines, 12+ dependents): namespacing, slug
assignment, truncation and page/server combination.

## Checklist

- [x] `vitest` (+ `@vitest/coverage-v8`) installed; a vitest config reuses the Svelte plugin but excludes `crx()`, defaults to the `node` environment and picks up `src/**/*.test.ts`
- [x] `npm test` (single run) and a watch script wired into package.json; a coverage script reporting on `src/domain` and `src/infra`
- [x] `domain/tools` merge algebra covered hardest: `slugifyServerName` and `assignServerSlugs` collision handling, `namespacedToolName` with `NAMESPACE_SEPARATOR` "__", truncation at `MAX_TOOL_NAME_LENGTH` 64 with the 63/64/65 boundaries, `buildServerMergedTools`, `combineWithPageTools` precedence when a page tool and a server tool share a name, and `toSerializedTools` output shape
- [x] `domain/chat`: transcript grouping (including the duplicate-group-key case that once crashed the transcript), title derivation, turn-phase transitions, and the session aggregate's message and tool-call append rules — driven over fake ports
- [x] `domain/providers`: selection resolution, capability resolution (`resolveCapability`, `isSelectable`, `reasonForCapability`) and the `ProviderError` → `describeProviderError` mapping for every variant of the union
- [x] `domain/settings`: both policies (`"default" | "always-confirm" | "auto-run-all"` and the MCP triple `"always-confirm" | "trust-read-only" | "auto-run-all"`) and the auto-run decision each produces for a read-only vs mutating tool
- [x] zero platform mocks in domain tests: no `chrome`, `fetch`, DOM or Svelte import under `src/domain` — asserted by construction and by `npm run guard:boundaries`
- [x] npm run check, npm test, npm run build and npm run verify green

## Comments

- **claude-sonnet** (2026-08-23T02:49:00.000Z): **Toolchain.** Installed `vitest@4.1.11`, `@vitest/coverage-v8@4.1.11`, `jsdom@30.0.1`, `@testing-library/svelte@5.4.2`, `@testing-library/jest-dom@7.0.1`, `@testing-library/user-event@14.6.6` (current, Svelte-5-compatible). `vitest.config.ts:1` is its own file, not a vite.config.ts reuse — it only imports `@sveltejs/vite-plugin-svelte`'s `svelte()` and the `$lib` alias, never `crx()`. Two Vitest 4 `test.projects` entries (the replacement for the deprecated `vitest.workspace.ts`), both `extends: true`: `"domain"` (`environment: "node"`, `src/domain/**/*.test.ts` + `src/infra/**/*.test.ts`) and `"component"` (`environment: "jsdom"`, `src/{sidepanel,options,ui}/**/*.test.ts`, `vitest.setup.ts` importing `@testing-library/jest-dom/vitest`). Hit one real snag proving the jsdom project: vite-plugin-svelte picks the SSR (`mount()` unavailable) compile target unless the `browser` resolve condition is present — jsdom being `test.environment` only supplies globals, it doesn't change what Svelte compiles to. Fixed with `resolve.conditions: ["browser"]` scoped to the `"component"` project only (vitest.config.ts:60-68) so it can't affect the node project's package resolution. Smoke-tested with `src/ui/__tests__/Smoke.svelte` + `smoke.test.ts` — a throwaway fixture proving Svelte-5-runes-compile + jsdom + Testing Library + jest-dom matchers all work together; card 84 builds real component tests on the same project, nothing else to change.
- **claude-sonnet** (2026-08-23T02:49:30.000Z): **Guard/tsconfig integration.** `.dependency-cruiser.cjs:361-363`'s `exclude.path` now also matches `\.test\.ts$` — without it, `domain-has-no-dependencies` flagged every domain test's `import ... from "vitest"` as the domain taking an npm dependency, which isn't the coupling that rule polices. `guard-boundaries.mjs`'s domain-purity/chrome-containment scans are NOT test-excluded — they deliberately DO scan `*.test.ts` under `src/domain`, since "zero platform mocks, enforced by guard:boundaries" is card 82's own checklist item; caught one real false-positive (a `TypeError("chrome.runtime.lastError: ...")` message string in `storage/error.test.ts`, matched by the blunt `chrome\.` regex despite not being a real call) — reworded rather than touching the guard, see comment there. `tsconfig.node.json:15` now includes `vitest.config.ts`/`vitest.setup.ts` so `npm run check`'s second `tsc` pass covers them. `tsconfig.app.json:7`'s `types` array gained `"@testing-library/jest-dom/vitest"` — needed because `vitest.setup.ts` (which does the RUNTIME matcher registration via `test.setupFiles`) lives outside `src/`, so svelte-check's own program never picks up jest-dom's `expect` augmentation unless it's named in `types` directly. `package.json` scripts: `test` (`vitest run`), `test:watch` (`vitest`), `test:coverage` (`vitest run --coverage`).
- **claude-sonnet** (2026-08-23T02:49:45.000Z): **domain/tools** — `src/domain/tools/merge.test.ts`, 44 tests, written directly (already had the module read in depth). Covers `slugifyServerName` (collapse/trim/empty-fallback), `assignServerSlugs` (bare-slug-first, `-2`/`-3` suffixing in input order, order-independence of unrelated slugs), `namespacedToolName`'s exact 63/64/65-char boundary (slug shrinks first, floors at 1 char, only then truncates the tool name itself — asserted with precise expected strings, not just lengths), `buildServerMergedTools` (namespacing, error-status contributing nothing, slug stability across ok/error mix, forced `untrustedContentHint`, `readOnlyHint` normalisation table, original `mcpAnnotations` preserved untouched, same-server tool-name collision disambiguation, call binding to the tool's ORIGINAL name not the namespaced one), `combineWithPageTools` (page-wins-its-name precedence, sequential `-2`/`-3` disambiguation, same-object-identity return when no rename is needed, annotation normalisation tables for both hints, call binding), and `toSerializedTools` (four-field projection, round-trip through a mixed page+server list, empty input). **Found a real domain bug, not fixed (non-trivial):** `disambiguateName` (merge.ts:145-154) silently fails to disambiguate two server tools whose namespaced names are BOTH already truncated to the full 64-char ceiling by `namespacedToolName`'s last-resort branch — its own `${name}-${suffix}` retry is longer than the limit, and `candidate.slice(0, MAX_TOOL_NAME_LENGTH)` throws away exactly the appended suffix digits, reproducing the original colliding name verbatim. The second tool becomes unreachable (only the first survives `tools.find` lookup by name in turn.ts). Documented as a permanent regression test — `merge.test.ts`'s "known bug: disambiguation silently fails when the colliding name is already at the 64-char ceiling" — rather than silently worked around. A real fix needs `disambiguateName` to reserve room for its own suffix up front, the same way `namespacedToolName` reserves room for the separator; left for a follow-up card since it changes truncation behavior at the boundary and deserves its own review.
- **claude-sonnet** (2026-08-23T02:50:00.000Z): **domain/providers** (written by a parallel subagent, reviewed) — 53 tests across `provider.test.ts`, `capability.test.ts`, `registry.test.ts`, `client-factory.test.ts`, `presets.test.ts`. `describeProviderError` tested per-variant of the `ProviderError` union via `test.each`; `resolveCapability`/`resolveCapabilities`/`isSelectable`/`reasonForCapability` covered including the `undefined` capability state; selection resolution (ok/dangling/none/default) driven over a hand-rolled in-memory `FakeProviderRegistry`; `client-factory` checked exhaustively over every `ProviderType`. `config-store.ts` correctly skipped — it's two port interfaces with no logic to assert. No bugs found; one design note (not a bug): `describeProviderError` returns the raw `message` verbatim for both `"unreachable-or-cors"` and `"not-supported"`, so the two kinds are only distinguishable by whatever the caller put in `message`, not by the description function itself.
- **claude-sonnet** (2026-08-23T02:50:15.000Z): **domain/settings + domain/permissions + domain/storage** (written by a parallel subagent, reviewed) — 107 tests: `settings/approval-policy.test.ts` (60) full `test.each` matrix of both policies × read-only/mutating, tested independently per decisions/20's "never share logic" rule; `createApprovalPolicyGate` proven to re-read a fake settings store on every call (mid-session policy change picked up next call, not cached) and that page-policy changes never touch the MCP call counter; confirmed `untrustedContentHint` is orthogonal to auto-run by construction (neither pure rule even accepts an annotations object). `permissions/host-permissions.test.ts` (23): `originPatternForUrl` over plain https, port, path/query/fragment stripping, IPv4/IPv6 literals, localhost, seven non-http(s) schemes → `undefined`, and malformed input confirmed never to throw. `storage/error.test.ts` (24): every `StorageErrorKind` through the one `StorageError` constructor, cause retention by reference (object/Error/empty-string/null), no-cause leaving `cause` undefined. Fixed one guard false-positive myself after review: a `TypeError("chrome.runtime.lastError: ...")` fixture string tripped `guard:boundaries`' domain-purity scan (reworded to "storage adapter: quota exceeded" — see toolchain comment above). No domain bugs found.
- **claude-sonnet** (2026-08-23T02:50:30.000Z): Repo-wide total so far: 251 tests, 13 files, `npx vitest run` green. `npm run check` still has 9 pre-existing-shape errors confined to the in-progress `domain/chat` suite (a parallel subagent is still writing it) — not yet gated, see next entry once that lands.
- **claude-sonnet** (2026-08-23T03:00:00.000Z): **domain/chat** (written by a parallel subagent, reviewed) — 115 tests across `turn.test.ts` (32), `service.test.ts` (27), `message.test.ts` (11), `session.test.ts` (15), `title.test.ts` (19), `transcript-groups.test.ts` (20), `ports.test.ts` (1). Recreated all 15 behaviours card 77's journal proved ad-hoc with an esbuild bundle on plain Node: plain streaming reply; read-only auto-run + call-log mirror + second round; denial terminal and model-readable; untrusted-content fencing applied to the model's copy only; `MAX_ITERATIONS` (8) tripping with its note; a hanging tool bounded by the injected `toolCallTimeoutMs` rung; a hallucinated tool name; a throwing `ModelGateway` becoming a retryable note; a mid-turn tab switch not redirecting a running turn's writes (`target` captured once); re-attaching to the same in-memory session object; navigation policy (retire/ignore); selection carry-over + rename normalisation + `touch:false` + title derivation; transcript grouping folding carriers, including the duplicate-group-key crash case; stop mid-turn keeping the partial reply with no note; stop during an approval wait denying via `raceApproval` rather than hanging. Coverage on the exercised files: `turn.ts` 98.2%, `service.ts` 98.3%, `message.ts`/`session.ts`/`title.ts`/`transcript-groups.ts`/`turn-phase.ts`/`store.ts` all 100%. **Domain observation (not a bug, now pinned by a test):** `turn.ts:341-344` — a model's terminal `"done"` stream event can itself carry `message.toolCalls`, which OVERWRITES rather than merges with anything already accumulated from separate `"tool-calls"` events; both are legitimate per-provider wire shapes, and `turn.test.ts` has a dedicated case for it. After landing: fixed 4 pre-existing type errors in `message.test.ts` (a `ChatMessage -> Record<string, unknown>` cast needed `as unknown as` first, since `ChatMessage` and an index-signature type don't structurally overlap) and 4 in `turn.test.ts` (`vi.fn(async () => ({ok: true, result: ...}))` widens `ok` to `boolean` outside the contextual typing a plain arrow function gets when assigned directly to `MergedToolExecutor` — added `as const` at each of the four call sites).
- **claude-sonnet** (2026-08-23T03:02:00.000Z): **Final gates, all green.** `npm test` (`vitest run`): 320 tests, 17 files, 0 failures. `npm run check`: 959 files, 0 errors, 0 warnings. `npm run guard` (boundaries + clean-code): exit 0 — `guard:boundaries`'s domain-purity scan is genuinely exercised now (46 files under src/domain scanned, including every new `*.test.ts`, zero platform-global hits); clean-code guard shows only pre-existing accepted debt (nothing above 0.5, nothing new introduced by this card's files). `npm run build`: clean Vite build, unchanged bundle shape. `npm run verify`: 9/9 required checks + the best-effort screenshot check, all PASS (run before the chat suite landed, but no production `src/` file changed after that point — only test files and the toolchain config — so it stands). Repo-wide test breakdown: tools/merge 44 · chat 115 (7 files) · providers 53 (5 files) · settings 60 + permissions 23 + storage 24 = 107 · smoke (jsdom proof) 1. Two things intentionally left for a human/follow-up card rather than fixed here: (1) the `disambiguateName` 64-char-ceiling collision bug in `src/domain/tools/merge.ts:145-154` (see the domain/tools entry above) — real, reproducible, low-probability (needs a tool name ≥61 chars after slug-floor truncation), not trivial to fix safely without its own review; (2) `src/domain/providers/provider.ts`'s `describeProviderError` returning identical raw `message` text for `"unreachable-or-cors"` and `"not-supported"` — a design note, not a defect. `storage-chat.json` in the repo root is a pre-existing untracked file from before this card started (present in the original git status); left untouched and not staged.
