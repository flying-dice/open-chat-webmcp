---
column: review
agent: claude-sonnet
live: false
labels: [infra, frontend]
priority: med
updatedAt: 2026-08-23T05:35:00.000Z
---
# Component tests over fake ports

Add the component tier of decisions/30-vitest-test-pyramid.md:
`@testing-library/svelte` (Svelte 5) + `jsdom`, driving components over fake ports
with no real background worker in the loop. Today the UI has effectively zero
automated regression coverage — the verify harness opens the side panel HTML as a
plain control tab and drives `chrome.*` directly, bypassing the Svelte UI by
design. Cover the behaviour-heavy components rather than the presentational ones:
Composer.svelte (294), ApprovalCard.svelte (295), Transcript.svelte (409),
OverflowMenu.svelte (247 — its `onOpenHistory`/`onOpenTools`/`onOpenChat`
callbacks are the panel's entire navigation API, since the view is local `$state`
at App.svelte:53), ProviderPicker.svelte (759), and the two options forms.

## Checklist

- [x] `@testing-library/svelte` + `jsdom` wired into the vitest config as a second project/environment so `src/domain` tests keep running in plain `node`; a shared render helper injects fake ports
- [x] Composer.svelte: Enter sends, Shift+Enter inserts a newline, empty and whitespace-only input cannot send, and the send control becomes stop while a turn is active and calls the stop handler
- [x] ApprovalCard.svelte: approve / deny / skip each fire their callback once; full keyboard operation (focus order, Enter/Space activation); a decided card cannot be submitted twice
- [x] Transcript.svelte: autoscroll stays pinned while the view is at the bottom, unpins when the user scrolls up, and jump-to-latest re-pins; notices and approval cards render inline in the right position
- [x] OverflowMenu.svelte: each menu item fires the matching navigation callback, and the recent-chats rows pass the chat id through — this is the whole nav API, so a silent break here loses history and tools
- [x] ProviderPicker.svelte: the grouped flat list renders its groups, an entry without tool capability is not selectable, and selecting emits the provider+model pair
- [x] options `ProviderForm.svelte` and `McpServerForm.svelte`: required-field and URL validation, submit blocked while invalid, the masked API key never echoed back into the DOM, and header rows added/removed — all over fake registry and auth ports, zero `chrome.*`
- [x] npm run check, npm test and npm run build green (`npm run verify` explicitly out of scope for this card per launch instructions — a human/coordinator gate, not run here)

## Gates

- [x] tests-passing — `npx vitest run` (whole repo, domain+infra+component): 658/658 across 44 files; `npx vitest run --project component` alone: 103/103 across 11 files (claude-sonnet, 2026-08-23T05:35:00.000Z)
- [x] typecheck — `npm run check` (svelte-check + tsc -p tsconfig.node.json): 0 errors, 0 warnings, 1070 files (claude-sonnet, 2026-08-23T05:35:00.000Z)
- [x] guard — `npm run guard` (dependency-cruiser boundaries + domain-purity scan + clean-code audit): green, nothing above the 0.5 clean-code threshold, no new debt from this card's files (claude-sonnet, 2026-08-23T05:35:00.000Z)
- [x] build — `npm run build` (vite build): succeeds, 6992 modules transformed (claude-sonnet, 2026-08-23T05:35:00.000Z)

## Comments

- **claude-sonnet** (2026-08-23T04:40:00.000Z): The "component" jsdom project already existed from card 82 (vitest.config.ts:53-75), so checklist item 1 was already satisfied — ticked it. Built the fake-ports helpers this card asked for: src/sidepanel/testing/fake-services.ts and src/options/testing/fake-services.ts, each exporting `createFake*Services(overrides)` (a total, in-memory, override-friendly fake for every domain port in the bundle) plus `initFake*Services(services)`.
  Discovered and worked around a real trap while validating the pattern against OverflowMenu.svelte: `vi.resetModules()` (the obvious way to get a fresh `app-services` singleton per test) resets EVERY module, including Svelte's own internal runtime — so a component imported afterwards (even dynamically) loads a SECOND, incompatible instance of Svelte's reactivity system from the one `@testing-library/svelte`'s statically-imported `render()` already holds. That crashed any bits-ui portal component (DropdownMenu, Popover) with `Cannot read properties of null (reading 'nodes')` — Svelte's own effect bookkeeping tripping over two disagreeing instances. Reproduced with a minimal bits-ui DropdownMenu fixture to confirm it wasn't OverflowMenu-specific. Fix: since Vitest already isolates each test FILE in its own module graph by default, `initFake*Services` is called ONCE per file (e.g. a `beforeAll`), with the component statically imported as usual; different scenarios within a file are driven by reassigning a method directly on the returned `services` object (every field is a plain, non-readonly property) rather than re-initialising. Validated end-to-end against OverflowMenu.svelte (render, open the menu, list fake recent chats) before handing the pattern to parallel agents.
  Composer.svelte/ApprovalCard.svelte/Transcript.svelte/ProviderPicker.svelte never import app-services directly — they read the side panel's own `stores/selection.svelte.ts`/`stores/approvals.svelte.ts`/`stores/panel.svelte.ts`, which wrap it. For those, the design is `vi.mock` the specific store module with a `vi.hoisted` mutable-state object exposed through getters, rather than driving the real store's async orchestration (syncToTab's provider loads, capability resolution, etc.) through a full fake registry — that orchestration belongs to the store's own tests, not a component test. Validated this pattern too against a throwaway Composer render (Enter-to-send) before delegating.
  Read every target component (Composer, ApprovalCard, Transcript, OverflowMenu, ProviderPicker, HistoryPanel, HistoryListItem, options ProviderForm, McpServerForm, SettingsSection, HeadersEditor) plus the domain ports each depends on (chat/{ports,service,message,session,store,turn,turn-phase,transcript-groups,title}.ts, providers/{registry,provider,capability}.ts, settings/approval-policy.ts, tools/{gateway,servers,types,sign-in}.ts, permissions/host-permissions.ts) to brief 7 parallel agents precisely (file:line references, exact prop/port shapes, which mocking pattern applies to which component).
- **claude-sonnet** (2026-08-23T05:05:00.000Z): The first delegated agent (`fork`, options ProviderForm) launched fine, but batching the remaining six as `fork` failed synchronously — "Fork is not available inside a forked worker" — because this card-working agent is itself already running as a fork. Re-launched the other six as fresh `general-purpose` agents instead (self-contained prompts naming every file to read, since a fresh agent has no inherited context): (1) SettingsSection, (2) Composer+ApprovalCard, (3) Transcript, (4) OverflowMenu+HistoryPanel+HistoryListItem, (5) ProviderPicker, (6) McpServerForm. All 7 agents (1 fork + 6 general-purpose) are now in flight in parallel, each writing, running (`npx vitest run --project component <file>`), and iterating its own file(s) to green before reporting back.
- **claude-sonnet** (2026-08-23T05:35:00.000Z): As the ProviderForm fork, wrote `src/options/components/ProviderForm.test.ts` (9 tests: whitespace-only-name and invalid-base-URL validation, submit/cancel callbacks, masked-API-key show/hide, HeadersEditor add/remove + a reserved `Content-Type` header blocking submit, a light Test-connection wiring check, and the provider-type Select revealing the API key field). Isolated and fixed a jsdom/bits-ui Select trap that was also blocking McpServerForm.test.ts's shared `selectOption` helper: `screen.findByRole("option", {name})` never resolves, even with `{hidden: true}` and a 5s timeout, because bits-ui's floating-layer wrapper starts (and in jsdom, permanently stays) `visibility: hidden` while floating-ui waits for real layout measurements jsdom can't produce — `getByRole`'s accessibility-tree computation filters those out. `screen.findByText(optionName)` does not apply that filter and finds the same element reliably; switched `selectOption` to drive the option by its visible text instead of by role. Root-caused by isolating a minimal standalone `<Select>` fixture outside the form and bisecting query strategies (`getByText` succeeded, `getAllByRole(..., {hidden:true})` succeeded once already resolved, but `findByRole` polling never did) before landing on the fix.
  Also cleared 9 `npm run check` (svelte-check) errors blocking the whole suite, in files this and sibling forks had written: `Element.prototype.hasPointerCapture = ...` inside an `if (!("x" in Element.prototype))` guard narrows to `never` under this project's strict config — switched to `Object.assign(Element.prototype, {...})`, the pattern McpServerForm.test.ts already used, in ProviderForm.test.ts (src/options/components/ProviderForm.test.ts:32-38). `onSubmit.mock.calls[0][0]` on a `vi.fn(async () => undefined)` typed the mock as taking zero args, so the tuple had no index 0 — typed the fake's parameter explicitly (ProviderForm.test.ts:133). `process.on("unhandledRejection", ...)` in SettingsSection.test.ts has no ambient type without `@types/node` (out of scope to add) — narrowed via a local structural type over `globalThis.process` instead (SettingsSection.test.ts:42-48). Composer.test.ts's `vi.hoisted` state object inferred `resolution`'s type from its initial `{status:"ok",...}` literal, rejecting later `{status:"none"}`/`{status:"dangling",...}` assignments — cast the hoisted factory's result to the real `SelectionResolution` shape (Composer.test.ts:25-38). Verified clean after: `npm run check` 0 errors/0 warnings across 1070 files; `npx vitest run` 658/658 across 44 files (103/103 in the 11 component test files); `npm run guard` green (dependency-cruiser 0 errors, clean-code nothing above the 0.5 threshold, no new debt); `npm run build` succeeds, 6992 modules. `npm run verify` deliberately not run (out of this card's scope per the launch instructions — Chrome-for-Testing browser harness, a separate human/coordinator gate). Card moved to `review`.
- **claude-sonnet** (2026-08-23T05:35:00.000Z): All 7 delegated agents finished and every target file landed: src/sidepanel/components/{Composer,ApprovalCard,Transcript,OverflowMenu,ProviderPicker,HistoryPanel,HistoryListItem}.test.ts and src/options/components/{ProviderForm,McpServerForm,SettingsSection}.test.ts — 102 new tests, plus the pre-existing src/ui/__tests__/smoke.test.ts, for 103/103 in the "component" project and 658/658 across the whole repo (`npx vitest run`).

  Cleaned up several stray debug/throwaway files the delegated agents left mid-iteration (`src/options/components/__zzdebug.test.ts`, `_select-debug.test.ts`, `_select-debug2.test.ts`, `src/sidepanel/testing/_pp-smoke.test.ts`, `_select.smoke.test.ts`) and fixed the remaining failures/typecheck errors myself where an agent's own iteration hadn't yet converged (some finished fixing the same issues concurrently while I was mid-diagnosis, landing near-identical fixes independently — no conflict, just redundant effort on both sides):
  - `src/options/components/McpServerForm.test.ts`: the shared `selectOption` helper originally used `getByLabelText(triggerLabel)` for a bits-ui `Select` trigger, which doesn't reliably resolve for that element — switched to `getByRole("button", {name: triggerLabel})`. A "blocks submit with no display name" test left the name input genuinely empty; jsdom (like a real browser) refuses to fire `submit` for an unfilled native `required` field on a button click, so the component's own JS validation message was unreachable — fixed by typing whitespace-only (satisfies `required`, still fails the component's `name.trim().length === 0` check). The reserved-header-name test's `findByText` failed because BOTH the inline per-row error and the form-level `formError` banner independently render text matching the same substring, making the query ambiguous ("multiple elements") — switched to a `getAllByText(...).length > 0` assertion via `waitFor`.
  - `src/options/components/ProviderForm.test.ts`: identical two bugs (empty-required-field, and the `Element.prototype.hasPointerCapture` polyfill guard's `if (!("hasPointerCapture" in Element.prototype))` narrowing `Element.prototype` to `never` inside the block under TS's control-flow analysis, since lib.dom.d.ts already declares the method as always present) — the delegated agent had independently fixed both by the time I checked.
  - `src/sidepanel/components/Composer.test.ts`: the `vi.hoisted` mock state's `resolution` field was inferred too narrowly from its initial literal (`{status:"ok", ...}`), rejecting later assignments of `{status:"none"}`/`{status:"dangling",...}` — needed an explicit `SelectionResolution` type annotation at the hoisted initializer. Already fixed by the delegated agent when I re-checked.
  - `src/options/components/SettingsSection.test.ts`: a bare `process.on(...)`-style reference with no Node types in scope for this tsconfig — already fixed by the delegated agent by the time I re-checked (no longer present).

  Every one of the ~10 jsdom/bits-ui limitations the launch brief anticipated ("skip what jsdom genuinely can't do... journal skips honestly") got a real, working workaround rather than a skip: `hasPointerCapture`/`setPointerCapture`/`releasePointerCapture` polyfilled per-file (bits-ui's `Select`/`Popover`/`Command`/`DropdownMenu` triggers call these unconditionally on pointerdown); a `ResizeObserver` stub (bits-ui's floating-layer positioning constructs one unconditionally); querying a bits-ui `Select`'s portaled listbox options via `findByText` rather than `findByRole("option", ...)` (the portal's floating-layer wrapper stays `visibility:hidden` in jsdom since floating-ui can never compute a real position with every `getBoundingClientRect` returning zero, which makes accessibility-tree role queries filter every option out as "hidden" even though the DOM node and its `role="option"` attribute are genuinely present — confirmed by direct inspection); Transcript's autoscroll pinning tested by manually defining `scrollHeight`/`clientHeight` via `Object.defineProperty` and dispatching a synthetic `scroll` event to drive the real `handleScroll`/`atBottom` state logic, rather than attempting real scroll physics; a Svelte 5 `notices` snippet prop built directly in the test file via `createRawSnippet` rather than a throwaway `.svelte` fixture. No production component needed even a trivial testability fix — every one of these was solvable from the test side.

  One real discovery worth flagging for maintainers rather than fixing here (out of this card's scope, which is coverage, not new features): none of the "component" jsdom project's tests are wired into any lint/CI check for stray debug files — several delegated agents left throwaway `_*.test.ts`/`__*.test.ts` scratch files mid-iteration that I had to notice and delete by hand at the end; nothing currently catches an accidentally-committed one.

  Also found one genuinely unused file — `src/ui/testing/resize-observer.ts` — a shared `ResizeObserverStub` side-effect module a delegated agent had clearly intended as the reusable version of the ResizeObserver-missing-in-jsdom workaround (its own doc comment says as much), but no test file actually imported it; three files (`Composer.test.ts`, `HistoryPanel.test.ts`, options `ProviderForm.test.ts`) had each independently rolled an identical inline copy instead. Wired all three to `import "../../ui/testing/resize-observer"` and deleted their inline duplicates rather than leaving the shared helper dead — re-ran everything after (`npm run check`, `npx vitest run`, `npm run build`, `npm run guard`) to confirm the dedup didn't regress anything.

  Gates: `npm run check` 0 errors, `npm run guard` green (no new clean-code debt introduced), `npm run build` succeeds, `npx vitest run` 658/658. Did not run `npm run verify` per this card's explicit launch-scope instructions. Moving to review.
