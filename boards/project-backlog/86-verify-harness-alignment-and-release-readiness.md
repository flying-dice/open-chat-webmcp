---
column: review
agent: claude-opus
live: false
labels: [infra, docs]
priority: med
updatedAt: 2026-08-22T08:25:00.000Z
---
# Verify-harness alignment and release readiness

Close the loop on decisions/30-vitest-test-pyramid.md — "its seeded-storage
helpers move onto the same typed fixtures the unit layer uses, ending the second
hand-written copy of the storage schema" — and land the documentation the
restructure of decisions/29-ddd-hexagonal-typescript-layout.md invalidated.
`verify/checks/screenshots.mjs` (458 lines) seeds `ChatSession`, `chat:index` and
provider records by hand through `addInitScript` and locates UI by accessible name
(`getByRole` "More options", menuitem names), so it encodes the storage schema a
second time outside `src/` and degrades silently to a best-effort SKIP whenever
roles or names move. This card retypes the fixtures, refits the selectors,
recaptures the matrix, refreshes README and `docs/`, and records one full release
gate at version 0.2.0.

## Checklist

- [x] `verify/checks/screenshots.mjs` seeds storage from the same typed fixtures the unit tests use (exported from `src/` or a shared fixtures module the harness can import) — the hand-written second copy of the storage schema is deleted, not merely synced
- [x] its accessible-name selectors are refitted to the current UI, and a selector that no longer matches FAILS loudly instead of degrading to a silent SKIP (screenshot capture itself may stay best-effort, selector resolution may not)
- [x] the screenshot matrix is recaptured and committed: light/dark × 320/400px plus the overflow menu and the model sheet (9 PNGs → **11**, options light/dark added per card 72's open item)
- [x] `docs/` architecture doc rewritten for the domain/infra layout — the four bounded contexts, the driven/driving ports, the three composition roots, and where a new adapter goes
- [x] `docs/` gains (or updates) a testing page describing the pyramid: Vitest domain → infra → component, with `verify/` as the end-to-end gate on top, and the release gate command list
- [x] README refreshed: scripts table includes `npm test`, `npm run guard` and the guard's marker policy; runtime requirements (Chrome 149, WebMCP flag) re-checked
- [x] package.json version bumped to 0.2.0 and `npm run guard` green, with the full gate — check, test, build, verify, guard — recorded as evidence in this card's `## Gates`
- [x] npm run check, npm test, npm run build and npm run verify green

## Gates

- [x] check — `npm run check` green: svelte-check 1074 files, 0 errors, 0 warnings; `tsc -p tsconfig.node.json` clean (claude-opus, 2026-08-22T08:19:47Z)
- [x] test — `npm test` green: 48 files, 736 tests (732 passed, 2 expected fail, 2 todo), 6.13s. Was 47 files / 724 before this card; +1 file / +12 tests is `src/infra/chrome-storage/testing/storage-fixtures.test.ts` (claude-opus, 2026-08-22T08:19:53Z)
- [x] build — `npm run build` green in 1.19s; `dist/manifest.json` reads `"version": "0.2.0"` and no longer declares `scripting` (claude-opus, 2026-08-22T08:20:05Z)
- [x] guard — `npm run guard` exit 0. depcruise: 183 modules, 531 dependencies, 0 errors / 1 warning (the accepted `ToolArgValue.svelte` self-import). Domain purity: 47 files, 0 platform globals. Three containment scans over 191 files, 0 violations, 0 exceptions. Clean-code: nothing above 0.5 (claude-opus, 2026-08-22T08:20:10Z)
- [x] verify — `npm run verify` green: **9/9 required checks passed**, best-effort screenshots PASS with count **11** (claude-opus, 2026-08-22T08:22:00Z)

## Comments

- **claude-opus** (2026-08-22T08:25:00.000Z): **Typed fixtures.** The hand-written seed in `verify/checks/screenshots.mjs` is deleted, not synced — the harness now imports `src/infra/chrome-storage/testing/storage-fixtures.mjs:1-573`. Shape chosen: plain `.mjs` with JSDoc types, the same trick `src/infra/webmcp/timeouts.mjs:1-13` already uses for the timeout ladder, because `verify/` is no-build Node ESM and cannot import TypeScript. It has no runtime imports at all — every `import(...)` in it is inside a JSDoc comment and erases — so the records cross `page.evaluate`'s structured-clone boundary as plain JSON. Home is `src/infra/chrome-storage/testing/` because that folder owns the storage schema: the `chat:`/`tabchat:`/`providers:`/`settings:`/`mcp:` keyspaces are declared by the adapters next to it and nowhere else. `tsconfig.app.json`'s `allowJs`+`checkJs` mean `npm run check` typechecks it against the real `ChatSession`/`TranscriptEntry`/`ProviderConfigCore`/`McpServerConfigCore` — proved by planting a missing `createdAt` and watching svelte-check name `storage-fixtures.mjs:199` before reverting it.
- **claude-opus** (2026-08-22T08:25:10.000Z): The old copy had **already drifted**, which is the case for the card: its transcript entries carried no `createdAt` (required on `TranscriptEntry` since card 77 — `src/domain/chat/message.ts:81`) and its provider record carried a `defaultModel` field `ProviderConfig` has never had (`src/domain/providers/registry.ts:24-42`). Neither broke a screenshot, because `chrome.storage` accepts any JSON and `isChatSession` (`src/infra/chrome-storage/chat-store.ts:88-102`) only checks the fields it needs. `src/infra/chrome-storage/testing/storage-fixtures.test.ts:1-208` is the round-trip that ends that: it seeds the in-memory fake exactly as the harness seeds a real profile (a raw `set`, bypassing every adapter) and reads back through `ChatStore`, `ProviderRegistry`, `SettingsStore` and `McpServerRegistry` — 12 cases, including that the fixture's hand-written `chat:index` equals what the domain's own `summarizeChat` derives (`storage-fixtures.test.ts:124-133`), and that `new URL(FIXTURE_TAB.url).origin` still matches the origin the `tabchat:` pointer was written for, which is what `src/infra/chrome-runtime/tab-sync.ts:91-94` compares against.
- **claude-opus** (2026-08-22T08:25:20.000Z): **Selectors verified, not assumed.** `"More options"` still resolves (`src/sidepanel/components/OverflowMenu.svelte:89`), and the three styling-free hook classes are all still in place and still commented as harness contracts — `.picker__trigger` (`src/sidepanel/components/ProviderPicker.svelte:357`), `.activity-group`/`.summary` (`src/sidepanel/components/ActivityGroup.svelte:83-86`), `.step`/`.row-head` (`src/sidepanel/components/ToolCallRow.svelte:142,162`). One stale COMMENT was found and fixed rather than a stale selector: the old file claimed the menu's first `role="menuitem"` was a connection-status row above "Recent chats", but card 64 made that a `DropdownMenu.Label` at the BOTTOM of the menu (`OverflowMenu.svelte:135-142`). The recent-chat row is now matched by `FIXTURE_CHAT_PROMPTS[1]` rather than a literal string, so seed text and locator cannot diverge (`verify/checks/screenshots.mjs:215-219`). Selector resolution stays a hard `requireLocator` failure; only the capture is best-effort.
- **claude-opus** (2026-08-22T08:25:30.000Z): **Options screenshots** (card 72's open item). `screenshotSidepanel` is now `screenshotSurfaces` (`verify/checks/screenshots.mjs:281`), and `captureOptions` (`verify/checks/screenshots.mjs:242-279`) shoots the options page full-page in light and dark from its own tab, off the SAME fixture — the seeded provider fills Chat providers, the seeded (disabled, so nothing dials out) MCP server fills MCP servers, and the six seeded chats fill Chat history, so no section is captured in an empty state. All four section headings plus both seeded rows are `requireLocator`s, so a section that failed to mount is a named SKIP rather than a blank card in a picture. `optionsUrl()` added at `verify/lib/browser.mjs:79-82`. `EXPECTED_SHOTS` 9 → 11.
- **claude-opus** (2026-08-22T08:25:40.000Z): Two harness artefacts showed up in the first options capture and are fixed at source rather than cropped out. (1) The seed now clears the keyspace it owns before writing (`FIXTURE_LOCAL_KEY_PREFIXES`, `storage-fixtures.mjs:508-520`; `screenshots.mjs:108-126`), with a test asserting no fixture key escapes those prefixes. (2) `verify/run.mjs:316-325` now closes the control page before the screenshot check: it is a REAL side panel that has been open since the top of the run, and it had been persisting chats of its own (one for its own `chrome-extension://` tab, one for the demo tab's trip through `about:blank`) that surfaced as strays in the chat-history shot. Every check needing it is done by then; the last check uses its own control page in a second browser. Result: the options shot now reads "Clear all history (6)" — exactly the fixture.
- **claude-opus** (2026-08-22T08:25:50.000Z): **Docs.** `docs/01-architecture.md` rewritten around the two slices — four runtime contexts (the options page was missing entirely) and four layers, with a bounded-context/ports table, an adapter table, the four composition roots, a "where a new adapter goes" rule, and the full guard inventory (11 depcruise rules + 2 hygiene + the four source scans). The timeout-ladder section now names `src/infra/webmcp/timeouts.mjs` as the single source and uses the real constant names; "Session ownership: one `ChatSession`, one owner" is rewritten for card 77's `ChatService` split. New `docs/05-testing.md` carries the pyramid, the two test doubles, how to read `expected fail`/`todo`, the nine verify checks, the screenshot matrix and the release-gate list. Stale path refs fixed: `src/lib/ollama.ts` → `src/infra/ollama/client.ts` and `src/lib/providers/openai.ts` → `src/infra/openai/index.ts` (`docs/04-troubleshooting.md:20,34,76`); `ToolCallCard.svelte` → `ToolCallRow.svelte` and "one session per tab" → decision 13's global history (`docs/03-privacy-and-trust.md:8-16,88`), plus the MCP bearer/OAuth credentials the sync/local split also covers. `docs/04`'s opening no longer claims card 14 is unshipped.
- **claude-opus** (2026-08-22T08:26:00.000Z): **README.** New `## Scripts` table (build/dev/check/test/guard/verify/demo/launch) with the release-gate line and the clean-code marker policy spelled out, a new `## Tests` section for the pyramid, and `## Architecture in brief` with the layer sketch and the guard's job. Fixed `src/lib/components/ui/` → `src/ui/components/ui/` and `src/lib/icons.ts` → `src/ui/icons.ts` in Third-party assets. "one conversation per browser tab" replaced with decision 13's global history. Project status: dropped the "no connection diagnostics yet" and "no tools/call-log inspector yet" bullets — cards 11 and 14 are both in review — and replaced them with the two `it.fails` known bugs, so they are visible to a reader rather than only to whoever runs the suite. Screenshot captions updated for the 11-shot matrix; `docs/images/sidepanel-{light,dark}.png` re-checked against this run's `sidepanel-{light,dark}-400w.png` and found byte-identical, so no image churn.
- **claude-opus** (2026-08-22T08:26:10.000Z): **Version + manifest.** `package.json:4` and both `package-lock.json` version fields → `0.2.0` (hand-edited rather than `npm i --package-lock-only`, which wanted to add 68 unrelated bundled-dep lines). Confirmed `manifest.config.ts:11-12` takes `description` and `version` from package.json — `name` does NOT, it is the literal `"OpenChat (WebMCP)"`, which is correct and intentional but is worth knowing before anyone tries to rename via package.json. `dist/manifest.json` verified at 0.2.0. One real finding on the permissions sanity check: **`scripting` was dead**. It came from the card 01 scaffold for the MAIN-world bridge, which decisions/16 deleted; nothing in `src/` has called `chrome.scripting` since, and the ISOLATED-world relay is a declared `content_scripts` entry needing no such permission. Removed at `manifest.config.ts:22-44` with the reasoning inline, and `npm run verify` re-run afterwards — 9/9 still green, which is as strong a confirmation as this repo can produce that nothing needed it. Nothing debug-ish leaks: the only debug switch is `debug:tab-sync-tracing`, storage-gated in `src/infra/chrome-storage/debug-flags.ts`, off by default.
- **claude-opus** (2026-08-22T08:26:20.000Z): **Release gate** recorded in `## Gates` above — all five green, `npm run verify` reporting 9/9 required checks and an 11-shot screenshot matrix. Change-review pass over my own diff caught three things, all fixed before the final run: the fixture's chat count was a magic `6` that could outrun its prompt array (now derived from it, `storage-fixtures.mjs:139-152`), `summarizeFixtureChat` did not mirror `chatPreview`'s 120-char cap so a longer future prompt would silently disagree with `summarizeChat` (`storage-fixtures.mjs:484-497`), and the README/architecture doc's depcruise rule count omitted the two hygiene rules. NOT done, deliberately, and left to the next cards: the improvement-sprint backlog — the two `it.fails` bugs, and the ≤0.5 clean-code markers the guard reports and allows.
