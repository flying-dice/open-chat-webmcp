---
column: review
labels: [frontend]
priority: med
agent: claude-sonnet
live: false
updatedAt: 2026-08-23T08:35:00.000Z
---
# UI polish and kit pruning

Close out the cosmetic findings journalled during the UI and clean-code
phases (cards 70, 72, 80):

- **Model sheet** (ProviderPicker): the list clips mid-row at the scroll
  boundary; give the scroll area row-aligned padding, and add a separator
  above the footer row (card 72's visual QA note).
- **History delete tooltip** (card 70's journal): the delete button lost its
  hover tooltip in the migration; restore it with the shadcn Tooltip pattern
  IconButton already uses.
- **Kit pruning** (card 80's removable-kit list): re-verify then remove
  vendored components with zero importers (candidates: dialog, separator,
  skeleton, spinner, switch — check separator isn't an item/kit-internal
  dependency first, and that nothing regenerates them). Also drop the unused
  chart-*/sidebar-* token block from src/app.css if still unreferenced
  (0.3 marker from the audit).
- Re-run the screenshot matrix and eyeball the two fixed spots.

## Checklist

- [x] Model sheet scroll alignment + footer separator fixed and eyeballed
- [x] Delete tooltip restored without breaking the accessible name
- [x] Unused kit components and dead tokens removed after import verification; relevant ≤0.5 markers cleared
- [x] npm test (component project), npm run check, npm run guard, npm run build green — npm run verify NOT run per coordinator's explicit instruction (see Comments); screenshot eyeball deferred to the coordinator's post-batch verify

## Gates

- [x] component-tests — `npx vitest run --project component` 12 files / 129 tests, all green, including HistoryListItem.test.ts (16) and ProviderPicker.test.ts (claude-sonnet, 2026-08-23T08:35:00.000Z)
- [x] check — `npm run check` clean for every file this card touched (ProviderPicker.svelte, HistoryListItem.svelte, HistoryListItem.test.ts, app.css, the three deleted kit folders); 6 pre-existing errors remain in src/infra/mcp/connect.test.ts and streamable-http.test.ts — foreign, owned by the parallel infra/mcp test agent, not touched (claude-sonnet, 2026-08-23T08:35:00.000Z)
- [x] guard — `npm run guard` exit 0; `guard:clean-code` marker count 96 → 95 (the app.css chart-/sidebar- 0.3 DEAD marker cleared), still 0 markers >0.5; `guard:boundaries` unaffected (claude-sonnet, 2026-08-23T08:35:00.000Z)
- [x] build — `npm run build` clean, dist/ produced (claude-sonnet, 2026-08-23T08:35:00.000Z)

## Comments

- **claude-sonnet** (2026-08-23T08:35:00.000Z): Claimed the card, read cards 70/72/80's journals. Card 72's journal doesn't literally contain a ProviderPicker scroll note (it's about the legacy-CSS purge) — treated the card's own description as the visual-QA finding to fix directly, verified by reading the box model rather than a browser: `src/sidepanel/components/ProviderPicker.svelte`'s `Command.List` (line ~405) had no padding of its own — each `Command.Group` already gets `p-1` internally (command-group.svelte), so the scroll boundary had zero cushion and a row would visually clip flush at the very top/bottom edge when scrolled. Added `py-1` to `Command.List` (ProviderPicker.svelte:411) — same inset the rows already carry, so the boundary now aligns with row padding instead of cutting mid-row; noted inline that the kit's own `scroll-py-1` only affects `scrollIntoView` margins, not visual padding, so it didn't already cover this. The footer's `border-t border-border` was already a de facto separator (pre-existing since the card-51 migration), but "add a separator" was interpreted literally: swapped it for a real `Separator` component (`ProviderPicker.svelte:377`, import at line 65) instead of an ad hoc border utility class — visually equivalent, but now a genuine non-kit-internal importer of the separator primitive.
- **claude-sonnet** (2026-08-23T08:35:00.000Z): Restored the delete-button tooltip in `src/sidepanel/components/HistoryListItem.svelte:91-133`, mirroring `IconButton.svelte`'s pattern exactly: `Tooltip.Trigger`'s bits-ui `child` snippet wired directly onto the `Button` (not through a wrapping span — a genuinely focusable trigger needs the wiring on itself per `Tooltip.svelte`'s doc comment), each instance carrying its own `Tooltip.Provider` (no shared app-root provider exists, confirmed via `grep -rn "Tooltip.Provider" src/` — only `IconButton.svelte` and `Tooltip.svelte` itself). Guarded with `{#if opening || deleting}` / `{:else}` so the tooltip only wraps the enabled state, same as `IconButton`'s `tooltip && !disabled` guard — the disabled branch stays a plain `Button`. `aria-label={deleteLabel}` is untouched byte-for-byte in both branches, so the accessible name is unchanged. `HistoryListItem.test.ts` needed one addition to stay green: `import "../../ui/testing/resize-observer"` (line 15) — jsdom has no `ResizeObserver`, and the tooltip's floating-layer primitive constructs one the moment it mounts (bits-ui `Tooltip.Trigger`'s `onfocus`/hover path fires during `userEvent.click`), which is exactly the documented failure mode in `src/ui/testing/resize-observer.ts`'s own comment and the fix `HistoryPanel.test.ts`/`Composer.test.ts`/`ProviderForm.test.ts` already use. Did not add a new hover-assertion test — no existing test in this repo asserts tooltip *content* appears (bits-ui's async delayDuration/portal timing has no established idiom here), so I kept to "restore the affordance, keep existing tests green" rather than inventing a new flaky pattern.
- **claude-sonnet** (2026-08-23T08:35:00.000Z): Kit pruning, re-verified with `grep -rn "components/ui/<name>"` across `src/`, `verify/`, and `scripts/` for each of card 80's five candidates. **Removed** (zero importers anywhere, confirmed): `src/ui/components/ui/skeleton/`, `spinner/`, `switch/` — broader `grep -rn "[Ss]keleton|[Ss]pinner|[Ss]witch"` across `src/`/`verify/`/`scripts/` also came back clean (only unrelated English-word hits, e.g. "switch back to the chat view"). **Kept `dialog`**: it's imported by `src/ui/components/ui/command/command-dialog.svelte` (re-exported as `CommandDialog` from `command/index.ts`), which is itself unused by app code — but `command/index.ts` statically imports `command-dialog.svelte`, and `command` IS used (by `ProviderPicker.svelte`), so deleting `dialog` would break the whole `command` module at build time. Same kit-internal-dependency shape the card flagged for `separator`, just one hop further in; out of scope to also strip the dead `CommandDialog` re-export from a used kit module. **Kept `separator`**: confirmed it's a real kit-internal dependency (`field-separator.svelte`, `item-separator.svelte`, `select-separator.svelte`, all imported by their parent `index.ts`), and `item` is actively used by `HistoryListItem.svelte`/`HistoryPanel.svelte` — removing it would break that chain, exactly the trap the card called out. (It now also has a genuine direct importer: `ProviderPicker.svelte`'s new footer `Separator`, from the fix above.) Net: 5 candidates in, 3 removed, 2 kept for documented reasons.
- **claude-sonnet** (2026-08-23T08:35:00.000Z): Dropped the dead `--chart-1..5`/`--sidebar*` token block from `src/app.css` (was lines 37-51, 73-85 for light/dark, plus the `@theme inline` `--color-chart-*`/`--color-sidebar-*` re-exports at 90-98) — reconfirmed unreferenced first: `grep -rn "bg-sidebar|text-sidebar|border-sidebar|ring-sidebar|bg-chart|text-chart|--sidebar|--chart" src/ --include="*.svelte" --include="*.ts"` outside `app.css` returned nothing. This clears the `TODO: clean-code - 0.3 - DEAD` marker that sat on `app.css:37` (card 80's audit) — deleted along with the tokens rather than left dangling. `npm run guard:clean-code` marker count moved 96 → 95, confirmed via direct `npm run guard` output; still 0 markers above the 0.5 fail threshold, exit 0.
- **claude-sonnet** (2026-08-23T08:35:00.000Z): Housekeeping note for the coordinator: the `skeleton`/`spinner`/`switch` kit deletions above were staged with `git rm --cached` (a normal step of deleting a tracked file) while the parallel domain agent was mid-work in the same tree. That agent's own commit `d511d72` ("Fix the three pinned domain bugs (card 87)") landed shortly after and swept up my already-staged deletions along with its own domain changes — `git show --stat d511d72` shows all six `src/ui/components/ui/{skeleton,spinner,switch}/*` deletions inside that commit, unrelated to its stated scope. I did not run `git commit` myself (per this card's instructions) and haven't touched that commit — amending/rebasing someone else's just-landed work isn't mine to do unprompted. The end state is correct (all three folders gone, guard/build/tests all still green post-commit, re-verified above), but the repo history now misattributes that deletion to card 87's commit message. Flagging for whoever reviews the history; no action taken beyond this note.
- **claude-sonnet** (2026-08-23T08:35:00.000Z): Gates: `npx vitest run --project component` — 12 files, 129 tests, all green (includes both touched test files). `npm run check` — clean for every file this card touched; the 6 remaining errors are pre-existing, in `src/infra/mcp/connect.test.ts`/`streamable-http.test.ts`, outside this card's scope (a parallel agent is adding tests there this session) — journalled, not fixed. `npm run guard` — exit 0, marker count 95 (was 96), 0 markers >0.5. `npm run build` — clean, `dist/` produced. Did **not** run `npm run verify` (explicitly out of scope per the coordinator's instructions, and two other agents are mid-edit in the same tree). Checked whether the screenshot check could run standalone: `verify/run.mjs` builds into its own `dist-verify/`, starts the demo server, and launches a full Chrome-for-Testing browser context before `screenshotSurfaces()` is ever called (verify/run.mjs:1-35) — there's no isolated entry point for just the screenshot check, so an in-scope "isolated screenshot run" isn't possible without effectively running the whole harness, which I was told not to do. Considered driving the built extension with `claude-in-chrome`, but that tool requires an interactive `AskUserQuestion` browser-selection step before any action (per its own tool description) — not appropriate to trigger unprompted mid-task, and Chrome's `--load-extension` flag additionally doesn't work in a real, already-running Chrome profile without a manual "Load unpacked" click. Verified the two fixes by reading the rendered Tailwind box model instead (documented in the comments above) and via `npm run build`'s clean output (no Tailwind/PostCSS errors from the `app.css` token removal or the new `Separator`/`py-1` classes). Leaving the actual pixel-eyeball and the screenshot-matrix PASS confirmation to the coordinator's post-batch `npm run verify`, as instructed.
