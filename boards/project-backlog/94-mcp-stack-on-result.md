---
column: review
agent: claude
live: false
labels: [backend]
priority: med
updatedAt: 2026-08-23T18:45:00.000Z
---
# MCP stack on the shared result tuple

Migrate `McpResult<T>` onto the shared `Result<T, McpError>` per
decisions/34-errors-as-values.md. The MCP stack already has never-throws
discipline, so this is largely a mechanical re-shape of
src/domain/tools/{types,gateway,sign-in}.ts and src/infra/mcp/* plus their
consumers (sidepanel mcp services, options test-connection, McpServerForm's
sign-in service) — but audit the internals while there: any internal throw
used for control flow between transport modules becomes a value, and the
oauth flow's known failures (discovery absent, registration rejected,
refresh expired, user-cancelled) must each be distinct vocabulary members
visible in signatures.

## Checklist

- [x] McpResult replaced by the shared Result across domain/tools and infra/mcp; signatures carry McpError end to end
- [x] OAuth known-failure vocabulary audited and complete; sign-in service surfaces them as values
- [x] Internal control-flow throws between transport modules eliminated; guard:throws entries for infra/mcp reduced to boundary-catch invariants only
- [x] All consumers and the 300+ MCP tests migrated to tuple assertions
- [x] npm test, npm run check, npm run guard, npm run build green (scoped to this card's files); npm run verify intentionally NOT run per card scope — coordinator's job. guard:throws fails ONLY on the one expected stale allowlist entry, see Gates below.

## Gates

- [x] tests-passing — whole-repo `npx vitest run`: 59 files, 925/925 tests passing (claude, 2026-08-23T18:35:00.000Z)
- [x] typecheck — `npx tsc -p tsconfig.app.json --noEmit`: 0 errors in src/domain/tools, src/infra/mcp, and every consumer this card touched; repo-wide `npm run check` (svelte-check) reports 0 errors/0 warnings across all 1082 files (claude, 2026-08-23T18:35:00.000Z)
- [x] build — `npm run build`: green (claude, 2026-08-23T18:35:00.000Z)
- [x] lint-format — `npx biome check` scoped to every file this card touched: clean; `npm run guard:biome` (whole repo): clean (claude, 2026-08-23T18:35:00.000Z)
- OVERRIDDEN — guard:throws (claude, 2026-08-23T18:35:00.000Z): fails on exactly one expected stale allowlist entry (`scripts/throw-allowlist.json`'s `src/infra/mcp/budget.ts` entry for `"return Promise.reject(new DOMException("`, declares 1, found 0 — the throw this card's checklist calls for removing is in fact gone). Per the card's own instructions the allowlist file is owned by the coordinator this batch and must not be edited here; the coordinator removes that one entry and guard:throws goes clean (10 sites, 10 declared).

## Comments

- **claude** (2026-08-23T18:10:00.000Z): `McpResult<T>` deleted from src/domain/tools/types.ts:1-108 — every signature across src/domain/tools/{gateway,sign-in}.ts and src/infra/mcp/{budget,json-rpc,headers,session,sse,streamable-http,legacy-sse,connect,oauth-http,oauth-metadata,oauth,results,gateway}.ts now spells `Result<T, McpError>` directly (matching how the storage ports already spell `Result<T, StorageError>` — no bespoke alias, per card 92's precedent). Construction sites use `ok(value)`/`fail(error)` from src/domain/result.ts; narrowing sites destructure `const [value, err] = ...`.
- **claude** (2026-08-23T18:10:00.000Z): OAuth vocabulary audit (src/domain/tools/types.ts:64-113) — added four McpError members that were previously folded into generic kinds: `discovery-absent` (oauth-metadata.ts:169-181, replaces a `not-mcp-endpoint`-shaped 404 on the final RFC 8414 metadata lookup), `registration-rejected` (oauth-metadata.ts's `registerClient`, replaces `invalid-response` for a non-2xx registration response — a malformed-but-200 response still stays `invalid-response`), `refresh-expired` (oauth.ts's `getValidAuth`, replaces `auth` for a missing refresh token or a refused refresh grant — remapped from `postToken`'s generic `auth` kind at the one call site that knows it's serving a refresh), `user-cancelled` (oauth.ts's `runAuthorizationFlow`, replaces `auth` for a closed sign-in window, an undefined `launchWebAuthFlow` result, and `error=access_denied`; a non-access_denied oauth error and a state mismatch stay `auth`). `describeMcpError` and src/options/forms/mcpTestConnection.ts's exhaustive switch both updated; the latter folds the three sign-in-only kinds into its existing outcome kinds since `discoverAllServerTools` can only actually reach `refresh-expired` (via `getValidAuth` inside `connect`), never the other three.
- **claude** (2026-08-23T18:10:00.000Z): Throw eliminated — src/infra/mcp/budget.ts:71-104's `raceWithBudget` no longer does `Promise.reject(new DOMException(...))`; it now resolves `Result<T, "timeout" | "other">` (a local, lightweight discriminant rather than a full McpError, since every caller already builds its own message per call site). Its three callers in src/infra/mcp/legacy-sse.ts (pump.endpoint(), the init-response wait, and the per-request wait inside `session.request`) all migrated from try/catch to destructuring. **Allowlist entry for the coordinator to remove**: `scripts/throw-allowlist.json`'s `src/infra/mcp/budget.ts` entry, `{"match": "return Promise.reject(new DOMException(", "count": 1, "reason": "migrates: card 93. ..."}` — `npm run guard:throws` currently reports this as a stale entry (10 real sites vs 11 declared) confirming the throw is gone. No other infra/mcp or domain/tools throw sites exist (`node scripts/guard-throws.mjs --inventory` confirms only this one was ever inside this card's scope).
- **claude** (2026-08-23T18:10:00.000Z): New test file src/domain/tools/types.test.ts adds the card's negative type-narrowing tests for `Result<T, McpError>` (mirroring src/domain/result.test.ts's `@ts-expect-error` pattern, but against the domain's own vocabulary rather than a stand-in error class) plus an exhaustiveness/distinctness check over `describeMcpError` for all 13 McpError kinds including the four new ones. New test helper src/infra/mcp/testing/unwrap.ts mirrors src/infra/chrome-storage/testing/unwrap.ts for McpError, for happy-path test assertions in the infra/mcp suite.
- **claude** (2026-08-23T18:10:00.000Z): Consumers migrated: src/sidepanel/services/mcpTools.ts:265-278 (`executeServerTool`'s `callServerTool` destructure), src/sidepanel/testing/fake-services.ts and src/options/testing/fake-services.ts (`createFakeMcpToolGateway` now built with `ok(...)`), src/options/forms/mcpTestConnection.ts (exhaustive switch extended, see above). src/domain/tools/sign-in.ts's oauth-port calls migrated to tuple destructuring; `McpSignInResult`/`McpSignInCompletion` (its own bespoke `{status:...}` vocabulary, not a Result) are unchanged — out of this card's scope per the checklist wording ("McpResult replaced", not every port's own return shape).
- **claude** (2026-08-23T18:10:00.000Z): Test-file migration (the 300+ MCP tests) split across four parallel agents by file, each briefed on the exact new signatures and the repo's established tuple-assertion idiom (`expect(x).toEqual([value, undefined])`, matching src/domain/result.test.ts and src/infra/chrome-storage/provider-registry.test.ts). src/domain/tools/sign-in.test.ts and src/infra/mcp/json-rpc.test.ts done directly by me. In progress / landing: connect, gateway, headers, legacy-sse, oauth-http, oauth-metadata, oauth, results, session (done), sse, streamable-http (done) test files. Final gate results to follow in the next journal entry once all land.
- **claude** (2026-08-23T18:35:00.000Z): All four migration agents landed. legacy-sse.test.ts and sse.test.ts (group A) and streamable-http.test.ts and session.test.ts (group B) were found already fully migrated by the time their agents started (a race with an earlier fork attempt that the harness rejected — "Fork is not available inside a forked worker" — turned out to have partially landed anyway before the relaunch; both agents verified rather than re-edited). gateway.test.ts, connect.test.ts, headers.test.ts, oauth-http.test.ts (group D) and results.test.ts, oauth.test.ts (group C) needed real edits, including group D's two `oauth-metadata.test.ts` kind reclassifications (`discovery-absent`, `registration-rejected`) matching the vocabulary audit above. Full picture now green:
  - `npx vitest run` (whole repo): 59 files, 925 tests passed.
  - `NO_COLOR=1 npx tsc -p tsconfig.app.json --noEmit`: zero errors anywhere in src/domain/tools or src/infra/mcp (production or test); the only other errors in the tree are pre-existing, foreign to this card, in the vendored shadcn kit (`src/ui/components/ui/**`), which decisions/34's Consequences exempts, and were already gone by the time of this check because `npm run check` (svelte-check + tsc) reports 0 errors/0 warnings across all 1082 files.
  - `npm run build`: green, 1.11s.
  - `npm run guard`: every sub-guard passes EXCEPT `guard:throws`, which fails on exactly the one expected stale entry — `scripts/throw-allowlist.json`'s `src/infra/mcp/budget.ts` entry (`"return Promise.reject(new DOMException("`, declares 1, found 0). Coordinator: delete that entry (see the earlier journal entry for exact match text) and `npm run guard:throws` will report clean (10 sites, 10 declared). `guard:boundaries` reports one PRE-EXISTING, FOREIGN warning unrelated to this card — a self-referential `no-circular` hit on `src/sidepanel/components/ToolArgValue.svelte` (exit code 0, not a failure) — not touched by this card, journaled here per scope rather than fixed.
  - `npm test`/full vitest, `npm run check`, `npm run build`, and `npm run guard` (modulo the one expected allowlist stale entry) are all green. `npm run verify` intentionally not run, per card scope — that's the coordinator's gate.
  - Ready for review: no McpResult references remain anywhere in src/ (confirmed by repo-wide grep); every domain/tools and infra/mcp signature spells `Result<T, McpError>`; the OAuth vocabulary is complete and distinct; the one internal throw is gone; every consumer and test is on tuple assertions.
- **claude** (2026-08-23T18:35:00.000Z): All four parallel test-migration agents landed. Two new-kind reclassifications confirmed in oauth-metadata.test.ts:169-181 (both discovery legs 404 -> `discovery-absent`; registration endpoint non-2xx -> `registration-rejected`, malformed-2xx cases stay `invalid-response`) and oauth.test.ts (missing refresh token, and a non-2xx refresh grant response, both -> `refresh-expired`; `runAuthorizationFlow`'s `user-cancelled` path has no dedicated test in oauth.test.ts since that file only exercises `getValidAuth`/`redirectUri`). Fixed two `useOptionalChain` lint warnings in oauth-metadata.test.ts:168,180 and ran a repo-scoped `biome format --write` pass across every touched file (fixed minor line-wrap drift, no semantic changes). One cross-card note for the record: `src/options/testing/fake-services.ts`'s `createFakeMcpToolGateway` edit (mine) ended up folded into card 93's commit 302db91 rather than staying uncommitted — both agents were editing that shared file concurrently and card 93 committed while my edit was present in the working tree. The change itself is correct (verified in the gates below); flagging only so the history is understood, not because anything needs undoing.
- **claude** (2026-08-23T18:35:00.000Z): **Final gate results** (scoped to this card's files: src/domain/tools/**, src/infra/mcp/**, src/sidepanel/services/mcpTools.ts, src/sidepanel/testing/fake-services.ts, src/options/testing/fake-services.ts, src/options/forms/mcpTestConnection.ts):
  - `npx vitest run src/domain/tools src/infra/mcp src/sidepanel/services/mcpTools.ts` — 15 test files, **263/263 passing**.
  - `npx tsc -p tsconfig.app.json --noEmit` — clean for every file this card touched (the only remaining repo-wide errors are pre-existing vendored-shadcn-kit noise in src/ui/components/ui/**, exempt per decisions/34's Consequences, and none in this card's scope).
  - `npm run build` — green.
  - `npx biome check` (lint + format) scoped to this card's files — clean.
  - `npm run guard:boundaries` / `guard:return-types` / `guard:clean-code` / `guard:biome` — all green; guard:clean-code's findings for src/sidepanel/services/mcpTools.ts are pre-existing (already TODO-flagged in the source before this card).
  - `npm run guard:throws` — **fails as expected, exactly one stale entry**: `src/infra/mcp/budget.ts` — `{"match": "return Promise.reject(new DOMException(", "count": 1, "reason": "migrates: card 93. ..."}` (the seed data's reason text says "card 93" but is this card's throw — likely a copy/paste artifact in the original seed). The guard reports "10 real sites vs 11 declared" and names this exact entry as stale (0 found, 1 declared) — **the coordinator should delete this one entry from scripts/throw-allowlist.json's `files["src/infra/mcp/budget.ts"]` array** (the whole key, since it was the only entry under that file) to bring the allowlist back in sync. No other infra/mcp or domain/tools throw sites exist or were ever in scope.
  - `npm run verify` intentionally not run, per card scope.
- **claude** (2026-08-23T18:45:00.000Z): **Independent re-verification by the parent agent** after both parallel test-migration groups reported back (their two summaries both claimed the whole card done, which is more than either was individually scoped for — this entry confirms their combined result rather than taking it on faith): `grep -rn "McpResult" src` finds only two hits, both my own comments naming the retired type for context, not a live usage. `npx vitest run` (whole repo, fresh): **59 files, 925/925 passing**. `npm run check`: **1082 files, 0 errors, 0 warnings**. `npm run build`: green. `npm run guard`: biome/boundaries/clean-code/return-types all clean; `guard:throws` fails on exactly the one stale `src/infra/mcp/budget.ts` entry named above, confirmed by direct inspection of `npm run guard:throws`'s own output ("10 throw/reject site(s) ... allowlist declares 11"). Spot-checked that the vocabulary reclassification tests actually exercise the new kinds rather than just constructing literals: `oauth-metadata.test.ts` has dedicated tests for `discovery-absent` (line 114) and `registration-rejected` (line 159); `oauth.test.ts` has dedicated tests for `refresh-expired` (lines 171, 190) — both were true regressions-would-be-caught tests, not decorative renames.
- **claude** (2026-08-23T18:45:00.000Z): **One pre-existing gap noted, not fixed (out of this card's scope):** `src/infra/mcp/oauth.ts`'s `runAuthorizationFlow` — the function `"user-cancelled"` actually lives in — has NO test coverage at all, before or after this card (confirmed via `git show HEAD~1:src/infra/mcp/oauth.test.ts`, i.e. the pre-card-94 version: only `getValidAuth` and `redirectUri()` were ever under test). `"user-cancelled"` is exercised only as a constructed literal in the new `src/domain/tools/types.test.ts` vocabulary/narrowing tests, never against the real implementation. This is a genuine coverage gap worth a future card (`runAuthorizationFlow` also has `chrome.identity.launchWebAuthFlow` to fake, which is why it was likely skipped originally), not something introduced or worsened by this migration — flagging for visibility rather than silently leaving it undocumented.
- **claude** (2026-08-23T18:45:00.000Z): Card confirmed ready for human review. Nothing committed by me at any point in this card's work.
