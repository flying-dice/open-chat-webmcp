---
column: review
agent: claude-opus
live: false
labels: [infra, docs]
priority: med
updatedAt: 2026-08-24T12:10:00.000Z
---
# Strict-safety final audit

Close the strict-safety session per decisions/34 and 35: prove the goal —
full strict type safety including typed error handling in function
signatures — and leave it enforced, not aspirational.

## Checklist

- [x] Sweep: zero `any` (explicit or inferred at exported boundaries — spot-audit with hover/`tsc --noEmit` tooling), zero non-test `as` casts without a comment naming why, zero `!` non-null assertions outside vendored kit; violations fixed or justified inline
- [x] Every exported function across src/ has an explicit return type; ports carry typed errors; a fresh clean-code-review inline pass over the migration diffs finds no new >0.5 findings
- [x] guard:throws, Biome, strict flags all wired into npm run guard and the pre-commit skill; a planted violation of each fails the gate (proven, then reverted)
- [x] docs/01-architecture.md + docs/05-testing.md updated for errors-as-values (how to add a failure mode, how tests assert errors); README scripts table updated
- [x] Full release gate re-run and recorded: npm test, npm run check, npm run guard, npm run build, npm run verify
- [x] Version bumped to 0.3.0

## Gates

- [x] tests-passing — `npm test` green: 950 tests across 61 files, unchanged by the audit (claude-opus, 2026-08-24T11:55:00Z)
- [x] typecheck — `npm run check` green: svelte-check 1088 files, 0 errors, 0 warnings; `tsc -p tsconfig.node.json` clean (claude-opus, 2026-08-24T11:55:00Z)
- [x] guards — `npm run guard` green, all five: biome ci 413 files clean; boundaries 190 modules / 617 dependencies, 0 errors (1 accepted `no-circular` warning) plus all four source scans with empty exception lists; clean-code 81 markers, none above 0.5; return-types 161/161; throws 7/7 (claude-opus, 2026-08-24T12:00:00Z)
- [x] build — `npm run build` clean in 1.16s; `dist/manifest.json` reads `"version": "0.3.0"` (claude-opus, 2026-08-24T12:00:00Z)
- [x] verify — `npm run verify` 9/9 required checks passed, plus the best-effort screenshot pass (11 files) (claude-opus, 2026-08-24T12:08:00Z)

## Comments

- **claude-opus** (2026-08-24T10:05:00.000Z): **THE SWEEP, WITH NUMBERS — and the method matters, because grep cannot answer this question.** `grep 'as '` over src/ returns 470 lines that are mostly the English word. I wrote a throwaway TypeScript-compiler-API walker instead (deleted after use) that parses every `.ts` and every `.svelte` `<script>` block and counts real `AsExpression`, `NonNullExpression` and `TypeAssertionExpression` nodes. **Explicit `any`: 2, both in src/ui/utils.ts:9,11** — `WithoutChild`/`WithoutChildren`, shadcn-svelte CLI output that still carries the generator's own `eslint-disable` comments, already lint-excluded on those grounds in biome.jsonc:120. Zero anywhere else, tests included, and zero in the vendored kit. **Non-null `!`: 0 in production.** Verified with the linter rather than by eye — `biome lint --only=style/noNonNullAssertion` forces the rule on past the test/fake override and reports 107, every one in a `.test.ts`, a `testing/` fake or `demo/`. That number is worth recording because biome.jsonc:150-154's own header said "production has 4, and those are held to the rule": cards 92-95 removed all four as a side effect, and I have corrected that comment. **Angle-bracket assertions (`<T>x`): 0.** **Definite-assignment `!` (`let x!: T`): 2**, both in src/infra/mcp/legacy-sse.ts:40-41 — a different operator from the non-null one the card names, and the canonical deferred-promise idiom; the invariant (the Promise executor runs synchronously, before `new Promise` returns) is now written at src/infra/mcp/legacy-sse.ts:32-39 along with why the two alternatives — `| undefined` plus `?.` everywhere, or ES2024's `Promise.withResolvers`, which is past the es2023 target — cost more than they prove.

- **claude-opus** (2026-08-24T10:20:00.000Z): **THE CAST SURVEY: 57 → 38 in production, and every one of the remaining 26 sites now names its invariant.** Of the 38, eight are `as const` and the card exempts them; 30 real assertions across 26 distinct sites. **Nineteen I deleted rather than commented, because a comment explaining an avoidable cast is a comment explaining why nobody fixed it.** (1) **src/background/sw.ts:184-215, six gone.** `isToolsUpdatedMessage`/`isCallToolResponse` each opened with their own inlined `typeof v === "object" && v !== null` and then read every field through `(v as Record<string, unknown>)`. They guard message types that src/infra/chrome-runtime/protocol.ts DEFINES, and that module already exports `isRuntimeMessage` — and `RuntimeMessage` is discriminated on `type`, so `isRuntimeMessage(v) && v.type === "runtime:tools-updated"` narrows the rest for free. The per-field `typeof` checks stayed: they look redundant to the compiler and are not, because the sender is a content script on an untrusted page. (2) **src/sidepanel/presentation/untrustedJson.ts (new, 65 lines), thirteen gone** — nine in SchemaProperty.svelte, two in ToolSchema.svelte, plus three copies of `isRecord` and three copies of the required-name string filter. Written inline, each read was a check-then-assert pair (`typeof rec?.type === "string" ? (rec.type as string) : undefined`) — the same fact stated twice, once to the reader and once to the compiler. `readString`/`readRecord`/`readArray`/`readStringArray` narrow a local instead, so the return type is PROVED. Deliberately not in src/ui: that folder's README sets the bar at "presentation BOTH Svelte surfaces render through", and the options page renders no tool schemas. (3) **src/infra/mcp/legacy-sse.ts:50** — `reject(e as Error)` on a `reject` whose parameter is already `any`; the cast asserted a type nothing asked for.

- **claude-opus** (2026-08-24T10:35:00.000Z): **The 26 that stayed, by category, each now carrying a `CAST:` note naming the invariant — and one cluster that is the real finding.** **Untyped platform APIs (4):** `chrome.runtime.sendMessage` resolves `any` in @types/chrome, so page-tool-executor.ts:39 and tab-sync.ts:134 assert the response shape precisely to stop that `any` spreading — the note says so, and says nothing downstream trusts it; relay.ts:267's `getTools()` and markdown.ts's `marked.parse(..., {async:false})`, which TypeScript will not resolve to the synchronous overload from an object literal. **`JSON.parse` is `any` (3):** relay.ts:198, chat-store.ts:190, ollama/client.ts's `ok(json as T)` — in all three `T` is the CALLER's claim, and each note says where that claim is actually checked (this is a real property, not a shrug: ollama/client.ts's decoders re-read every field through `isRecord`). **Validated decode (3):** provider-registry.ts, mcp-server-registry.ts, provider-config-store.ts each end a field-by-field `if` with `v as unknown as X`; TypeScript does not turn an inline conjunction into a whole-object narrowing, and the note says the `if` IS the decode. **`readonly` tuple `.includes(string)` (3):** servers.ts, provider-registry.ts, mcp-server-registry.ts — widening the RECEIVER, and the note makes the point that this keeps the check honest where widening the argument would not. On that last one I also **fixed** provider-config-store.ts:65, which widened the ARGUMENT (`value.status as string` on an `unknown`) — now a `typeof` guard. **The cluster: src/infra/chrome-storage/keyed-record-store.ts, eleven of the twenty-six.** Rather than eleven separate excuses it gets one header (:42-61) stating the shared fact — the store is generic over `TParts` while its machinery is necessarily key-by-key (`Object.keys`, a merge into an accumulator, a spread of two halves), none of which TypeScript models as operations on a mapped type — plus the alternative weighed and rejected (making it type-safe end to end pushes the same erasure OUT into both registries' declarative specs, for no runtime guarantee), plus the thing actually worth auditing: **the erasure is CONTAINED.** Every assertion is inside that file, `KeyedRecordStore` is fully typed, and both registries and their tests are assertion-free. Each of the eleven then gets one line naming what it stands in for.

- **claude-opus** (2026-08-24T10:45:00.000Z): **INFERRED `any` at exported boundaries — the half of the card that grep genuinely cannot reach, and it found two.** Built a second throwaway script (deleted) that runs a real `ts.Program` over the 97 non-test modules, walks `checker.getExportsOfModule` for every one, resolves aliases, filters to VALUE symbols (a type alias's "type of symbol" is `any` by construction — my first pass produced 300 false positives that way, which is exactly the trap the card's "spot-audit" wording is about), and stringifies each with `NoTruncation`. **292 exported value symbols; 2 mentioned `any`, and they were the same thing twice: `export default app` in both src/sidepanel/main.ts and src/options/main.ts.** `mount()` returns Svelte's legacy component-instance shape, `{ $on?(…): …; $set?(…): … } & Record<string, any>`. Nothing imports a composition root — both surfaces load it as an HTML entry script — so the Svelte scaffold's default export was dead weight publishing an `any`-flavoured type as a module's public surface. Deleted from both (src/sidepanel/main.ts:169-176, src/options/main.ts:71-78) with the reason recorded in place. Re-run: **292 symbols, zero mention `any`.** Build and verify both confirm the entry points still mount.

- **claude-opus** (2026-08-24T11:15:00.000Z): **THE CLEAN-CODE PASS — and why I did NOT do it inline.** The card says "inline pass, per the skill's proportionality gate judgment". I exercised that judgment the other way: the cumulative strict-safety diff is `c605e6b..HEAD` = **143 files, +5,523 / −2,523**, which is two orders of magnitude past the skill's "> 50 lines OR > 3 files → full sub-agent audit" threshold, and the card's own framing (find regressions the migration INTRODUCED across five commits) is exactly what a per-principle fan-out is for. Three parallel agents — DRY, SRP+COUPLING, KISS+DEAD+NAMING — each scoped to that range, each told to check the existing 77 markers before reporting so I got new findings and not a re-read of accepted debt. **All three came back with nothing above 0.4.** Both the DRY and the SRP agent independently opened with the same unprompted observation, that this is an unusually disciplined migration whose non-mechanical changes each carry a comment tying them to decisions/34 or 35, and the SRP agent specifically found the notice-channel design applied consistently and named two changes as net coupling IMPROVEMENTS (the pointer-before-swap reorder, and both copies of the dead `buildClient` try/catch being deleted rather than migrated). No new cross-layer edges, no adapter importing an adapter, nothing platform-shaped in src/domain.

- **claude-opus** (2026-08-24T11:30:00.000Z): **Seven findings, four recorded as markers, two fixed, one refused — with the reasoning, because "score ≤ 0.5" is not by itself a reason to write it down.** **FIXED (2).** `describeStorageError` (src/ui/storageMessage.ts) had zero production callers — every one of the ten real call sites uses the sibling `storageFailureMessage` — so it was deleted rather than marked: a second exported way to word one union is how two surfaces start saying different things. Its exhaustiveness property is NOT lost; src/ui/storageMessage.test.ts:42-53 now asserts the same "no two kinds share a sentence" through the remaining export (the fixed `what` cancels out) and records why. And four sites in src/infra/mcp/gateway.ts and headers.ts hand-wrote `return [undefined, err];` instead of calling `fail(err)` — a small crack in "one blessed way to fail" that a future change to `Err<E>` would have had to find by grep. **MARKED (4).** `settings-store.ts:30` [0.35] — the migration turned five `chrome.storage` reads across three files into the same copy-pasted "read → propagate → decode-with-fallback" composite, where each was previously one line; a `readDecoded(area, key, decode, fallback)` in area.ts collapses all five. `legacy-sse.ts:232` [0.35] — card 94's Result-ification of budget.ts left the same `err === "timeout" ? … : …` ternary at three sites. `ProvidersSection.svelte:272` and `McpServersSection.svelte:87` [0.35 each] — the whole card-95 write-failure protocol (`failure` state, `reportWriteFailure`, `Result`-returning submit handlers, the paired form rendering the message) duplicated across that file pair; same shape as the four DRY markers it already carries, but genuinely new and named by none of them. **REFUSED (1), and this is the one worth arguing.** The DRY agent's top finding (0.4) was the `exactOptionalPropertyTypes` idiom — `...(x !== undefined && { x })` — repeated at ~26 sites across nine files with a near-identical rationale comment each time. I probed the proposed helper against a real `tsc --exactOptionalPropertyTypes` run: `optional("body", body)` **does** compile. It is still the wrong trade, because it converts a compiler-checked property name into a string literal, and a spread does not get excess-property checking, so a typo silently drops the field — strictly worse in a repo whose whole session was about moving facts INTO the type system. The real cost was never the one-line spread; it was the paragraph pasted 26 times. So that paragraph now has one home (docs/01-architecture.md, under the strictness flags) and no marker was planted proposing a fix I do not believe in. Two sub-0.3 items I also declined: the two `unwrap` test helpers are duplicated because `adapters-do-not-import-adapters` forbids the shared home, i.e. the same forced duplication the isRecord markers already document; and the `#app` mount guard in both roots is four lines the roots are deliberately parallel about, already covered in spirit by the 0.25 root-to-root marker both files carry. **Marker count: 81 → 77 → 81** — four retired by the isRecord consolidation (sw.ts and the three inspector components are no longer reimplementation sites, and I rewrote the surviving five markers' file lists, which said "at least nine times" and would otherwise have been quietly wrong), four added above.

- **claude-opus** (2026-08-24T11:45:00.000Z): **ENFORCEMENT PROOF — one planted violation per guard, each confirmed to fail NAMING the offence, each reverted.** Not "the guard exists" but "the guard bites", which is a different claim. **`guard:biome`** — `const x = v as any` in src/domain/tools/servers.ts → `src/domain/tools/servers.ts:102:18 lint/suspicious/noExplicitAny`, `Found 1 warning`, non-zero exit (the `--error-on-warnings` flag is what makes a warning blocking). **`guard:boundaries`, both halves, because the file's own header says neither alone is the guard.** Import half: `src/domain/tools/merge.ts` importing `../../infra/chrome-storage/area` → two named rules at once, `error domain-is-pure` and `error only-roots-construct-infra`, `2 errors`. Text-scan half: `chrome.tabs.TAB_ID_NONE` added to src/domain/chat/session.ts → reported twice, by the domain-purity pass and by the `chrome.*` containment pass, both quoting the offending line. **`guard:clean-code`** — a `TODO: clean-code - 0.8 - KISS:` marker → `1 marker(s) above 0.5 — these must be fixed`, exit 1; exit 0 again on revert. **`guard:return-types`** — an exported function with the annotation removed → `1 exported function(s) with no declared return type`, quoting the signature, exit 1. **`guard:throws`** — one `throw new Error(...)` → `8 throw/reject site(s) … allowlist declares 7`, `1 site(s) not on the allowlist`, quoting the throw and pointing at scripts/throw-allowlist.json, exit 1. All five reverted, `npm run guard` back to exit 0, `git status` shows only this card's own edits. **The pre-commit skill was stale and is fixed** (.claude/skills/pre-commit/SKILL.md:10-15): card 91 added three guards after that text was written, so step 3 still described `npm run guard` as clean-code plus boundaries. It now names all five with what each one fails on, and says the thing an agent most needs to know — that `guard:biome` fails on a FORMATTING difference, so `npm run format` belongs before a push, and that a newly discovered expected failure is a `Result` in the signature, never a new allowlist entry.

- **claude-opus** (2026-08-24T11:50:00.000Z): **Docs.** **docs/01-architecture.md** gains `## Errors as values` between the composition roots and the guards — the tuple and why it is a tuple rather than a record (the dependent-destructuring narrowing, and the warning that widening the success arm's `error` to `E | undefined` silently kills it at every call site), the four vocabularies in a table, **how to add a failure mode as four numbered steps** (extend the vocabulary in the domain → return `fail()` at the adapter boundary with the platform error kept in `cause` → map it to copy in the UI, where `storageMessage.ts`'s exhaustiveness test is the thing that stops a new kind arriving without wording → and then the compiler forces every caller, which is the return on the whole migration), the "not everything returns a `Result`" rule with `ChatService`'s three postures, and where `throw` is still allowed (seven sites, each allowlisted, plus the three places `try`/`catch` legitimately survives). The `## The guards` section said "`npm run guard` is two scripts" — now five, with a paragraph each for biome, return-types and throws, and the maximal-strictness flag set including the honest note on why `noPropertyAccessFromIndexSignature` is the one flag decision 35 lists that is NOT adopted. **docs/05-testing.md** gains `### Asserting a failure`: destructure and assert the whole error object rather than just its `kind`, `toThrow` is now reserved for the seven allowlisted invariants, and — the part that is easy to miss — the `@ts-expect-error` **negative type probes** in the four vocabulary test files run under `npm run check`, not `npm test`, because `tsconfig.app.json` includes `*.test.ts` and an UNUSED `@ts-expect-error` is itself an error. That inversion is the mechanism: widen the success arm's `error` member and the probe stops erroring, so the build fails on the now-unnecessary suppression. **README.md**'s scripts table gains `lint`/`lint:fix`/`format`/`format:check`, notes that `check` also typechecks the tests, and replaces the one-line guard row with a five-row table of what each gate fails on.

- **claude-opus** (2026-08-24T12:10:00.000Z): **THE CONSOLE-ONLY DECISION card 95 handed me — judged half-right, and left as boards/project-backlog/106-unsaved-conversation-notice.md rather than as a paragraph.** Card 95's note said both roots wire `ChatServiceDeps.reportStorageFailure` to `console.error` (src/sidepanel/main.ts:114), so the ABSORBED failures stay diagnostics, and offered its own defence: no user action is attached, and the wording cannot honestly cover a failed write and a failed read at once. I agree with half of that and not the other half. **`syncToTab`/`applyNavigation` should stay console-only** — a `chrome.tabs` event drove them, nobody asked, and src/sidepanel/stores/notices.svelte.ts's own contract is "a failure whose consequence follows the user out of the view they caused it in", which a tab-switch read does not. **The transcript mutators should not.** They persist fire-and-forget so a token stream never waits on a write; when that write keeps failing, the conversation ON SCREEN is not being saved and is gone at the next tab switch. Silent data loss is precisely the failure a person can act on — copy the text out, stop relying on History — and the panel has had a channel to say it in since card 95. The stated objection about wording is also weaker than it looks: `storageFailureMessage(what, err)` is already parameterised on `what`. What makes this NOT a lightweight fix is different and real: `reportStorageFailure` is handed a DEVELOPER string (`[webmcp][chat] save failed for chat ${id}`), so surfacing it means changing a domain interface, its four call sites, both roots' wiring, the copy and the tests — with a de-duplication question (a run of failing debounced writes must read as one problem) and a clearing question (a later success must retract it) that both want their own tests. That is a card, and landing it unreviewed at the release gate of a session whose entire point was to leave the tree in a PROVEN state would have been the wrong trade. So: card 106, with the split, the reason it is not one line, and the four checklist items. **Version 0.3.0** in package.json and both `version` fields of package-lock.json; `dist/manifest.json` reads it back (manifest.config.ts:12 takes it from the package). **Nothing committed.** Two throwaway analysis scripts (the AST cast walker and the `ts.Program` exported-boundary auditor) were deleted after use — they are reproducible from this journal and are not worth carrying as repo scripts until something needs them a second time.
