---
column: review
agent: claude-sonnet
live: false
labels: [frontend]
priority: high
updatedAt: 2026-08-23T15:40:00.000Z
---
# Apply the type and icon scale

Implement card 98's approved spec (decisions/36) across both surfaces:
normalize every text role and icon size onto the named scale, fixing the
inventoried offenders in impact order (the Tools-panel empty state and
section labels first, per Jonathan's report). Component tests asserting
copy/roles must stay green; screenshot matrix re-captured and eyeballed
against the spec at both widths, light and dark.

## Checklist

- [x] Scale tokens/utilities established once (app.css or shared classes) rather than per-component literals
- [x] All offenders from card 98's fix-list normalized; no stray one-off sizes left (grep sweep for text-* size classes outside the scale)
- [x] Icon sizes consistent per role (toolbar, inline, empty-state glyph)
- [x] decisions/36 moved to Accepted
- [ ] Screenshot matrix re-captured, eyeballed, and committed where referenced — deferred: `npm run verify` (which drives `verify/checks/screenshots.mjs`) was intentionally NOT run per the coordinator's instruction (it runs post-batch); nothing here should block that pass.
- [x] npm test, npm run check, npm run guard, npm run build green — `npm run verify` itself deferred to the coordinator (see above)

## Gates

- [x] tests-passing — `npx vitest run`: 56 files, 862 tests, all green (claude-sonnet, 2026-08-23T15:40:00.000Z)
- [x] typecheck — `npm run check`: svelte-check 1076 files, 0 errors/warnings; `tsc -p tsconfig.node.json` clean (claude-sonnet, 2026-08-23T15:40:00.000Z)
- [x] guard — `npm run guard`: boundaries clean (1 pre-existing self-referential `no-circular` warning on ToolArgValue.svelte's recursive self-import, unrelated to this card); clean-code scan unchanged, nothing new above the 0.5 threshold (claude-sonnet, 2026-08-23T15:40:00.000Z)
- [x] build — `npm run build`: vite build green, `text-code`/`size-4`/`size-5`/`size-9` utilities confirmed present in the emitted CSS (claude-sonnet, 2026-08-23T15:40:00.000Z)

## Comments

- **claude-sonnet** (2026-08-23T15:40:00.000Z): Started. Read decisions/36 and card 98's journal for the 17-item ordered fix-list and risk notes. Re-located every fix by grepping class strings/components (line numbers in the review predate the lib->presentation/forms rename) rather than trusting the old line refs.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Icon.svelte API change (fix #13).** `src/sidepanel/components/Icon.svelte:104-115` — replaced the numeric `size?: number | string` prop with `class?: string` (default `"size-4"`), cn-merged onto both render branches (the custom sparkle/ollama inline-svg path and the `HugeiconsIcon` path); deleted the old `dimension`/`style:width/height` plumbing entirely, since a literal `size-*` class on the glyph itself beats both Hugeicons' width/height presentation attributes AND any ancestor kit `[&_svg:not([class*='size-'])]:size-N` forcing rule (the class makes that `:not()` guard skip the icon). Migrated all 22 `<Icon>` call sites off numeric `size=` onto `size-4` (glyph/16px, the workhorse — buttons, menu items, tabs, chips, inline text) or `size-5` (mark/20px — identity glyphs and empty-state icons): `MessageActions.svelte:68`, `ContextChip.svelte:99,131`, `ActivityIndicator.svelte:88`, `ProviderPicker.svelte:333,366`, `ToolCallRow.svelte:192`, `ActivityGroup.svelte:87,93`, `Transcript.svelte:216` (mark), `ToolsPanel.svelte:59,73,89,123` (mark, alongside the `Empty.Media` tile fix below), and 8 sites in `OverflowMenu.svelte`. `IconButton.svelte:87-93` — deleted the dead `glyphSize`/"compact 18px" fiction (Button always forces `size-4` regardless of its own size variant, so there never was a second glyph size); corrected the stale "40px/24px default, 32px/18px compact" doc comment to the real 36px/32px hit targets with one constant 16px glyph.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Fix #1-2 — ToolsPanel.svelte.** All four empty states re-titled to noun phrases per the spec (`ToolsPanel.svelte:60,76,94,131`): "Extensions can't reach this page", "WebMCP is off in this browser", "No page tools here", "No server tools connected" (this last state previously had no title at all — added one, since the spec names it as the fourth title). Sentence-length copy moved into `Empty.Description`. `Empty.Title` gets a call-site `class="text-base font-medium tracking-tight"` override (kit default is `text-lg`, per the Consequences note this must be overridden at the call site, not in `src/ui/components/ui/`). `Empty.Media` gets `class="size-9"` (was kit-default `size-10`) wrapping an `Icon ... class="size-5"` glyph (was `size-6` via the kit's forced rule) — matches the tile/mark pairing. Section labels (`ToolsPanel.svelte:55,119`) already matched the `label` role class; matched `PresetPicker.svelte:47,59,51,63,72` to it (`font-semibold`->`font-medium` on the Local/Hosted eyebrows and the preset-tile titles, which are `body-strong` list-row titles, not eyebrows).
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Fixes #3-6, #16.** `Transcript.svelte`: user bubble gets `text-sm` (was inheriting 16px) and `px-4 py-2.5` per the review's padding suggestion; chat empty-state paragraphs `text-xs`->`text-sm` to match the Tools panel's empty-state treatment. `Composer.svelte:193` Textarea `text-base`->`text-sm` (call-site override of the kit's `text-base md:text-sm`, since the `md:` breakpoint never fires in a 320-400px side panel). `ActivityIndicator.svelte:90` sentence span gets `text-sm`. `ToolListItem.svelte:63` tool name `font-mono font-semibold`->`font-mono text-code font-semibold`.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Fix #7 — CallLogEntry.svelte / ToolCallRow.svelte unified.** Tool name in both: `font-mono text-code`. Detail headings (Arguments/Result/Error) in both: `text-xs font-medium tracking-wide text-muted-foreground uppercase` (was `text-sm font-medium` in CallLogEntry, `text-xs font-semibold` with error-conditional colour in ToolCallRow — the error/success distinction now lives on the body text and status dot, not the label). Detail body (payload/error text) in both: `text-sm` (was `text-xs` in ToolCallRow). Metadata (duration/timestamp/"this page") already `text-xs` in both, unchanged.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Fixes #8-12.** `ProviderPicker.svelte`: sheet title `:377` `text-muted-foreground`->`text-foreground` (+ added the missing `tracking-tight` to match the `title` role exactly) — fixes the "biggest AND greyest" hierarchy inversion; reason line `:326` and badge line `:337` `text-sm`->`text-xs`; dead `size={20}` at `:333` removed as part of the Icon migration above. `ApprovalCard.svelte:95` `text-base`->`text-sm` (the one place 14px mono is allowed as a surface `title`, per the spec's mono-runs-one-step-below-sans rule). `Header.svelte:95` `font-normal text-muted-foreground`->`font-medium text-foreground` (the panel's one title now leads by weight since it can't lead by size). `Markdown.svelte`: h1 1.125rem->0.9375rem, h2 merged into the h3-h6 group at 0.875rem (was 1rem/0.9375rem split); inline code/code-header/copy-btn/table font-size switched from a private `0.8125rem` literal to `var(--text-code)`. `src/app.css` — added a hand-authored `@theme` block (clearly commented as separate from the CLI-generated one at the top of the file) defining `--text-code: 0.8125rem` / `--text-code--line-height: 1.25rem`; confirmed in the built CSS.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Fixes #14-15, #17 + general sweep.** `ContextChip.svelte:131` chevron folded into the Icon-migration pass above (18px->16px/`size-4`). `SchemaProperty.svelte:72`, `ToolArgs.svelte:33`, `ToolArgValue.svelte:48,55,57,59` — mono name/key/value now `font-mono text-code` (`ToolArgs`'/`ToolArgValue`'s key `dt` also gets `font-medium`, down from `font-semibold`); description/format/constraints untouched (already `text-sm`/`text-xs`). `McpServerRow.svelte:77` / `ProviderRow.svelte:153` `font-semibold`->`text-sm font-medium`. General sweep (`grep` for `text-base|lg|xl|2xl|3xl` across `src/sidepanel`, `src/options`, `src/ui` excluding the vendored kit): found and fixed five options section `<h2>`s (`HistorySection.svelte:75`, `SettingsSection.svelte:160,214`, `AttributionSection.svelte:20`, `McpServersSection.svelte:147`) missing `tracking-tight` from the `title` role; also added `class="text-sm"` to every `Input`/`Textarea`/`InputGroup.Input` call site that lacked an explicit size override (`HeadersEditor.svelte:71,79`, `ProviderForm.svelte:299,323,343`, `McpServerForm.svelte:432,459,495,527,536,540`, `ProviderPicker.svelte:449`) so none of them can render at 16px below the kit's `md:` breakpoint (matters most for the two side-panel instances — Composer's textarea and ProviderPicker's manual-model-id input — since a 320-400px panel never crosses 768px).
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Deferred — NOT touched, per the coordinator's instruction that a parallel agent owns `ProvidersSection.svelte` right now.** `src/options/components/ProvidersSection.svelte:359` has the identical `<h2 class="text-base font-medium">Chat providers</h2>` pattern as the five sibling section headings fixed above — it is missing `tracking-tight` from the `title` role. One-line fix for whoever picks it up next: `class="text-base font-medium tracking-tight"`. Also note for that same file/component: it uses `HugeiconsIcon` directly (not through `Icon.svelte`) in a few spots per the original grep sweep — those already render at a consistent 16px via the kit's own forcing rule, so no action is needed there, only the heading.
- **claude-sonnet** (2026-08-23T15:40:00.000Z): **Gates.** `npx vitest run`: 862/862 green (no test asserted on the old copy or on `size=`/class names, confirming decisions/36's Consequences note). `npm run check`: 0 errors. `npm run guard`: clean (pre-existing debt markers only, nothing new). `npm run build`: green; spot-checked the emitted CSS for `.text-code{...}`, `--text-code:.8125rem`, and `.size-4{}`/`.size-5{}`/`.size-9{}` all present. `npm run verify` intentionally NOT run (coordinator runs it post-batch per instructions) — this is the only reason the screenshot-matrix checklist item is left unticked. decisions/36 moved to Accepted. Moving to `review`.
