{
  "$doc": [
    "Every `throw` and `Promise.reject(` in production src/, named and reasoned.",
    "Enforced by scripts/guard-throws.mjs (`npm run guard:throws`), per",
    "decisions/34-errors-as-values.md: a KNOWN failure mode belongs in the",
    "signature as a Result<T, E>; `throw` is reserved for a violated invariant \u2014",
    "a state that cannot exist unless the code is wrong.",
    "",
    "Each entry is { match, count, reason }. `match` is a substring of the throw's",
    "own source line (text, not a line number, so it survives edits above it);",
    "`count` is EXACT \u2014 the guard fails if the real count is higher or lower, so",
    "removing a throw means removing its entry in the same diff.",
    "",
    "`reason` must name the invariant. 'defensive' is not an invariant.",
    "",
    "Seeded by card 91 with the full inventory as it stood: 15 sites in 9 files.",
    "Card 92 removed the four src/infra/chrome-storage/area.ts entries \u2014 the only",
    "throws in the tree that carried an EXPECTED failure \u2014 leaving 11 in 8 files.",
    "Test code (*.test.ts, testing/) is out of scope \u2014 see the guard's header.",
    "Entries tagged `migrates:` are known errors-as-values debt owned by a card."
  ],
  "files": {
    "src/content/relay.ts": [
      {
        "match": "throw err;",
        "count": 1,
        "reason": "Rethrow, not a new failure: the catch exists only to retry `executeTool` with the JSON-string argument form Chrome 152 wants (the 'Failed to parse input' path above). Any OTHER error is not this handler's to interpret, and swallowing it would hide a native WebMCP fault from the caller that does map it."
      }
    ],
    "src/options/app-services.ts": [
      {
        "match": "\"[webmcp] options services were already initialised",
        "count": 1,
        "reason": "Invariant: src/options/main.ts calls initOptionsServices exactly once, before mount(). A second call means two service bundles exist in one surface \u2014 two stores, two debounce maps \u2014 which is a wiring bug with no correct recovery."
      },
      {
        "match": "\"[webmcp] options services are not initialised",
        "count": 1,
        "reason": "Invariant: the composition root has run before any component reads a service. Reachable only from a module imported outside main.ts's graph \u2014 a structural mistake, and far better reported than papered over with an undefined port."
      }
    ],
    "src/sidepanel/app-services.ts": [
      {
        "match": "\"[webmcp] side panel services were already initialised",
        "count": 1,
        "reason": "Invariant: src/sidepanel/main.ts calls initSidePanelServices exactly once. Two chat stores in one panel each hold their own debounce map and index-write queue and stop serialising against each other \u2014 the interleaving card 55 fixed."
      },
      {
        "match": "\"[webmcp] side panel services are not initialised",
        "count": 1,
        "reason": "Invariant: the composition root has run before any component reads a service. Same structural-mistake case as the options twin."
      }
    ],
    "src/options/main.ts": [
      {
        "match": "\"options/index.html is missing its #app mount point.\"",
        "count": 1,
        "reason": "Invariant: #app is in this surface's OWN index.html, so its absence is a packaging bug. Card 91 introduced this in place of a `document.getElementById(\"app\")!`, which handed mount() a null target and failed further from the cause."
      }
    ],
    "src/sidepanel/main.ts": [
      {
        "match": "\"sidepanel/index.html is missing its #app mount point.\"",
        "count": 1,
        "reason": "Invariant: #app is in this surface's OWN index.html \u2014 same packaging-bug case as the options twin, same card-91 replacement of a non-null assertion."
      }
    ],
    "src/options/forms/mcpTestConnection.ts": [
      {
        "match": "\"discoverAllServerTools returned no result for a single config\"",
        "count": 1,
        "reason": "Invariant: discoverAllServerTools returns exactly one entry per config passed in (src/domain/tools/gateway.ts). A single-element input coming back empty violates that port's contract; it is not a reachable outcome of testing a connection."
      }
    ],
    "src/options/components/SettingsSection.svelte": [
      {
        "match": "throw err;",
        "count": 2,
        "reason": "migrates: card 95. Rethrows after rolling the optimistic toggle back, so the failure still surfaces rather than leaving the UI silently out of step with storage. Card 92 changed only what it rethrows: the settings port now returns Result<void, StorageError>, so `err` here is a CHECKED value rather than a caught exception, and the throw is a placeholder for the error notice card 95 renders in its place. The rollback is the behaviour that must not regress."
      }
    ]
  }
}
