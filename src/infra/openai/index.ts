// `openai` adapter — see ./README.md for what lands here and from where.
// Empty until cards 74-79 move it. Only a composition root imports this
// barrel; the domain sees the port, never the adapter.

export {};
